# Generated by the bounded-campaign orchestrator + regression test.
# Only the canonical artifacts (REPORT.md, generation_log.jsonl,
# summary.json, winner.json, golden/baseline.json) should be
# committed; the `_summary_*.json`, `_baseline_strategy.json`,
# `_tmp_*.json`, and `_regression/*` files are per-run noise that
# the orchestrator / regression test creates on every invocation.
_*.json
_tmp_*.json
_summary_*.json
_regression/

