# Audit configuration for Fluxion
# The 'paste' crate is unmaintained but is a transitive dependency via polars->faer->gemm
# It's used only for compile-time macro generation and does not affect runtime security

[advisories]
# Make unmaintained warnings informational (non-blocking)
informational_warnings = ["unmaintained"]
# Allow the specific unmaintained advisory
ignore = [
  "RUSTSEC-2024-0436"  # paste is unmaintained but used only for compile-time macros
]
