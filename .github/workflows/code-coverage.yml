name: Code Coverage

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]

jobs:
  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-rust-env
      
      - name: Install tarpaulin
        run: cargo install cargo-tarpaulin
      
      - name: Generate coverage
        # cargo-tarpaulin has issues instrumenting SIMD-heavy dependencies (pulp, ndarray, faer).
        # These cause compile-time assertion panics due to size mismatches in SIMD code (__m256d vs Complex<f64>).
        # Solution: Use --ignore-panics to skip panics during instrumentation compilation.
        run: |
          cargo tarpaulin \
            --timeout 600 \
            --out Xml \
            --ignore-panics \
            2>&1 | tee tarpaulin.log
          
          # Verify coverage XML was generated
          if [ ! -f cobertura.xml ]; then
            echo "Failed to generate coverage report"
            exit 1
          fi
      
      - name: Upload to codecov
      # Only upload if success AND we are NOT running locally in ACT
        if: success() && !env.ACT
        uses: codecov/codecov-action@v3
        with:
          files: ./tarpaulin.xml
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
