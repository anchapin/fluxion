---
phase: 17-internal-loads
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/17-internal-loads/17-VERIFICATION.md]
autonomous: true
requirements: []
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "LOADS-05 orphaned requirement removed from verification"
    - "Phase 17 verification only references valid requirements"
  artifacts:
    - path: ".planning/phases/17-internal-loads/17-VERIFICATION.md"
      provides: "Updated verification report without LOADS-05 reference"
      contains: "No mention of LOADS-05"
  key_links:
    - from: "17-VERIFICATION.md"
      to: "17-CONTEXT.md"
      via: "Consistent requirement references"
      pattern: "LOADS-[0-9]{2}"
---

<objective>
Remove orphaned LOADS-05 requirement reference from Phase 17 verification report.

Purpose: LOADS-05 was mentioned in the verification prompt but does not exist in REQUIREMENTS.md. This plan removes the orphaned requirement reference from the verification report to maintain consistency between verification and requirements documents.
Output: Clean verification report that only references valid requirement IDs.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-internal-loads/17-VERIFICATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

## Issue Details

From 17-VERIFICATION.md:
- Truth #18 (Failed): "LOADS-05 requirement (from prompt)"
- Reason: "LOADS-05 not found in REQUIREMENTS.md - orphaned requirement"
- Artifacts: LOADS-05 requirement not found in requirements document
- Missing: "Either add LOADS-05 to REQUIREMENTS.md or remove it from phase verification"

## Analysis

LOADS-05 was mentioned in the verification prompt but was never defined in REQUIREMENTS.md. Looking at the requirements document:

Phase 17 Internal Loads section (lines 40-45 in REQUIREMENTS.md):
- LOADS-01: Implement internal lighting loads with schedules
- LOADS-02: Implement internal equipment loads with schedules
- LOADS-03: Implement occupancy/people loads with schedules
- LOADS-04: Support customizable load profiles (weekday/weekend/holiday)

There are only 4 LOADS requirements for Phase 17, not 5. LOADS-05 does not exist and was likely a typo or copy-paste error in the verification prompt.

The ROADMAP.md confirms this:
- Phase 17 Requirements: LOADS-01, LOADS-02, LOADS-03, LOADS-04 (4 requirements total)
- No LOADS-05 mentioned in any phase context or plan

## Solution

Remove the orphaned LOADS-05 reference from 17-VERIFICATION.md:
1. Remove Truth #18 from the Observable Truths table
2. Update the score from "16/18 truths verified" to "16/17 truths verified"
3. Remove LOADS-05 from the Requirements Coverage table
4. Remove LOADS-05 from the Gaps Summary section
5. Update the Overall Assessment to reflect the corrected gap count

This makes the verification report consistent with the actual requirements defined in REQUIREMENTS.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove LOADS-05 from Observable Truths table</name>
  <files>.planning/phases/17-internal-loads/17-VERIFICATION.md</files>
  <action>Remove Truth #18 from the Observable Truths table in 17-VERIFICATION.md (around line 55).

Remove this row:
```
| 18  | LOADS-05 requirement (from prompt) | ✗ FAILED | LOADS-05 not found in REQUIREMENTS.md - orphaned requirement |
```

Then update the score line from:
```
**Score:** 16/18 truths verified
```

To:
```
**Score:** 16/17 truths verified
```

This removes the orphaned requirement and corrects the verification score.</action>
  <verify>grep -n "LOADS-05" .planning/phases/17-internal-loads/17-VERIFICATION.md | wc -l</verify>
  <done>No references to LOADS-05 remain in the file</done>
</task>

<task type="auto">
  <name>Task 2: Remove LOADS-05 from Requirements Coverage table</name>
  <files>.planning/phases/17-internal-loads/17-VERIFICATION.md</files>
  <action>Remove the LOADS-05 row from the Requirements Coverage table in 17-VERIFICATION.md (around line 95).

Remove this row:
```
| LOADS-05 | Prompt only | [Not found in REQUIREMENTS.md] | ✗ BLOCKED | LOADS-05 mentioned in prompt but not in REQUIREMENTS.md - orphaned requirement |
```

This ensures the requirements coverage table only lists valid requirements that exist in REQUIREMENTS.md.</action>
  <verify>grep -A1 "Requirements Coverage" .planning/phases/17-internal-loads/17-VERIFICATION.md | grep -c "LOADS-"</verify>
  <done>Only LOADS-01 through LOADS-04 appear in Requirements Coverage table</done>
</task>

<task type="auto">
  <name>Task 3: Remove LOADS-05 from Gaps Summary section</name>
  <files>.planning/phases/17-internal-loads/17-VERIFICATION.md</files>
  <action>Update the Gaps Summary section in 17-VERIFICATION.md (around line 131) to remove the LOADS-05 gap and correct the gap count from "Two gaps" to "One gap".

Change from:
```
**Gaps Summary**

Two gaps blocking full goal achievement:

1. **Test Compilation Errors** (Partial):
   ...

2. **Orphaned Requirement LOADS-05** (Failed):
   - Verification prompt mentioned LOADS-05 as a requirement ID
   - LOADS-05 does not exist in `.planning/REQUIREMENTS.md`
   - Cannot verify this requirement as it was never defined
   - **Fix:** Either add LOADS-05 to REQUIREMENTS.md with proper description, or remove it from phase verification
```

To:
```
**Gaps Summary**

One gap blocking full goal achievement:

1. **Test Compilation Errors** (Partial):
   - `solve_single_step` signature was updated to accept lighting, equipment, occupancy parameters
   - Test code at lines 4405 and 4743 in `src/sim/engine.rs` still uses old 5-argument signature
   - This prevents test execution and validation of internal loads integration
   - **Fix:** Update test code to pass `None, None, None` for the three new optional parameters
```

This removes the documentation issue and focuses on the single remaining technical gap.</action>
  <verify>grep -n "Orphaned Requirement LOADS-05" .planning/phases/17-internal-loads/17-VERIFICATION.md</verify>
  <done>No references to orphaned requirement LOADS-05 in Gaps Summary</done>
</task>

<task type="auto">
  <name>Task 4: Update Overall Assessment section</name>
  <files>.planning/phases/17-internal-loads/17-VERIFICATION.md</files>
  <action>Update the Overall Assessment section in 17-VERIFICATION.md (around line 147) to reflect that only one gap remains.

Change from:
```
**Overall Assessment:**
Phase 17 implementation is substantively complete and functional. All core internal loads functionality (weekly schedules, equipment trait, profile loading, thermal model integration) is implemented and wired correctly. The only blockers are test code compilation errors and an orphaned requirement reference, neither of which affect production code.
```

To:
```
**Overall Assessment:**
Phase 17 implementation is substantively complete and functional. All core internal loads functionality (weekly schedules, equipment trait, profile loading, thermal model integration) is implemented and wired correctly. The only blocker is test code compilation errors, which do not affect production code.
```

This accurately reflects the single remaining gap after removing the orphaned requirement.</action>
  <verify>grep -n "Overall Assessment" .planning/phases/17-internal-loads/17-VERIFICATION.md -A3</verify>
  <done>Overall Assessment correctly describes single remaining gap</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify no LOADS-05 references remain in 17-VERIFICATION.md: grep -n "LOADS-05" .planning/phases/17-internal-loads/17-VERIFICATION.md
2. Verify Observable Truths table has 17 entries (not 18): grep -c "^[0-9]" .planning/phases/17-internal-loads/17-VERIFICATION.md | head -20
3. Verify Requirements Coverage table has 4 LOADS requirements: grep -c "LOADS-" .planning/phases/17-internal-loads/17-VERIFICATION.md
4. Verify Gaps Summary mentions only one gap
</verification>

<success_criteria>
1. No references to LOADS-05 exist anywhere in 17-VERIFICATION.md
2. Observable Truths table contains 17 truths (1-17) with score "16/17 truths verified"
3. Requirements Coverage table contains only LOADS-01 through LOADS-04 (4 requirements)
4. Gaps Summary describes one gap (test compilation errors) instead of two
5. Overall Assessment accurately reflects single remaining blocker
6. Verification report is consistent with REQUIREMENTS.md (which has no LOADS-05)
</success_criteria>

<output>
After completion, create `.planning/phases/17-internal-loads/17-06-SUMMARY.md`
</output>
