---
phase: 11
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/sim/engine.rs
autonomous: true
requirements:
  - ROBUST-01
  - BUG-03
must_haves:
  truths:
    - "apply_parameters() detects NaN and Inf values before range validation"
    - "Error messages specify which parameter is invalid, the invalid value, and valid range"
    - "Misleading error messages corrected (e.g., vague 'invalid parameters' replaced with specific details)"
  artifacts:
    - path: "src/sim/engine.rs"
      provides: "NaN/Inf detection in ThermalModel.apply_parameters()"
      contains: "apply_parameters"
    - path: "src/lib.rs"
      provides: "Corrected error messages across Python API"
      contains: "validate_parameters"
  key_links:
    - from: "BatchOracle.validate_parameters()"
      to: "NaN/Inf detection logic"
      via: "is_finite() check"
      pattern: "is_finite.*validate_parameters"
    - from: "ThermalModel.apply_parameters()"
      to: "NaN/Inf detection"
      via: "is_finite() check before range validation"
      pattern: "is_finite.*apply_parameters"
    - from: "Error messages"
      to: "User-friendly format"
      via: "Parameter index, value, and valid range"
      pattern: "Parameter at index.*out of range"
---

<objective>
Strengthen input validation with NaN/Inf detection and correct misleading error messages

Purpose: Catch invalid parameters (NaN, Inf) before they cause physics failures; improve error message clarity to help users quickly identify and fix parameter issues

Output: NaN/Inf detection in apply_parameters() and validate_parameters(), specific error messages with parameter index/value/range, corrected misleading messages
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-API-Robustness/11-RESEARCH.md

# Current validation issues (from src/lib.rs lines 539-576)
- BatchOracle::validate_parameters() checks range bounds but NOT NaN/Inf
- Range check: u_value >= MIN_U_VALUE && u_value <= MAX_U_VALUE
- Problem: NaN passes range check (NaN >= 0.1 is false, NaN <= 5.0 is false)
- Error messages: Generic "Parameter out of range" without specifying which parameter
- ROBUST-01 requirement: "Strengthen input validation in apply_parameters (range checking, NaN/Inf detection)"
- BUG-03 requirement: "Correct inaccurate error messages or misleading diagnostics"

# Pitfall from 11-RESEARCH.md
- NaN/Inf values pass range checks like value >= min && value <= max
- Solution: Use value.is_finite() before range validation

# Error message improvement requirements
- Include parameter index
- Include actual value
- Include valid range
- Actionable guidance (what to fix)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NaN/Inf detection to BatchOracle.validate_parameters()</name>
  <files>src/lib.rs</files>
  <action>Enhance BatchOracle::validate_parameters() to detect NaN and Inf values:

1. Before range validation, add NaN/Inf check for each parameter:
   - Iterate over params with enumerate()
   - Check !value.is_finite() (catches both NaN and Inf)
   - Return Result::Err with specific message if invalid:
     * Format: "Parameter at index {} is not finite (got: {})" or "Parameter at index {} is NaN" / "Parameter at index {} is infinite"
     * Include index, actual value (f64::to_string()), and guidance

2. Update existing range validation messages to include:
   - Parameter index
   - Actual value with formatting (e.g., "{:.2} W/m²K")
   - Valid range [min, max]
   - Parameter name (e.g., "Window U-value")

3. Example improved messages:
   - Old: "Parameter out of range"
   - New: "Window U-value (0.05 W/m²K) out of range [0.1, 5.0]"
   - NaN: "Parameter at index 0 is NaN (cannot be used in simulation)"
   - Inf: "Parameter at index 1 is infinite (cannot be used in simulation)"

Pattern from 11-RESEARCH.md: Use is_finite() before range validation; include parameter index, value, and range in error messages
</action>
  <verify>
    <automated>cargo test --lib test_nan_inf_detection -x</automated>
  </verify>
  <done>BatchOracle.validate_parameters() detects NaN/Inf before range validation with specific error messages</done>
</task>

<task type="auto">
  <name>Task 2: Add NaN/Inf detection to ThermalModel.apply_parameters()</name>
  <files>src/sim/engine.rs</files>
  <action>Enhance ThermalModel::apply_parameters() to detect NaN and Inf values:

1. Add validation at start of apply_parameters():
   - Check all values in params slice for NaN/Inf using params.iter().all(|v| v.is_finite())
   - If any value is not finite, return Result::Err with specific message:
     * Format: "Parameter at index {} is not finite (got: {})"
     * Include which parameter failed and its value

2. Document the validation:
   - Add doc comment explaining NaN/Inf detection prevents physics failures
   - Explain why NaN/Inf cannot be used (causes infinite loops, NaN propagation)

3. Apply parameter bounds checks after NaN/Inf validation:
   - Check window_u_value in [MIN_U_VALUE, MAX_U_VALUE]
   - Check hvac_setpoint in [MIN_SETPOINT, MAX_SETPOINT]
   - Return specific error messages with parameter name, value, and valid range

Pattern from 11-RESEARCH.md: Catch NaN/Inf at the source (apply_parameters) before they propagate to physics calculations
</action>
  <verify>
    <automated>cargo test --lib test_thermal_model_nan_detection -x</automated>
  </verify>
  <done>ThermalModel.apply_parameters() validates parameters and raises specific errors for NaN/Inf and out-of-range values</done>
</task>

<task type="auto">
  <name>Task 3: Correct misleading error messages across Python API</name>
  <files>src/lib.rs</files>
  <action>Audit and fix misleading error messages in Python API:

1. Review all error messages in #[pymethods] and map_err() calls:
   - Search for generic messages: "error", "failed", "invalid"
   - Identify vague messages that don't specify what went wrong

2. Replace misleading messages with specific, actionable messages:
   - Bad: "Invalid parameters"
   - Good: "Window U-value (0.05 W/m²K) out of range [0.1, 5.0]"
   - Bad: "ONNX error"
   - Good: "ONNX Runtime inference failed: {specific_error}, falling back to analytical mode"
   - Bad: "Simulation failed"
   - Good: "Simulation failed due to NaN in zone temperatures at timestep {step}"

3. Common error message patterns to implement:
   - Parameter validation: "{ParameterName} ({value} {unit}) out of range [{min}, {max}]"
   - NaN/Inf: "Parameter at index {index} is {NaN|infinite} (value: {value})"
   - ONNX failure: "ONNX inference failed: {error}, falling back to analytical"
   - Simulation failure: "Physics calculation failed at timestep {step}: {reason}"

4. Ensure all error messages provide:
   - What went wrong (specific issue)
   - Where it went wrong (parameter index, line, timestep)
   - What to do (valid range, fallback action)

Pattern from 11-RESEARCH.md: Error messages should be specific enough for users to self-diagnose and fix issues without debugging
</action>
  <verify>
    <automated>cargo test --lib test_error_messages -x</automated>
  </verify>
  <done>All Python API error messages are specific and actionable; misleading generic messages replaced with detailed descriptions</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run cargo test --lib to verify all code compiles and tests pass
2. Test NaN detection:
   - Pass NaN parameter to validate_parameters() -> raises ValidationError with "Parameter at index X is NaN"
   - Pass Inf parameter -> raises ValidationError with "Parameter at index X is infinite"
3. Test error message clarity:
   - Out-of-range parameter -> "Window U-value (0.05 W/m²K) out of range [0.1, 5.0]"
   - Verify parameter name, value, unit, and valid range all present
4. Verify no generic error messages remain:
   - Search for "error", "failed", "invalid" in error paths
   - Confirm all are accompanied by specific details
</verification>

<success_criteria>
- BatchOracle.validate_parameters() detects NaN and Inf before range validation
- ThermalModel.apply_parameters() validates all parameters and rejects NaN/Inf
- Error messages include parameter index, value, and valid range
- Error messages include parameter name (e.g., "Window U-value", "HVAC setpoint")
- No generic error messages remain; all errors are specific and actionable
- Users can self-diagnose parameter issues from error messages without debugging
</success_criteria>

<output>
After completion, create `.planning/phases/11-API-Robustness/11-03-SUMMARY.md`
</output>
