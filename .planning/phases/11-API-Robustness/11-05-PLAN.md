---
phase: 11
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/ai/surrogate.rs
  - src/sim/engine.rs
autonomous: true
requirements:
  - ROBUST-03
  - ROBUST-04
  - ROBUST-05
must_haves:
  truths:
    - "Logging verbosity controlled via RUST_LOG environment variable (trace/debug/info/warn/error)"
    - "Extreme weather data (missing values, out-of-range temperatures) handled without panics"
    - "SessionPool initialization is thread-safe and safe for concurrent ONNX loading"
  artifacts:
    - path: "src/lib.rs"
      provides: "Logging verbosity control and extreme data handling"
      contains: "log::warn, log::error"
    - path: "src/ai/surrogate.rs"
      provides: "Thread-safe SessionPool initialization"
      contains: "SessionPool::new"
    - path: "src/sim/engine.rs"
      provides: "Extreme weather data validation"
      contains: "validate_weather_data"
  key_links:
    - from: "RUST_LOG environment variable"
      to: "Log output verbosity"
      via: "env_logger or similar crate"
      pattern: "RUST_LOG.*=.*trace|debug|info|warn|error"
    - from: "Weather data validation"
      to: "Error handling"
      via: "missing values and out-of-range checks"
      pattern: "weather.*missing|out.of.range"
    - from: "SessionPool initialization"
      to: "Thread safety"
      via: "Arc<Mutex<>> or OnceLock"
      pattern: "Arc.*Mutex.*SessionPool|OnceLock.*SessionPool"
---

<objective>
Improve logging verbosity control, handle extreme weather data safely, and verify thread-safe SessionPool initialization

Purpose: Provide users control over logging verbosity for debugging; prevent panics from extreme weather data; ensure ONNX SessionPool can be safely initialized concurrently

Output: RUST_LOG-based logging control, weather data validation for missing/out-of-range values, thread-safe SessionPool initialization
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-API-Robustness/11-RESEARCH.md

# Current logging status
- Uses log crate with env_logger (from Cargo.toml dependencies)
- No explicit RUST_LOG documentation or examples
- ROBUST-03 requirement: "Improve logging verbosity control (tracing vs debug vs info)"

# Weather data handling
- Weather data loaded from TMY files (Denver TMY from v0.2)
- No explicit validation for missing values (NaN) or out-of-range temperatures
- ROBUST-04 requirement: "Handle extreme weather data (missing values, out-of-range temperatures)"

# SessionPool initialization
- SurrogateManager has SessionPool for concurrent ONNX inference
- ROBUST-05 requirement: "Ensure thread-safe initialization of global ONNX SessionPool"
- Need to verify current implementation is thread-safe

# Logging levels from 11-RESEARCH.md
- trace: Very detailed, step-by-step debugging
- debug: Detailed information for debugging
- info: General informational messages
- warn: Warning messages for recoverable issues
- error: Error messages for failures
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve logging verbosity control and documentation</name>
  <files>src/lib.rs</files>
  <action>Enhance logging verbosity control with RUST_LOG environment variable:

1. Add logging initialization in main/entry point:
   - Use env_logger::init() or log::logger() initialization
   - Ensure RUST_LOG environment variable is respected

2. Add log statements at appropriate levels:
   - log::trace: Detailed step-by-step in thermal calculations (apply_parameters, solve_timesteps inner loop)
   - log::debug: Detailed information for debugging (parameter values, temperatures, loads)
   - log::info: General informational messages (model loaded, simulation started/completed)
   - log::warn: Warning messages for recoverable issues (ONNX fallback, out-of-range parameters)
   - log::error: Error messages for failures (NaN propagation, simulation crashes)

3. Update doc comments to document RUST_LOG usage:
   - Add to BatchOracle and Model doc comments:
     * "Use RUST_LOG environment variable to control logging verbosity (e.g., RUST_LOG=debug)"
     * Explain log levels: trace, debug, info, warn, error
   - Add examples in documentation:
     * RUST_LOG=info python -c "import fluxion; ..."
     * RUST_LOG=debug python -c "import fluxion; ..."
     * RUST_LOG=fluxion=trace python -c "import fluxion; ..."

4. Add log statements to key paths:
   - apply_parameters: log parameter values at debug level
   - solve_timesteps: log timestep progress at info level (every 1000 timesteps)
   - ONNX fallback: log warning at warn level
   - Validation failures: log error at error level

Pattern from 11-RESEARCH.md: Integrate with Python logging module via RUST_LOG; provide clear documentation for users
</action>
  <verify>
    <automated>cargo test --lib test_logging_control -x</automated>
  </verify>
  <done>RUST_LOG environment variable controls logging verbosity; documentation explains trace/debug/info/warn/error levels</done>
</task>

<task type="auto">
  <name>Task 2: Add extreme weather data validation</name>
  <files>src/lib.rs</files>
  <action>Add validation for extreme weather data (missing values, out-of-range temperatures):

1. Add validate_weather_data() function:
   - fn validate_weather_data(weather: &[WeatherData]) -> Result<(), FluxionError>
   - Check for missing values: weather.iter().all(|w| w.is_complete())
   - Check for NaN temperatures: weather.iter().all(|w| w.temperature.is_finite())
   - Check for out-of-range temperatures: -50°C <= temp <= 60°C (reasonable Earth temperature range)
   - Return specific error messages:
     * "Missing weather data at timestep {index}: {missing_field}"
     * "Temperature is NaN at timestep {index}"
     * "Temperature {temp}°C at timestep {index} out of range [-50, 60]°C"

2. Call validate_weather_data() in BatchOracle and Model initialization:
   - In new() methods, after loading weather data
   - If validation fails, return ValidationError with specific message
   - Log warning at warn level for recoverable issues

3. Handle missing values gracefully (if recoverable):
   - Interpolate missing temperatures from neighboring timesteps
   - Use reasonable defaults for missing solar/insolation values
   - Log interpolation at debug level

4. Document weather data validation:
   - Add doc comment explaining validation rules
   - Note that extreme values are validated to prevent physics failures
   - Explain that missing values are interpolated when possible

Pattern from 11-RESEARCH.md: Validate weather data to prevent panics; handle extreme values gracefully
</action>
  <verify>
    <automated>cargo test --lib test_extreme_weather -x</automated>
  </verify>
  <done>Weather data validated for missing values and out-of-range temperatures; errors raised with specific messages; recoverable issues handled gracefully</done>
</task>

<task type="auto">
  <name>Task 3: Verify and ensure thread-safe SessionPool initialization</name>
  <files>src/ai/surrogate.rs</files>
  <action>Verify and ensure thread-safe SessionPool initialization:

1. Review current SessionPool implementation:
   - Check if SessionPool uses Arc<Mutex<>> or similar thread-safe wrapper
   - Check if initialization happens in lazy_static or OnceLock
   - Identify any potential race conditions

2. If not thread-safe, fix initialization:
   - Use std::sync::OnceLock for lazy, thread-safe initialization
   - Or use Arc<Mutex<SessionPool>> with proper locking
   - Ensure only one ONNX session pool is created per model path

3. Add concurrent loading test:
   - Create test that loads multiple ONNX models concurrently
   - Use rayon::par_iter() or tokio spawn multiple threads
   - Verify no race conditions or panics
   - Check that all threads get valid SessionPool instances

4. Document thread-safety guarantees:
   - Add doc comment: "SessionPool initialization is thread-safe and can be called from multiple threads concurrently"
   - Explain the synchronization mechanism (OnceLock, Arc<Mutex<>>, etc.)
   - Note that each model path has its own SessionPool

5. Add logging for SessionPool initialization:
   - log::info("Initializing SessionPool for model: {model_path}")
   - log::debug("SessionPool initialized with {session_count} sessions")

Pattern from 11-RESEARCH.md: Ensure global ONNX SessionPool can be safely initialized concurrently without race conditions
</action>
  <verify>
    <automated>cargo test --lib test_session_pool_thread_safe -x</automated>
  </verify>
  <done>SessionPool initialization is thread-safe; concurrent loading tests pass; no race conditions detected</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run cargo test --lib to verify all code compiles and tests pass
2. Test logging verbosity:
   - RUST_LOG=error python -c "import fluxion; fluxion.BatchOracle().evaluate_population([[1.5, 20.0]], False)" -> Only error logs visible
   - RUST_LOG=debug python -c "..." -> Debug logs showing parameter values, timestep progress
   - RUST_LOG=trace python -c "..." -> Trace logs showing detailed thermal calculations
3. Test extreme weather data:
   - Pass weather data with NaN temperature -> ValidationError raised with specific message
   - Pass weather data with temperature 100°C -> ValidationError raised with out-of-range message
   - Pass weather data with missing values -> Interpolated if recoverable, error if not
4. Test SessionPool thread safety:
   - Load ONNX model from multiple threads concurrently -> No race conditions
   - Verify all threads get valid SessionPool instances
</verification>

<success_criteria>
- RUST_LOG environment variable controls logging verbosity (trace/debug/info/warn/error)
- Documentation explains how to use RUST_LOG and what each level shows
- Weather data validated for missing values and out-of-range temperatures
- Missing weather values interpolated when possible
- SessionPool initialization is thread-safe (OnceLock or Arc<Mutex<>>)
- Concurrent ONNX loading tests pass without race conditions
- Log statements at appropriate levels throughout codebase
</success_criteria>

<output>
After completion, create `.planning/phases/11-API-Robustness/11-05-SUMMARY.md`
</output>
