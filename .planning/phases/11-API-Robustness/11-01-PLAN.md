---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/lib/error.rs
autonomous: true
requirements:
  - API-01
  - API-02
  - API-04
must_haves:
  truths:
    - "Python users can catch specific exception types (ValidationError, SurrogateError, SimulationError)"
    - "Parameter bounds are discoverable via get_parameter_bounds() method"
    - "Python validate_parameters() method raises ValidationError with clear messages"
  artifacts:
    - path: "src/lib/error.rs"
      provides: "Custom exception types and error handling"
      contains: "create_exception!(fluxion, FluxionError, PyException)"
    - path: "src/lib.rs"
      provides: "Parameter bounds and validation methods"
      exports: ["get_parameter_bounds", "validate_parameters"]
  key_links:
    - from: "src/lib.rs"
      to: "src/lib/error.rs"
      via: "use fluxion::error::*"
      pattern: "use fluxion::error::"
    - from: "BatchOracle.get_parameter_bounds()"
      to: "ParameterBounds struct"
      via: "PyClass derive"
      pattern: "fn get_parameter_bounds.*ParameterBounds"
    - from: "BatchOracle.validate_parameters()"
      to: "ValidationError exception"
      via: "Result<(), PyErr> conversion"
      pattern: "validate_parameters.*ValidationError"
---

<objective>
Implement custom exception types and expose parameter discovery/validation methods for Python API

Purpose: Improve Python API usability by providing domain-specific exceptions and programmatic access to parameter bounds, reducing user confusion and improving error handling

Output: Custom exception hierarchy (FluxionError, ValidationError, SurrogateError, SimulationError), ParameterBounds struct, get_parameter_bounds() method, validate_parameters() Python method
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-API-Robustness/11-RESEARCH.md

# Current API patterns from src/lib.rs
- Current errors: Generic PyRuntimeError and PyValueError (lines 63, 80, 118, 499, 801, 838, 874, 1005, 1736, 1794-1818)
- Existing Rust validation: BatchOracle::validate_parameters() (lines 539-576) returns Result<(), String>
- Parameter bounds: MIN_U_VALUE (0.1), MAX_U_VALUE (5.0) constants (lines 526-527)
- No Python-side validate_parameters() method
- No ParameterBounds struct exposed to Python

# Key constants for parameter bounds
- MIN_U_VALUE: 0.1 W/m²K
- MAX_U_VALUE: 5.0 W/m²K
- MIN_HEATING_SETPOINT: 15.0°C
- MAX_HEATING_SETPOINT: 30.0°C
- MIN_COOLING_SETPOINT: 15.0°C
- MAX_COOLING_SETPOINT: 30.0°C
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create custom exception types module</name>
  <files>src/lib/error.rs</files>
  <action>Create new error module with PyO3 custom exceptions:

1. Create src/lib/error.rs with:
   - Rust error enum: FluxionError with variants (Validation, Surrogate, Simulation)
   - PyO3 exception macros: create_exception!(fluxion, FluxionError, PyException), create_exception!(fluxion, ValidationError, FluxionError), create_exception!(fluxion, SurrogateError, FluxionError), create_exception!(fluxion, SimulationError, FluxionError)
   - From<FluxionError> for PyErr implementation to convert Rust errors to Python exceptions
   - Use thiserror derive macros for Display and Error implementations

2. Pattern from 11-RESEARCH.md:
   - Validation errors for parameter bounds, NaN/Inf
   - Surrogate errors for ONNX Runtime failures
   - Simulation errors for physics failures, NaN propagation

3. Register exceptions in fluxion module (pymodule fn):
   - Add "FluxionError", "ValidationError", "SurrogateError", "SimulationError" to module

Do NOT: Use existing PyRuntimeError or PyValueError for new error paths (keep for backward compatibility only)
</action>
  <verify>
    <automated>cargo test --lib fluxion::error -x</automated>
  </verify>
  <done>Custom exception types defined and registered in Python module</done>
</task>

<task type="auto">
  <name>Task 2: Create ParameterBounds struct and expose to Python</name>
  <files>src/lib.rs</files>
  <action>Create ParameterBounds struct as PyO3 class:

1. Add ParameterBounds struct with #[pyclass] and #[derive(Clone)]:
   - Fields: min_u_value, max_u_value, min_heating_setpoint, max_heating_setpoint, min_cooling_setpoint, max_cooling_setpoint
   - Use #[pyo3(get)] for read-only access to all fields
   - Values from BatchOracle constants: MIN_U_VALUE, MAX_U_VALUE, MIN_HEATING_SETPOINT, MAX_HEATING_SETPOINT, MIN_COOLING_SETPOINT, MAX_COOLING_SETPOINT

2. Add static method to ParameterBounds:
   - #[staticmethod] fn get_bounds() -> Self (returns ParameterBounds with constants)

3. Add get_parameter_bounds() to BatchOracle and Model Python APIs:
   - #[pymethods] impl: fn get_parameter_bounds(&self) -> ParameterBounds
   - Returns ParameterBounds::get_bounds()

4. Register ParameterBounds in fluxion module (pymodule fn)

Pattern from 11-RESEARCH.md: Programmatic discovery of valid parameter ranges for optimization libraries
</action>
  <verify>
    <automated>cargo test --lib test_parameter_bounds -x</automated>
  </verify>
  <done>ParameterBounds struct accessible from Python, get_parameter_bounds() method available on BatchOracle and Model</done>
</task>

<task type="auto">
  <name>Task 3: Expose validate_parameters() to Python API</name>
  <files>src/lib.rs</files>
  <action>Add Python-side validate_parameters() method to BatchOracle and Model:

1. Add validate_parameters() to BatchOracle #[pymethods]:
   - fn validate_parameters(&self, params: Vec<f64>) -> PyResult<()>
   - Calls existing BatchOracle::validate_parameters(&params)
   - Maps String error to ValidationError::new_err(msg)
   - Returns Ok(()) if valid, raises ValidationError if invalid

2. Add validate_parameters() to Model #[pymethods]:
   - Same pattern as BatchOracle
   - Reuses Model's internal validation logic (if exists) or creates from BatchOracle pattern

3. Enhance error messages in Rust validate_parameters():
   - Include parameter index, value, and valid range in error messages
   - Example: "Window U-value (0.05 W/m²K) out of range [0.1, 5.0]"
   - Check for NaN/Inf before range validation using is_finite()

Pattern from 11-RESEARCH.md: Pythonic APIs should raise exceptions on invalid input with clear, actionable messages
</action>
  <verify>
    <automated>cargo test --lib test_python_validate_parameters -x</automated>
  </verify>
  <done>Python validate_parameters() method raises ValidationError with clear messages for invalid parameters</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run cargo test --lib to verify all new code compiles and tests pass
2. Verify Python can import fluxion and access new APIs:
   - fluxion.FluxionError, fluxion.ValidationError, fluxion.SurrogateError, fluxion.SimulationError exist
   - BatchOracle.get_parameter_bounds() returns ParameterBounds with correct values
   - BatchOracle.validate_parameters() raises ValidationError for invalid params
3. Test exception hierarchy: ValidationError inherits from FluxionError
</verification>

<success_criteria>
- Custom exception types (FluxionError, ValidationError, SurrogateError, SimulationError) registered in Python module
- ParameterBounds struct exposes all design variable ranges programmatically
- get_parameter_bounds() method available on BatchOracle and Model
- validate_parameters() Python method raises ValidationError with clear messages including parameter index, value, and valid range
- NaN/Inf detection before range validation
- All existing tests continue to pass (backward compatibility maintained)
</success_criteria>

<output>
After completion, create `.planning/phases/11-API-Robustness/11-01-SUMMARY.md`
</output>
