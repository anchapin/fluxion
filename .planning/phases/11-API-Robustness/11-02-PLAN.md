---
phase: 11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/lib/parameters.rs
autonomous: true
requirements:
  - API-03
  - API-05
must_haves:
  truths:
    - "BuildingParameters struct provides type-safe parameter access with named fields"
    - "BuildingParameters validates constraints on construction (ranges, NaN/Inf)"
    - "Return types consistent between Model and BatchOracle methods"
  artifacts:
    - path: "src/lib/parameters.rs"
      provides: "BuildingParameters typed struct"
      contains: "struct BuildingParameters"
    - path: "src/lib.rs"
      provides: "Type-safe parameter API and standardized return types"
      exports: ["BuildingParameters", "Model.simulate", "BatchOracle.evaluate_population"]
  key_links:
    - from: "BuildingParameters struct"
      to: "Vec<f64> conversion"
      via: "to_vec() method"
      pattern: "fn to_vec.*Vec<f64>"
    - from: "BatchOracle.evaluate_population()"
      to: "Result<Vec<f64>, PyErr> return type"
      via: "PyResult<Vec<f64>>"
      pattern: "fn evaluate_population.*PyResult"
    - from: "Model.simulate()"
      to: "Result<f64, PyErr> return type"
      via: "PyResult<f64>"
      pattern: "fn simulate.*PyResult"
---

<objective>
Add typed BuildingParameters struct and standardize return types across Model and BatchOracle APIs

Purpose: Improve type safety and reduce misuse of parameter vectors while maintaining backward compatibility; standardize error handling patterns across Python API

Output: BuildingParameters PyO3 class with validation, optional conversion to/from Vec<f64>, consistent Result<T, PyErr> return types
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-API-Robustness/11-RESEARCH.md
@.planning/phases/11-API-Robustness/11-01-PLAN.md

# Current API patterns
- Parameter vectors use Vec<f64> throughout (no semantic meaning, error-prone)
- Current return types mixed: Some methods use PyResult<T>, others use bare types
- BatchOracle.evaluate_population() returns Vec<f64> (no error propagation to Python)
- Model.simulate() returns f64 (no error propagation to Python)

# Decision from 11-RESEARCH.md
- BuildingParameters should coexist with Vec<f64> for backward compatibility
- Validate constraints on construction (ranges, NaN/Inf)
- Provide to_vec() method for backward compatibility

# Return type standardization requirements
- API-05: "Standardize return types across Model and BatchOracle methods"
- Pattern: Use PyResult<T> for all methods that can fail

# IMPORTANT: Separation from Plan 06 (gap closure plan)
- Plan 02 Task 3: Focus ONLY on adding PyResult<T> to bare return types (e.g., Model.simulate() returning f64 → PyResult<f64>)
- Plan 06 Task 3: Focus ONLY on updating existing PyResult methods to use ? operator instead of map_err
- This ensures no duplicate work on same methods
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BuildingParameters typed struct</name>
  <files>src/lib/parameters.rs</files>
  <action>Create BuildingParameters struct as PyO3 class with type-safe API:

1. Create src/lib/parameters.rs with BuildingParameters struct:
   - #[pyclass] and #[derive(Clone, Debug)]
   - Named fields: window_u_value (f64), heating_setpoint (f64), cooling_setpoint (f64)
   - Use #[pyo3(get, set)] for read/write access from Python
   - Validate on construction in #[new] method:
     * Check window_u_value in [MIN_U_VALUE, MAX_U_VALUE]
     * Check heating_setpoint in [MIN_HEATING_SETPOINT, MAX_HEATING_SETPOINT]
     * Check cooling_setpoint in [MIN_COOLING_SETPOINT, MAX_COOLING_SETPOINT]
     * Check all values are finite (!value.is_nan() && !value.is_infinite())
     * Raise ValidationError with clear message if invalid

2. Add to_vec() method:
   - #[pymethods] impl: fn to_vec(&self) -> Vec<f64>
   - Returns vec![self.window_u_value, self.heating_setpoint, self.cooling_setpoint]
   - Enables backward compatibility with existing Vec<f64> APIs

3. Add TryFrom<Vec<f64>> implementation:
   - impl TryFrom<Vec<f64>> for BuildingParameters
   - Converts parameter vector to BuildingParameters with validation
   - Returns Error if invalid length or values

4. Register BuildingParameters in fluxion module (pymodule fn in lib.rs)

Pattern from 11-RESEARCH.md: Type safety via named fields reduces Vec<f64> misuse; coexistence maintains backward compatibility
</action>
  <verify>
    <automated>cargo test --lib test_building_parameters -x</automated>
  </verify>
  <done>BuildingParameters struct with named fields, validation on construction, to_vec() conversion method</done>
</task>

<task type="auto">
  <name>Task 2: Update BatchOracle methods to accept BuildingParameters</name>
  <files>src/lib.rs</files>
  <action>Add overloaded methods to BatchOracle for BuildingParameters:

1. Add evaluate_population_building_parameters() method:
   - #[pymethods] impl: fn evaluate_population_building_parameters(&self, params: Vec<BuildingParameters>, use_surrogates: bool) -> PyResult<Vec<f64>>
   - Convert each BuildingParameters to Vec<f64> using to_vec()
   - Call existing evaluate_population() logic
   - Return PyResult<Vec<f64>> (standardized return type)

2. Keep existing evaluate_population(params: Vec<Vec<f64>>, use_surrogates: bool) -> Vec<f64> for backward compatibility
   - Mark as #[deprecated] if desired (optional for v0.3)

3. Add optional overload that accepts Vec<BuildingParameters> directly (if PyO3 supports method overloading)

Pattern from 11-RESEARCH.md: Maintain backward compatibility by keeping old Vec<f64> signature alongside new typed API
</action>
  <verify>
    <automated>cargo test --lib test_batch_oracle_building_params -x</automated>
  </verify>
  <done>BatchOracle accepts both Vec<Vec<f64>> and Vec<BuildingParameters> with type-safe validation</done>
</task>

<task type="auto">
  <name>Task 3: Standardize return types across Model and BatchOracle (Bare → PyResult)</name>
  <files>src/lib.rs</files>
  <action>Convert ONLY methods that currently return BARE types to use PyResult<T>:

1. Update BatchOracle methods:
   - evaluate_population() -> PyResult<Vec<f64>> (currently returns Vec<f64> - BARE type)
   - evaluate_population_numpy() -> PyResult<PyArray1<f64>> (if exists and returns bare type)
   - Load surrogate methods -> PyResult<()> (if they return bare types)

2. Update Model methods:
   - simulate() -> PyResult<f64> (currently returns f64 - BARE type)
   - load_surrogate() -> PyResult<()> (if exists and returns bare type)

3. Wrap error paths with appropriate exceptions:
   - Validation errors -> ValidationError::new_err(msg)
   - ONNX errors -> SurrogateError::new_err(msg)
   - Simulation errors -> SimulationError::new_err(msg)
   - Use ? operator for clean error propagation

4. IMPORTANT: Only update methods that currently return BARE types (f64, Vec<f64>, ())
   - Do NOT modify methods that already return PyResult<T> (those will be handled by Plan 06)
   - Do NOT remove existing map_err conversions from methods that already use PyResult<T>

Pattern from 11-RESEARCH.md: Standardize error handling with PyResult<T> and domain-specific exceptions

SCOPE NOTE: This task ONLY handles bare → PyResult conversion. Plan 06 Task 3 will handle refining existing PyResult methods to use ? operator.
</action>
  <verify>
    <automated>cargo test --lib test_return_types_standardization -x</automated>
  </verify>
  <done>All Model and BatchOracle methods that previously returned bare types now return PyResult<T> with appropriate exception types</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run cargo test --lib to verify all code compiles and tests pass
2. Verify Python can import fluxion and use BuildingParameters:
   - fluxion.BuildingParameters(window_u_value=1.5, heating_setpoint=20.0, cooling_setpoint=24.0)
   - Invalid construction raises ValidationError with clear message
   - to_vec() returns correct parameter vector
3. Verify return type consistency:
   - BatchOracle.evaluate_population() returns PyResult<Vec<f64>> (was Vec<f64>)
   - Model.simulate() returns PyResult<f64> (was f64)
   - All error paths use domain-specific exceptions (ValidationError, SurrogateError, SimulationError)
4. Verify backward compatibility: Existing code using Vec<f64> still works
</verification>

<success_criteria>
- BuildingParameters struct with named fields (window_u_value, heating_setpoint, cooling_setpoint)
- BuildingParameters validates constraints on construction (ranges, NaN/Inf)
- BuildingParameters.to_vec() converts to Vec<f64> for backward compatibility
- BatchOracle and Model accept both Vec<f64> and BuildingParameters
- All Model and BatchOracle methods that previously returned bare types now return PyResult<T> with domain-specific exceptions
- Backward compatibility maintained: Existing Vec<f64> usage continues to work
- Methods that already returned PyResult<T> are NOT modified here (handled by Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/11-API-Robustness/11-02-SUMMARY.md`
</output>
