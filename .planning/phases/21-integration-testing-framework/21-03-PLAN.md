---
phase: 21
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - tests/integration/test_ashrae_140_regression.rs
  - .github/workflows/nightly_regression.yml
autonomous: true
requirements:
  - INTEG-05
user_setup:
  - service: github
    why: "Create GitHub issues on nightly regression test failures"
    env_vars: []
    dashboard_config:
      - task: "Add GITHUB_TOKEN secret"
        location: "GitHub Repository Settings -> Secrets and variables -> Actions"
        instruction: "Generate a personal access token with repo scope and add as GITHUB_TOKEN secret"

must_haves:
  truths:
    - "User can run comprehensive ASHRAE 140 regression test that checks all 18 cases"
    - "Regression test detects regressions from baseline (Cases 195, 600, 620, 900, 960)"
    - "Nightly workflow runs full regression test and creates GitHub issues on failure"
    - "Test generates markdown report for CI visibility"
  artifacts:
    - path: "tests/integration/test_ashrae_140_regression.rs"
      provides: "Comprehensive ASHRAE 140 regression test"
      exports: ["test_ashrae_140_comprehensive_regression"]
    - path: ".github/workflows/nightly_regression.yml"
      provides: "GitHub Actions workflow for nightly regression tests"
      contains: "schedule: cron('0 0 * * *')"
  key_links:
    - from: "tests/integration/test_ashrae_140_regression.rs"
      to: "src/validation/mod.rs"
      via: "ASHRAE140Validator"
      pattern: "ASHRAE140Validator::new"
    - from: "tests/integration/test_ashrae_140_regression.rs"
      to: "tests/ashrae_140_validation.rs"
      via: "Existing validation test patterns"
      pattern: "validate_analytical_engine"
    - from: ".github/workflows/nightly_regression.yml"
      to: "tests/integration/test_ashrae_140_regression.rs"
      via: "cargo test --test integration test_ashrae_140_regression"
      pattern: "test_ashrae_140_comprehensive_regression"
---

<objective>
Create comprehensive ASHRAE 140 regression test suite that runs all 18 cases and detects regressions from baseline.

Purpose: Provide a full regression test that catches changes breaking ASHRAE 140 compliance, runs nightly to avoid slowing PR feedback, and creates GitHub issues on failure.
Output: Comprehensive regression test (all 18 cases) and nightly GitHub Actions workflow
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integration-testing-framework/21-CONTEXT.md
@.planning/phases/21-integration-testing-framework/21-RESEARCH.md
@.planning/phases/21-integration-testing-framework/21-01-SUMMARY.md
@.planning/phases/21-integration-testing-framework/21-02-SUMMARY.md
@.planning/ashrae_140_validation.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement comprehensive ASHRAE 140 regression test</name>
  <files>tests/integration/test_ashrae_140_regression.rs</files>
  <action>
    Replace pytest.skip("TODO") stub with comprehensive regression test:
    - Create test_ashrae_140_comprehensive_regression() function
    - Use ASHRAE140Validator::new() to create validator
    - Call validate_analytical_engine() to run all 18 cases
    - Check for regressions in critical cases: 195, 600, 620, 900, 960
    - Use ValidationStatus::Fail to detect failures
    - Panic on regressions in Cases 195, 600, 620 (these should always pass)
    - Log warnings (not panic) for Cases 900, 960 (still being calibrated)
    - Generate markdown report using report.to_markdown()
    - Assert report contains expected sections (# ASHRAE 140 Validation Report, case IDs)
    - Print report summary for CI visibility using report.print_summary()
    - Follow existing patterns from tests/ashrae_140_validation.rs (test_ashrae_140_comprehensive_validation)
    - Add docstring explaining test purpose and regression detection logic
  </action>
  <verify>
    <automated>cargo test --test integration test_ashrae_140_regression -- --nocapture</automated>
  </verify>
  <done>Test runs all 18 ASHRAE 140 cases, detects regressions in critical cases (195, 600, 620), logs warnings for 900-series, generates markdown report</done>
</task>

<task type="auto">
  <name>Task 2: Create nightly GitHub Actions workflow for regression tests</name>
  <files>.github/workflows/nightly_regression.yml</files>
  <action>
    Create GitHub Actions workflow that runs nightly:
    - Use schedule trigger: cron('0 0 * * *') (runs daily at midnight UTC)
    - Use Ubuntu latest runner (ubuntu-latest)
    - Checkout code with actions/checkout@v4
    - Set up Rust toolchain with actions-rust-lang/setup-rust-action@v1
    - Install Python dependencies (pip install -r requirements-dev.txt)
    - Build Fluxion with release profile: cargo build --release --features python-bindings
    - Run maturin develop to install Python bindings
    - Run comprehensive regression test: cargo test --test integration test_ashrae_140_regression --release
    - Upload test results as artifacts for debugging
    - Create GitHub issue on failure using github-script:
      - Use step.status != 'success' condition
      - Use GITHUB_TOKEN secret (user must add this)
      - Include test output in issue body
      - Tag issue with "regression" and "ashrae-140"
    - Follow existing patterns from .github/workflows/ci.yml and .github/workflows/rust-tests.yml
  </action>
  <verify>
    <automated>gh workflow view nightly_regression.yml 2>&1 | grep schedule</automated>
  </verify>
  <done>Workflow runs daily at midnight, executes comprehensive regression test, uploads test results, creates GitHub issues on failure with regression tags</done>
</task>

</tasks>

<verification>
Run regression test and verify workflow configuration:

```bash
# Run comprehensive regression test locally
cargo test --test integration test_ashrae_140_regression --release -- --nocapture

# Verify workflow file exists and is valid YAML
cat .github/workflows/nightly_regression.yml | yq eval '.' -

# Check workflow syntax (requires gh CLI)
gh workflow view nightly_regression.yml
```

Expected outcomes:
- Regression test runs all 18 ASHRAE 140 cases in <5 minutes
- Test detects regressions in critical cases (195, 600, 620) and panics
- Test logs warnings for 900-series cases (not panic, still calibrating)
- Markdown report is generated and contains all case results
- Workflow file is valid YAML and has schedule trigger
- Workflow creates GitHub issues on failure with regression tags
</verification>

<success_criteria>
1. User can run `cargo test --test integration test_ashrae_140_regression --release` and all cases pass
2. Regression test completes in <5 minutes (18 cases × ~15 sec each)
3. Test panics on regressions in Cases 195, 600, 620 (critical cases)
4. Test logs warnings (not panics) for Cases 900, 960 (still calibrating)
5. Test generates markdown report with all case results visible in CI output
6. Nightly workflow runs daily at midnight UTC
7. Workflow creates GitHub issues on failure with regression and ashrae-140 tags
8. Workflow uploads test results as artifacts for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-testing-framework/21-03-SUMMARY.md`
</output>
