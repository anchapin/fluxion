---
phase: 21
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/integration/test_pyo3_bindings.rs
  - tests/integration/test_numpy_arrays.py
autonomous: true
requirements:
  - INTEG-04
user_setup: []

must_haves:
  truths:
    - "User can run pytest tests that validate PyO3 bindings work correctly"
    - "NumPy array shapes and dtypes are preserved across FFI boundary"
    - "Invalid inputs raise Python exceptions (not segfaults)"
    - "Large NumPy arrays (10,000+ elements) are handled without FFI issues"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Pytest configuration and fluxion_module fixture"
      exports: ["fluxion_module"]
    - path: "tests/integration/test_pyo3_bindings.rs"
      provides: "Rust-side PyO3 binding validation tests"
      contains: "test_batch_oracle_python_init", "test_model_from_case"
    - path: "tests/integration/test_numpy_arrays.py"
      provides: "Python-side NumPy array validation tests"
      contains: "TestNumPyArrays.test_array_shape_validation"
  key_links:
    - from: "tests/integration/test_numpy_arrays.py"
      to: "tests/conftest.py"
      via: "pytest fixture: fluxion_module"
      pattern: "def fluxion_module"
    - from: "tests/integration/test_pyo3_bindings.rs"
      to: "src/lib.rs"
      via: "PyO3 Python bindings"
      pattern: "#[cfg(feature = \"python-bindings\")]"
    - from: "tests/integration/test_numpy_arrays.py"
      to: "tests/integration/test_pyo3_bindings.rs"
      via: "Shared test coverage of PyO3 FFI boundary"
      pattern: "NumPy array"
---

<objective>
Create comprehensive Python-side integration tests for PyO3 bindings with real NumPy array validation.

Purpose: Validate that the Python-Rust FFI boundary works correctly with NumPy arrays, including shape preservation, dtype conversion, error handling, and large array support.
Output: pytest test suite for NumPy array validation (shape, dtype, FFI errors) and Rust-side PyO3 binding tests
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integration-testing-framework/21-CONTEXT.md
@.planning/phases/21-integration-testing-framework/21-RESEARCH.md
@.planning/phases/21-integration-testing-framework/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pytest fixture for fluxion_module</name>
  <files>tests/conftest.py</files>
  <action>
    Extend existing conftest.py to provide a fluxion_module fixture:
    - Add a module-scoped fixture that imports and returns the fluxion Python module
    - Handle import errors gracefully (skip tests if fluxion not installed)
    - Use pytest.fixture(scope="module") decorator for efficiency (import once per test session)
    - Return the module object so tests can access BatchOracle, Model, etc.
    - Extend existing pytest_configure to support the new fixture
    - Follow existing patterns from existing conftest.py (pytest.fluxion_available pattern)
    - Add docstring explaining the fixture purpose and usage
  </action>
  <verify>
    <automated>pytest tests/conftest.py -v --collect-only 2>&1 | grep fluxion_module</automated>
  </verify>
  <done>fluxion_module fixture is available in all Python tests, imports fluxion successfully, skips gracefully if fluxion not installed</done>
</task>

<task type="auto">
  <name>Task 2: Implement NumPy array shape and dtype validation tests</name>
  <files>tests/integration/test_numpy_arrays.py</files>
  <action>
    Replace all pytest.skip("TODO") calls with real tests:
    - test_array_shape_validation: Create NumPy arrays of various shapes (1D, 2D, 3D), pass to VectorField, verify shape preserved via to_numpy()
    - test_array_dtype_conversion: Test f32 and f64 arrays, verify both convert to f64 internally, assert np.allclose() results match
    - test_large_numpy_array_handling: Create array with 10,000+ elements, pass to fluxion, verify no FFI issues, check integration result is valid
    - test_empty_array_handling: Test empty arrays are handled gracefully (raise ValueError, not segfault)
    - test_nan_array_handling: Test NaN values are propagated correctly (not silently dropped)
    - Use fluxion_module fixture to access fluxion API
    - Use numpy operations: np.array(), .tolist(), np.allclose(), .shape, .dtype
    - Use pytest.raises() for error handling tests
    - Follow existing patterns from tests/test_python_bindings.py (if it exists)
  </action>
  <verify>
    <automated>pytest tests/integration/test_numpy_arrays.py -v</automated>
  </verify>
  <done>All NumPy tests pass, shapes are preserved, dtypes convert correctly (f32→f64), large arrays work without issues, empty arrays raise ValueError, NaN values are handled</done>
</task>

<task type="auto">
  <name>Task 3: Implement Rust-side PyO3 binding validation tests</name>
  <files>tests/integration/test_pyo3_bindings.rs</files>
  <action>
    Create comprehensive Rust-side tests for PyO3 bindings:
    - test_batch_oracle_python_init: Verify BatchOracle can be instantiated from Python, test that evaluate_population works with Python list of lists
    - test_model_from_case: Verify Model.from_case() works, test that simulate() returns valid energy values
    - test_vector_field_numpy_conversion: Verify VectorField.to_numpy() returns correct shape and dtype
    - test_ffi_error_handling: Verify invalid inputs (negative num_zones, out-of-range setpoints) raise Python exceptions, not segfaults
    - Use pyo3::Python::with_gil(|py| { ... }) pattern for Python GIL management
    - Use assert!(result.is_ok()) for success cases, assert!(result.is_err()) for error cases
    - Follow existing patterns from src/lib.rs (PyO3 class definitions) and tests/test_batch_oracle_throughput.rs (test structure)
    - DO NOT mock PyO3 bindings — use real Python object creation
  </action>
  <verify>
    <automated>cargo test --test integration test_pyo3_bindings -- --nocapture</automated>
  </verify>
  <done>BatchOracle initializes correctly from Python, Model.from_case() works, VectorField.to_numpy() returns correct NumPy arrays, invalid inputs raise PyErr (not panics)</done>
</task>

</tasks>

<verification>
Run Python integration tests to verify PyO3 bindings work correctly:

```bash
# Run Python tests with verbose output
pytest tests/integration/test_numpy_arrays.py -v

# Run Rust-side PyO3 binding tests
cargo test --test integration test_pyo3_bindings -- --nocapture

# Run all Python integration tests
pytest tests/integration/ -v
```

Expected outcomes:
- All NumPy array tests pass (shape, dtype, large arrays, empty arrays, NaN handling)
- Rust-side PyO3 binding tests pass (BatchOracle, Model, VectorField, error handling)
- Invalid inputs raise Python exceptions (ValueError, TypeError) not segfaults
- Large arrays (10,000+ elements) work without FFI issues
</verification>

<success_criteria>
1. User can run `pytest tests/integration/test_numpy_arrays.py -v` and all tests pass
2. NumPy array shapes are preserved across FFI boundary (1D, 2D, 3D arrays)
3. NumPy dtypes convert correctly (f32→f64, f64→f64)
4. Invalid inputs raise Python exceptions (not segfaults)
5. Large arrays (10,000+ elements) are handled without FFI issues
6. Rust-side PyO3 binding tests validate BatchOracle, Model, and VectorField APIs
7. Error handling is tested (empty arrays, NaN values, out-of-range parameters)
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-testing-framework/21-02-SUMMARY.md`
</output>
