---
phase: 21
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/data/README.md
  - tests/data/v0.4/
  - tests/data/v0.5/
  - tests/data/latest
autonomous: true
requirements:
  - INTEG-06
user_setup: []

must_haves:
  truths:
    - "User can reference versioned test data (tests/data/v0.5/denver.epw)"
    - "Test data is organized by version (v0.4 baseline, v0.5 current)"
    - "README documents structure and usage patterns"
    - "latest symlink points to current version for convenience"
  artifacts:
    - path: "tests/data/README.md"
      provides: "Documentation for test data directory structure"
      contains: "versioned subdirs", "usage examples"
    - path: "tests/data/v0.4/"
      provides: "v0.4 baseline reference results"
      contains: "reference_results.yaml", "epw files"
    - path: "tests/data/v0.5/"
      provides: "v0.5 current test data"
      contains: "epw files", "reference results"
    - path: "tests/data/latest"
      provides: "Symlink to current version (v0.5)"
      target: "../v0.5"
  key_links:
    - from: "tests/integration/test_e2e_scenarios.rs"
      to: "tests/data/v0.5/"
      via: "BuildingScenario::with_weather()"
      pattern: "tests/data/v0.5/"
    - from: "tests/ashrae_140_validation.rs"
      to: "tests/data/v0.4/"
      via: "ASHRAE 140 reference results"
      pattern: "tests/ashrae_140/"
    - from: "tests/data/latest"
      to: "tests/data/v0.5/"
      via: "Symlink for convenience"
      pattern: "../v0.5"
---

<objective>
Create centralized test data management with versioned subdirectories for EPW files and reference results.

Purpose: Provide a structured, versioned repository for test data that prevents breaking old tests when adding new data, supports CI/local dev with consistent paths, and documents usage patterns.
Output: Versioned test data directories (v0.4, v0.5), latest symlink, comprehensive README
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integration-testing-framework/21-CONTEXT.md
@.planning/phases/21-integration-testing-framework/21-RESEARCH.md
@.planning/phases/21-integration-testing-framework/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v0.4 baseline test data directory</name>
  <files>tests/data/v0.4/</files>
  <action>
    Create and populate v0.4 baseline directory:
    - Copy existing ASHRAE 140 reference results from tests/ashrae_140/ to tests/data/v0.4/
    - Create reference_results.yaml with v0.4 baseline results (extract from docs/ASHRAE140_RESULTS.md)
    - Copy any existing EPW weather files to tests/data/v0.4/
    - Create subdirectory structure: tests/data/v0.4/ashrae_140/, tests/data/v0.4/weather/
    - Add .gitkeep file to preserve empty directories
    - Document baseline status in README (v0.4 = 18/18 cases passing)
    - Follow existing patterns from tests/ashrae_140/ directory structure
  </action>
  <verify>
    <automated>ls -la tests/data/v0.4/ && find tests/data/v0.4/ -name "*.yaml" -o -name "*.epw" | wc -l</automated>
  </verify>
  <done>v0.4 directory exists with reference results and EPW files, README documents baseline status, directory structure matches tests/ashrae_140/</done>
</task>

<task type="auto">
  <name>Task 2: Create v0.5 current test data directory</name>
  <files>tests/data/v0.5/</files>
  <action>
    Create and populate v0.5 current directory:
    - Create subdirectory structure: tests/data/v0.5/ashrae_140/, tests/data/v0.5/weather/
    - Add placeholder files for current development (can be empty initially)
    - Create reference_results.yaml placeholder with "work in progress" marker
    - Add .gitkeep files to preserve empty directories
    - Document current status in README (v0.5 = work in progress)
    - Copy any new EPW files needed for Phase 21 integration tests
    - Follow existing patterns from tests/data/v0.4/ structure
  </action>
  <verify>
    <automated>ls -la tests/data/v0.5/ && find tests/data/v0.5/ -name "*.yaml" -o -name "*.epw" | wc -l</automated>
  </verify>
  <done>v0.5 directory exists with subdirectories, README documents work-in-progress status, placeholder files for current development</done>
</task>

<task type="auto">
  <name>Task 3: Update README with comprehensive documentation</name>
  <files>tests/data/README.md</files>
  <action>
    Update README with comprehensive test data documentation:
    - Document directory structure (v0.4, v0.5, latest symlink)
    - Provide usage examples: `let weather_path = "tests/data/v0.5/denver.epw";`
    - Explain versioning strategy (why versioned subdirs)
    - Document how to add new data (create v0.6/, update symlink)
    - Document environment variable: FLUXION_TEST_DATA_DIR for custom paths
    - Document file contents (EPW files, reference results, etc.)
    - Add troubleshooting section (common issues, how to fix)
    - Follow existing patterns from CLAUDE.md documentation style
  </action>
  <verify>
    <automated>cat tests/data/README.md | grep -E "v0.4|v0.5|latest|FLUXION_TEST_DATA_DIR" | wc -l</automated>
  </verify>
  <done>README documents structure, usage examples, versioning strategy, environment variable, troubleshooting, follows CLAUDE.md documentation style</done>
</task>

</tasks>

<verification>
Verify test data directory structure and documentation:

```bash
# Check directory structure
tree tests/data/ -L 2

# Verify symlink points to correct target
readlink tests/data/latest

# Check README exists and has key sections
cat tests/data/README.md | grep -E "## Structure|## Usage|## Adding New Data|## Environment Variable"

# Count files in each version
find tests/data/v0.4/ -type f | wc -l
find tests/data/v0.5/ -type f | wc -l
```

Expected outcomes:
- tests/data/v0.4/ contains baseline reference results and EPW files
- tests/data/v0.5/ contains current development data (may be minimal initially)
- tests/data/latest is a symlink to ../v0.5
- README documents structure, usage, versioning, environment variable
- Directory structure is versioned and organized by data type (ashrae_140/, weather/)
</verification>

<success_criteria>
1. User can reference versioned test data: `tests/data/v0.5/denver.epw`
2. v0.4 directory contains baseline reference results from docs/ASHRAE140_RESULTS.md
3. v0.5 directory exists for current development work
4. latest symlink points to v0.5 for convenience
5. README documents structure, usage examples, versioning strategy
6. Environment variable FLUXION_TEST_DATA_DIR is documented for custom paths
7. Directory structure follows existing patterns from tests/ashrae_140/
8. Empty directories have .gitkeep files to preserve structure
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-testing-framework/21-04-SUMMARY.md`
</output>
