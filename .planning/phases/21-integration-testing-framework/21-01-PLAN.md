---
phase: 21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/testing/integration/mod.rs
  - src/testing/integration/fixtures.rs
  - src/testing/integration/wiring.rs
  - src/testing/integration/scenarios.rs
  - src/lib.rs
autonomous: true
requirements:
  - INTEG-01
  - INTEG-02
user_setup: []

must_haves:
  truths:
    - "User can import and use BuildingScenario builder to create test models"
    - "User can create pre-built scenarios (low_mass, high_mass, multi_zone, vav, heat_pump)"
    - "User can use WiringTracer to verify module call chains"
    - "All test fixtures use isolated temporary directories (no shared state)"
  artifacts:
    - path: "src/testing/integration/mod.rs"
      provides: "Integration testing framework module"
      exports: ["fixtures", "wiring", "scenarios"]
    - path: "src/testing/integration/fixtures.rs"
      provides: "BuildingScenario builder for test construction"
      contains: "BuildingScenario", "HvacType"
    - path: "src/testing/integration/wiring.rs"
      provides: "Runtime tracing for wiring validation"
      exports: ["WiringTracer"]
    - path: "src/testing/integration/scenarios.rs"
      provides: "Pre-built test scenarios"
      exports: ["low_mass_scenario", "high_mass_scenario", "multi_zone_scenario"]
    - path: "src/lib.rs"
      provides: "Public API including testing module"
      contains: "pub mod testing"
  key_links:
    - from: "tests/integration/test_e2e_scenarios.rs"
      to: "src/testing/integration/fixtures.rs"
      via: "use fluxion::testing::integration::BuildingScenario"
      pattern: "BuildingScenario::new"
    - from: "tests/integration/test_wiring.rs"
      to: "src/testing/integration/wiring.rs"
      via: "use fluxion::testing::integration::WiringTracer"
      pattern: "WiringTracer::new"
    - from: "src/lib.rs"
      to: "src/testing/integration/mod.rs"
      via: "pub mod testing"
      pattern: "testing::"
---

<objective>
Build the core E2E integration testing framework with reusable fixtures and wiring validation infrastructure.

Purpose: Provide a solid foundation for integration tests that can catch wiring issues and enable rapid test scenario construction without boilerplate.
Output: Builder-pattern fixtures (BuildingScenario), runtime tracing (WiringTracer), pre-built scenarios (low_mass, high_mass, multi_zone)
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integration-testing-framework/21-CONTEXT.md
@.planning/phases/21-integration-testing-framework/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BuildingScenario builder with sensible defaults</name>
  <files>src/testing/integration/fixtures.rs</files>
  <action>
    Complete the BuildingScenario builder implementation:
    - Add all builder methods: with_zone_count(), with_weather(), with_hvac(), with_window_u_value(), with_heating_setpoint(), with_cooling_setpoint()
    - Add validation in build() method: check num_zones > 0, window_u_value in [0.1, 5.0], setpoints in [15, 30]
    - Add create_model() method that constructs a real ThermalModel (not a mock) using the builder parameters
    - Use VectorField::from_scalar() for initializing temperatures and mass_temperatures
    - Apply all parameters to the ThermalModel instance before returning it
    - Default values: num_zones=1, window_u_value=1.5, heating_setpoint=20.0, cooling_setpoint=26.0
    - Follow existing patterns from tests/test_batch_oracle_throughput.rs (create_base_model helper)
    - DO NOT mock anything — use real ThermalModel and VectorField types
  </action>
  <verify>
    <automated>cargo test --lib testing::integration::fixtures -- --nocapture</automated>
  </verify>
  <done>BuildingScenario::new() creates valid builder, .build() validates and returns scenario, .create_model() returns working ThermalModel with correct parameters applied</done>
</task>

<task type="auto">
  <name>Task 2: Implement WiringTracer for runtime tracing</name>
  <files>src/testing/integration/wiring.rs</files>
  <action>
    Complete the WiringTracer implementation:
    - Ensure Arc<Mutex<Vec<String>>> is thread-safe for use in parallel tests
    - Add record_call() method that pushes call name to internal vector
    - Add verify_called() method that checks all expected calls exist in recorded calls
    - Add get_calls() method that returns a clone of recorded calls (for debugging)
    - Add clear() method to reset recorded calls between test runs
    - Implement Clone trait for WiringTracer (share Arc between test and model)
    - Mark all methods with #[cfg(test)] to ensure test-only compilation
    - Follow existing patterns from src/ai/surrogate.rs (SessionPool uses Arc for thread safety)
  </action>
  <verify>
    <automated>cargo test --lib testing::integration::wiring -- --nocapture</automated>
  </verify>
  <done>WiringTracer::new() creates tracer, .record_call() stores call, .verify_called() returns true only if all expected calls recorded, .clear() resets state</done>
</task>

<task type="auto">
  <name>Task 3: Implement pre-built scenarios and expose testing module</name>
  <files>src/testing/integration/scenarios.rs, src/testing/integration/mod.rs, src/lib.rs</files>
  <action>
    Complete pre-built scenarios and module exposure:
    - Implement all scenario functions in scenarios.rs: low_mass_scenario(), high_mass_scenario(), multi_zone_scenario(), vav_scenario(), heat_pump_scenario()
    - Each scenario should return a BuildingScenario with ASHRAE 140-like parameters (see tests/ashrae_140_case_600.rs for reference values)
    - Add with_heating_setpoint() and with_cooling_setpoint() methods to BuildingScenario (already stubbed in fixtures.rs)
    - Export all modules in mod.rs: pub mod fixtures, wiring, scenarios
    - Verify src/lib.rs has `pub mod testing;` (already added via sed)
    - Ensure all public types are re-exported: BuildingScenario, HvacType, WiringTracer, and scenario functions
    - Follow existing patterns from src/validation/mod.rs (re-export public types)
  </action>
  <verify>
    <automated>cargo test --lib testing::integration::scenarios -- --nocapture</automated>
  </verify>
  <done>All scenario functions return valid BuildingScenario instances, testing module is publicly accessible, types can be imported with `use fluxion::testing::integration::*`</done>
</task>

</tasks>

<verification>
Run integration test smoke tests to verify framework is usable:

```bash
# Test that framework compiles and basic functionality works
cargo test --lib testing::integration

# Verify module is publicly accessible
cargo test --lib -- --nocapture testing
```

Expected outcomes:
- All tests in testing::integration module compile and run
- BuildingScenario creates valid ThermalModel instances
- WiringTracer correctly records and verifies function calls
- Pre-built scenarios return properly configured BuildingScenario instances
</verification>

<success_criteria>
1. User can import `fluxion::testing::integration::{BuildingScenario, WiringTracer}` and use them in tests
2. BuildingScenario builder provides fluent API: `.with_zone_count(3).with_window_u_value(2.5).build()`
3. WiringTracer can record calls and verify expected call chains
4. Pre-built scenarios (low_mass, high_mass, multi_zone, vav, heat_pump) are available and create valid models
5. All test infrastructure is compiled with #[cfg(test)] (no runtime overhead in production)
6. Framework uses real ThermalModel implementations (no mocks)
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-testing-framework/21-01-SUMMARY.md`
</output>
