---
phase: 21
plan: 05
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-03
files_modified:
  - tests/integration/test_wiring.rs
  - tests/integration/test_e2e_scenarios.rs
  - tests/integration/test_batch_oracle.rs
  - tests/integration/test_cli.rs
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INTEG-03
  - INTEG-07
  - INTEG-08
user_setup: []

must_haves:
  truths:
    - "User can run wiring validation tests that detect integration issues (e.g., solve_timesteps() never calling predict_loads())"
    - "E2E tests validate full system workflows (BatchOracle, Model, CLI, Surrogates, HVAC, Psychrometrics, Internal loads, Multi-zone)"
    - "CI/CD workflow runs integration tests and benchmarks on every PR"
    - "Performance regression detection fails PR if >10% slowdown from baseline"
  artifacts:
    - path: "tests/integration/test_wiring.rs"
      provides: "Wiring validation tests using WiringTracer"
      contains: "test_surrogate_integration_wiring", "test_batch_oracle_parallelism"
    - path: "tests/integration/test_e2e_scenarios.rs"
      provides: "E2E tests for all major features"
      contains: "test_batch_oracle_throughput", "test_hvac_variants"
    - path: "tests/integration/test_batch_oracle.rs"
      provides: "BatchOracle E2E tests with realistic workloads"
      contains: "test_population_evaluation_100", "test_population_evaluation_1000"
    - path: "tests/integration/test_cli.rs"
      provides: "CLI integration tests using std::process::Command"
      contains: "test_cli_validate_command", "test_cli_simulation_command"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow with integration tests and benchmarks"
      contains: "integration tests", "benchmarks", "performance regression detection"
  key_links:
    - from: "tests/integration/test_wiring.rs"
      to: "src/testing/integration/wiring.rs"
      via: "WiringTracer"
      pattern: "WiringTracer::new"
    - from: "tests/integration/test_wiring.rs"
      to: "src/sim/engine.rs"
      via: "ThermalModel::solve_timesteps"
      pattern: "solve_timesteps"
    - from: "tests/integration/test_wiring.rs"
      to: "src/ai/surrogate.rs"
      via: "SurrogateManager::predict_loads"
      pattern: "predict_loads"
    - from: "tests/integration/test_e2e_scenarios.rs"
      to: "src/testing/integration/fixtures.rs"
      via: "BuildingScenario"
      pattern: "BuildingScenario::new"
    - from: ".github/workflows/ci.yml"
      to: "tests/integration/"
      via: "cargo test --test integration"
      pattern: "test_e2e_scenarios"
    - from: ".github/workflows/ci.yml"
      to: "benches/"
      via: "cargo bench"
      pattern: "benchmark"
---

<objective>
Implement wiring validation tests, comprehensive E2E tests for all major features, and integrate with CI/CD workflow with performance regression detection.

Purpose: Detect wiring issues between modules (solve_timesteps → predict_loads), validate full system workflows, run integration tests and benchmarks on every PR, and fail PRs on performance regressions >10%.
Output: Wiring validation tests, E2E scenario tests, BatchOracle tests, CLI tests, CI/CD workflow integration
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integration-testing-framework/21-CONTEXT.md
@.planning/phases/21-integration-testing-framework/21-RESEARCH.md
@.planning/phases/21-integration-testing-framework/21-01-SUMMARY.md
@.planning/phases/21-integration-testing-framework/21-03-SUMMARY.md
@.planning/phases/21-integration-testing-framework/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement wiring validation tests</name>
  <files>tests/integration/test_wiring.rs</files>
  <action>
    Replace pytest.skip("TODO") stubs with real wiring validation tests:
    - test_surrogate_integration_wiring: Create ThermalModel with WiringTracer, run solve_timesteps(8760, None, true), assert predict_loads was called
    - test_batch_oracle_parallelism: Create BatchOracle with WiringTracer, evaluate population with use_ai=true, assert predict_loads_batched was called
    - test_weather_data_flow: Load weather file, run simulation, assert weather data is used (check temperatures are not constant)
    - test_surrogate_predictions_used: Run simulation with use_ai=true vs false, assert different energy results (surrogates actually used)
    - Use WiringTracer::new() to create tracer, clone it into model
    - Use assert!(tracer.verify_called(&["predict_loads"])) to verify calls
    - Use real ThermalModel and SurrogateManager (no mocks)
    - Follow existing patterns from src/testing/integration/wiring.rs (WiringTracer API) and tests/test_batch_oracle_throughput.rs (test structure)
  </action>
  <verify>
    <automated>cargo test --test integration test_wiring -- --nocapture</automated>
  </verify>
  <done>All wiring validation tests pass, solve_timesteps calls predict_loads when use_ai=true, BatchOracle uses predict_loads_batched for populations, weather data flows through, surrogates affect results</done>
</task>

<task type="auto">
  <name>Task 2: Implement comprehensive E2E scenario tests</name>
  <files>tests/integration/test_e2e_scenarios.rs</files>
  <action>
    Replace pytest.skip("TODO") stubs with real E2E tests for all major features:
    - test_batch_oracle_throughput: Create BatchOracle, evaluate 1000 configs, verify <100ms total, verify all results are finite
    - test_hvac_variants: Use BuildingScenario to test VAV, CAV, HeatPump, Chiller (using rstest parameterization), verify each produces valid energy
    - test_python_api_batch_oracle: Import BatchOracle from Python, evaluate population, verify results match Rust API
    - test_python_api_model: Import Model from Python, run simulate(years=1), verify energy output is valid
    - test_surrogate_integration: Run simulation with use_ai=true and false, verify surrogates produce reasonable results (not NaN, within reasonable range)
    - test_psychrometrics: Test dew point, humidity ratio, enthalpy calculations with realistic weather data
    - test_internal_loads: Test lighting, equipment, occupancy with schedules using BuildingScenario
    - test_multi_zone_physics: Test inter-zone conductance using multi_zone_scenario(), verify zone coupling works
    - Use BuildingScenario builder from 21-01 for test construction
    - Use rstest for parameterized tests (HvacType variants)
    - Use real implementations (no mocks) to catch wiring issues
    - Follow existing patterns from tests/test_batch_oracle_throughput.rs (throughput test) and tests/hvac_equipment.rs (HVAC tests)
  </action>
  <verify>
    <automated>cargo test --test integration test_e2e_scenarios -- --nocapture</automated>
  </verify>
  <done>All E2E tests pass, BatchOracle throughput <100ms for 1000 configs, all HVAC variants produce valid energy, Python API matches Rust API, surrogates work, psychrometrics calculations valid, internal loads work, multi-zone physics works</done>
</task>

<task type="auto">
  <name>Task 3: Implement BatchOracle and CLI integration tests</name>
  <files>tests/integration/test_batch_oracle.rs, tests/integration/test_cli.rs</files>
  <action>
    Implement BatchOracle and CLI integration tests:

    For test_batch_oracle.rs:
    - test_population_evaluation_100: Evaluate 100 configs, verify all results are finite, verify count matches input
    - test_population_evaluation_1000: Evaluate 1000 configs, verify throughput >=1000 configs/sec
    - test_parameter_vector_semantics: Test U-value range [0.1, 5.0], setpoint range [15, 30], verify out-of-range returns NaN
    - test_surrogate_integration: Compare use_surrogates=true vs false, verify results are different but reasonable
    - test_parallelism_correctness: Verify rayon par_iter is used (check thread count >1)
    - Use rand for generating random populations (seed for reproducibility)
    - Use std::time::Instant for throughput measurement
    - Follow existing patterns from tests/test_batch_oracle_throughput.rs

    For test_cli.rs:
    - test_cli_validate_command: Run `fluxion validate --all` using std::process::Command, verify exit code 0, verify output contains "Validation Report Summary", verify Case 600 and 960 in output
    - test_cli_simulation_command: Run `fluxion simulate --years 1 --no-surrogates`, verify exit code 0, verify output contains "Energy (MWh)"
    - test_cli_sensitivity_command: Run `fluxion sensitivity --config <path>`, verify exit code 0, verify sensitivity analysis output
    - Use tempfile::tempdir() for config file isolation
    - Use assert!(output.status.success()) for exit code validation
    - Follow existing patterns from tests/cli_integration.rs
  </action>
  <verify>
    <automated>cargo test --test integration test_batch_oracle test_cli -- --nocapture</automated>
  </verify>
  <done>BatchOracle evaluates 1000 configs in <1s, parameter validation returns NaN for out-of-range, surrogates work, CLI validate command runs all cases, CLI simulate command works, CLI sensitivity command works</done>
</task>

<task type="auto">
  <name>Task 4: Integrate integration tests and benchmarks into CI/CD workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Update existing CI workflow to run integration tests and benchmarks:
    - Add "Integration Tests" step after unit tests:
      - Run `cargo test --test integration --lib`
      - Run `pytest tests/integration/ -v`
      - Upload test results as artifacts
    - Add "Benchmarks" step after integration tests:
      - Run `cargo bench --no-fail-fast --bench integration_bench`
      - Upload benchmark results (criterion target directory)
    - Add "Performance Regression Detection" step:
      - Use Criterion's baseline comparison: `cargo bench --bench integration_bench --save-baseline baseline`
      - Compare against previous baseline, fail if >10% slowdown
      - Use critcmp tool or custom script for comparison
      - Comment on PR with performance metrics
    - Ensure steps run on Ubuntu latest with Rust stable
    - Set up Python and install dependencies before Python tests
    - Use caching for Cargo registry and dependencies to speed up CI
    - Follow existing patterns from .github/workflows/ci.yml and .github/workflows/rust-tests.yml
    - Add step names: "Run Integration Tests", "Run Benchmarks", "Check Performance Regressions"
  </action>
  <verify>
    <automated>gh workflow view ci.yml 2>&1 | grep -E "Integration Tests|Benchmarks|Performance"</automated>
  </verify>
  <done>CI workflow runs integration tests (Rust + Python), runs benchmarks, detects >10% performance slowdown from baseline, uploads test results and benchmark artifacts</done>
</task>

</tasks>

<verification>
Run all integration tests and verify CI workflow:

```bash
# Run all integration tests
cargo test --test integration --lib

# Run Python integration tests
pytest tests/integration/ -v

# Run wiring validation tests
cargo test --test integration test_wiring -- --nocapture

# Run E2E scenario tests
cargo test --test integration test_e2e_scenarios -- --nocapture

# Run BatchOracle and CLI tests
cargo test --test integration test_batch_oracle test_cli -- --nocapture

# Verify CI workflow syntax
gh workflow view ci.yml

# Check workflow has integration test step
cat .github/workflows/ci.yml | grep -A 5 "Integration Tests"
```

Expected outcomes:
- All integration tests pass (Rust and Python)
- Wiring validation detects missing predict_loads() calls
- E2E tests validate all major features (BatchOracle, Python API, CLI, Surrogates, HVAC, Psychrometrics, Internal loads, Multi-zone)
- BatchOracle evaluates 1000 configs in <1s (>=1000 configs/sec)
- CLI commands work correctly (validate, simulate, sensitivity)
- CI workflow includes integration tests, benchmarks, performance regression detection
- CI fails PR if >10% performance slowdown from baseline
</verification>

<success_criteria>
1. User can run `cargo test --test integration` and all tests pass
2. Wiring validation tests detect missing predict_loads() calls when use_ai=true
3. E2E tests validate all 8 major feature areas (BatchOracle, Python API, CLI, Surrogates, HVAC, Psychrometrics, Internal loads, Multi-zone)
4. BatchOracle evaluates 1000 configs in <1s (throughput >=1000 configs/sec)
5. CLI commands (validate, simulate, sensitivity) work correctly with proper exit codes
6. CI workflow runs integration tests on every PR (Rust and Python)
7. CI workflow runs benchmarks on every PR
8. CI fails PR if >10% performance slowdown from baseline (using Criterion baseline comparison)
9. Test results and benchmark artifacts are uploaded to CI for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-testing-framework/21-05-SUMMARY.md`
</output>
