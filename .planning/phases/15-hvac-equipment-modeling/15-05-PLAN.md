---
phase: 15-hvac-equipment-modeling
plan: 05
type: execute
wave: 1
depends_on: ["15-04"]
files_modified:
  - src/sim/hvac/tests/equipment_tests.rs
  - src/sim/hvac/tests/cycling_tests.rs
autonomous: true
requirements: [HVAC-01, HVAC-02, HVAC-03, HVAC-04, HVAC-05, HVAC-08]
gap_closure: true

must_haves:
  truths:
    - "VAV, CAV, HeatPump, Chiller, and Boiler implement VariableCapacityEquipment trait with all required methods"
    - "VariableCapacityEquipment trait methods (calculate_capacity, calculate_efficiency, calculate_power, update_state) are tested"
    - "CyclingTracker.calculate_cycling_loss() is tested for startup penalties, minimum runtime, and PLR degradation"
    - "Part-load ratio (PLR) tracking is validated across all equipment types"
  artifacts:
    - path: "src/sim/hvac/tests/equipment_tests.rs"
      provides: "Unit tests for VariableCapacityEquipment trait"
      min_lines: 100
      exports: ["test_variable_capacity_trait", "test_plr_tracking", "test_vav_implementation", "test_cav_implementation", "test_heatpump_implementation", "test_chiller_implementation", "test_boiler_implementation"]
    - path: "src/sim/hvac/tests/cycling_tests.rs"
      provides: "Unit tests for cycling loss tracking"
      min_lines: 80
      exports: ["test_cycling_losses", "test_minimum_runtime_enforcement", "test_startup_penalty", "test_plr_degradation"]
  key_links:
    - from: "src/sim/hvac/tests/equipment_tests.rs"
      to: "src/sim/hvac/equipment.rs"
      via: "use fluxion::sim::hvac::{VariableCapacityEquipment, VAVTerminal, CAVSystem, HeatPump, Chiller, Boiler}"
      pattern: "use.*VariableCapacityEquipment"
    - from: "src/sim/hvac/tests/cycling_tests.rs"
      to: "src/sim/hvac/cycling.rs"
      via: "use fluxion::sim::hvac::cycling::CyclingTracker"
      pattern: "use.*CyclingTracker"
---

<objective>
Implement comprehensive unit tests for HVAC equipment traits and cycling loss tracking to validate the infrastructure created in Plans 15-01 through 15-04.

Purpose: Close test gaps where equipment_tests.rs and cycling_tests.rs contain only unimplemented!() stubs. These tests are critical for verifying that VariableCapacityEquipment implementations work correctly before integration into ThermalModel.

Output: Fully implemented test suites with 11 test functions validating equipment trait methods and cycling loss behavior.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-hvac-equipment-modeling/15-VERIFICATION.md
@.planning/phases/15-hvac-equipment-modeling/15-04-SUMMARY.md

From VERIFICATION.md - Gap Analysis:
**Gap 1:** equipment_tests.rs contains only 2 unimplemented!() stubs (15 lines total)
- Missing: test_variable_capacity_trait, test_plr_tracking, test_vav_implementation, test_cav_implementation, test_heatpump_implementation, test_chiller_implementation, test_boiler_implementation

**Gap 2:** cycling_tests.rs contains only 2 unimplemented!() stubs (16 lines total)
- Missing: test_cycling_losses, test_minimum_runtime_enforcement, test_startup_penalty, test_plr_degradation

From 15-04-SUMMARY.md:
- VariableCapacityEquipment trait exists with 7 equipment implementations (VAVTerminal, CAVSystem, HeatPump, Chiller, Boiler)
- CyclingTracker exists with calculate_cycling_loss() method
- EfficiencyCurve module exists with polynomial evaluation
- All HVAC equipment modules exist (equipment.rs, efficiency_curves.rs, cycling.rs, control.rs, economizer.rs)

Existing test patterns from efficiency_curve_tests.rs (110 lines, 6 tests):
- Uses #[test] attribute and assert! macros
- Tests specific behaviors with expected values
- Tests edge cases (PLR=0.0, PLR=1.0, temperature extremes)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement VariableCapacityEquipment trait unit tests</name>
  <files>src/sim/hvac/tests/equipment_tests.rs</files>
  <behavior>
    - Test 1: test_variable_capacity_trait - Verify VAVTerminal, CAVSystem, HeatPump, Chiller, Boiler all implement VariableCapacityEquipment
    - Test 2: test_plr_tracking - Verify PLR tracking works across all equipment types (update_state updates current_plr, current_plr clamped 0-1)
    - Test 3: test_vav_implementation - Verify VAVTerminal calculate_capacity, calculate_efficiency, calculate_power return reasonable values
    - Test 4: test_cav_implementation - Verify CAVSystem calculate_capacity, calculate_efficiency, calculate_power return reasonable values
    - Test 5: test_heatpump_implementation - Verify HeatPump calculate_capacity, calculate_efficiency (heating vs cooling), calculate_power work
    - Test 6: test_chiller_implementation - Verify Chiller calculate_capacity, calculate_efficiency, calculate_power work with cooling mode
    - Test 7: test_boiler_implementation - Verify Boiler calculate_capacity, calculate_efficiency, calculate_power work with heating mode
  </behavior>
  <action>
    Replace the 2 unimplemented!() stubs in equipment_tests.rs with 7 comprehensive test functions.

    Test pattern from efficiency_curve_tests.rs:
    - Use assert! with relative tolerance (approx::assert_relative_eq!)
    - Test multiple PLR values (0.0, 0.5, 1.0)
    - Test temperature variations (-10°C, 20°C, 35°C)
    - Verify COP/power values are reasonable (COP > 0, power > 0 when load > 0)

    For test_variable_capacity_trait:
    - Create instances of VAVTerminal, CAVSystem, HeatPump, Chiller, Boiler
    - Verify they implement VariableCapacityEquipment (call trait methods)

    For test_plr_tracking:
    - For each equipment type: call update_state() with different loads
    - Verify current_plr() returns expected clamped values
    - Edge cases: load=0 (PLR=0), load=rated_capacity (PLR=1), load>rated_capacity (PLR clamped to 1)

    For equipment-specific tests (VAV, CAV, HeatPump, Chiller, Boiler):
    - Call calculate_capacity() at PLR=0.5, outdoor_temp=20°C, verify > 0
    - Call calculate_efficiency() at PLR=0.5, outdoor_temp=20°C, mode=Cooling/Heating, verify COP > 0
    - Call calculate_power() with realistic load, verify power = load / efficiency (with PLR degradation)
    - For HeatPump: test both Heating and Cooling modes (separate efficiency curves)
  </action>
  <verify>
    <automated>cargo test --package fluxion --lib sim::hvac::tests::equipment_tests -- --nocapture</automated>
  </verify>
  <done>All 7 test functions pass, validating VariableCapacityEquipment trait implementations for VAV, CAV, HeatPump, Chiller, and Boiler</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cycling loss tracking unit tests</name>
  <files>src/sim/hvac/tests/cycling_tests.rs</files>
  <behavior>
    - Test 1: test_cycling_losses - Verify cycling_loss_multiplier increases when PLR < 1.0 (degradation factor applies)
    - Test 2: test_minimum_runtime_enforcement - Verify equipment must run for minimum_timesteps after startup (must_run flag)
    - Test 3: test_startup_penalty - Verify startup_count increments and penalty_kwh is applied on off→on transition
    - Test 4: test_plr_degradation - Verify efficiency multiplier = 1.0 + degradation_factor * (1.0 - PLR) (higher penalty at low PLR)
  </behavior>
  <action>
    Replace the 2 unimplemented!() stubs in cycling_tests.rs with 4 comprehensive test functions.

    Test behavior based on CyclingTracker implementation from cycling.rs:
    - calculate_cycling_loss() returns efficiency multiplier (not energy directly)
    - Startup penalty: loss_kwh += startup_penalty_kwh on off→on transition
    - Minimum runtime: equipment must run for minimum_timesteps after startup
    - PLR degradation: efficiency multiplier = 1.0 + plr_degradation_factor * (1.0 - plr)

    For test_cycling_losses:
    - Create CyclingTracker with plr_degradation_factor=0.2 (20% at 0% PLR)
    - Call calculate_cycling_loss() with is_on=true, PLR=0.5 → verify multiplier = 1.0 + 0.2 * 0.5 = 1.1
    - Call calculate_cycling_loss() with is_on=true, PLR=1.0 → verify multiplier = 1.0 (no degradation at full load)
    - Call calculate_cycling_loss() with is_on=true, PLR=0.0 → verify multiplier = 1.0 + 0.2 * 1.0 = 1.2

    For test_minimum_runtime_enforcement:
    - Create CyclingTracker with minimum_runtime_timesteps=5
    - First call: is_on=true, plr=0.5 → current_runtime_timesteps=1, verify no must_run
    - Second call: is_on=false, plr=0.0 → verify returns multiplier (can't turn off yet)
    - Third-Fifth calls: Continue calling with is_on=true until current_runtime_timesteps=5
    - Sixth call: is_on=false, plr=0.0 → verify returns 1.0 (can turn off now)

    For test_startup_penalty:
    - Create CyclingTracker with startup_penalty_kwh=0.1
    - First call: is_on=true, plr=0.5 → verify startup_count=1, penalty added
    - Second call: is_on=false, plr=0.0 → verify startup_count still 1 (no new startup)
    - Third call: is_on=true, plr=0.5 → verify startup_count=2 (new startup)

    For test_plr_degradation:
    - Test degradation_factor=0.2 at various PLR values:
      - PLR=1.0 → multiplier=1.0 (no degradation)
      - PLR=0.5 → multiplier=1.1
      - PLR=0.0 → multiplier=1.2
    - Test degradation_factor=0.3 at PLR=0.3 → multiplier=1.0 + 0.3 * 0.7 = 1.21
  </action>
  <verify>
    <automated>cargo test --package fluxion --lib sim::hvac::tests::cycling_tests -- --nocapture</automated>
  </verify>
  <done>All 4 test functions pass, validating CyclingTracker startup penalties, minimum runtime enforcement, and PLR degradation</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run full HVAC test suite: `cargo test --package fluxion --lib sim::hvac`
2. Verify all 11 new tests pass (7 from equipment_tests.rs, 4 from cycling_tests.rs)
3. Verify no unimplemented!() stubs remain in equipment_tests.rs or cycling_tests.rs
4. Check test coverage: equipment_tests.rs > 100 lines, cycling_tests.rs > 80 lines
</verification>

<success_criteria>
1. equipment_tests.rs has 7 working test functions (was 2 stubs)
2. cycling_tests.rs has 4 working test functions (was 2 stubs)
3. All 11 tests pass with `cargo test --package fluxion --lib sim::hvac`
4. VariableCapacityEquipment trait methods validated across all 5 equipment types
5. CyclingTracker startup penalties, minimum runtime, and PLR degradation validated
6. Test files > 180 total lines (was 31 lines of stubs)
</success_criteria>

<output>
After completion, create `.planning/phases/15-hvac-equipment-modeling/15-05-SUMMARY.md`
</output>
