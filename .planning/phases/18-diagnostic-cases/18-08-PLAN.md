---
phase: 18-diagnostic-cases
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ashrae_140_cases_800_810.rs
autonomous: true
requirements:
  - DIAG-02
gap_closure: true
must_haves:
  truths:
    - "HVAC equipment case tests (800-810) compile and execute"
    - "All 11 HVAC equipment cases pass with energy values within reference ranges"
    - "Equipment efficiency metrics (COP, EER) validate correctly"
  artifacts:
    - path: tests/ashrae_140_cases_800_810.rs
      provides: "HVAC equipment test implementations"
      contains: "ThermalModel::from_spec usage"
  key_links:
    - from: tests/ashrae_140_cases_800_810.rs
      to: src/validation/ashrae_140_cases.rs
      via: ASHRAE140Case::CaseXXX.spec()
      pattern: "ASHRAE140Case::Case800\\(\\)\\.spec\\(\\)"
    - from: tests/ashrae_140_cases_800_810.rs
      to: src/sim/engine.rs
      via: ThermalModel::from_spec
      pattern: "ThermalModel::<VectorField>::from_spec"
---

<objective>
Fix compilation errors in HVAC equipment tests (Cases 800-810) by replacing incorrect `apply_case_spec()` method calls with correct `from_spec()` constructor calls.

Purpose: Unblock DIAG-02 requirement validation by enabling compilation and execution of all 11 HVAC equipment case tests.
Output: All 11 HVAC equipment tests (800-810) compile and execute successfully.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-diagnostic-cases/18-CONTEXT.md
@.planning/phases/18-diagnostic-cases/18-VERIFICATION.md
@.planning/phases/18-diagnostic-cases/18-06-SUMMARY.md

# Gap Context from VERIFICATION.md

**Gap 1: HVAC Equipment Case Compilation Errors (BLOCKER)**
- Issue: Tests for Cases 800-810 use `apply_case_spec()` method which doesn't exist
- Root Cause: Test code uses wrong method name - should use `from_spec()`
- Impact: All 11 HVAC equipment case tests fail to compile, cannot validate DIAG-02 requirement
- Evidence: Compilation error: `error[E0599]: no method named 'apply_case_spec' found for struct 'ThermalModel<T>'`
- Fix Required: Replace all instances of `model.apply_case_spec(&case_spec);` with `let mut model = ThermalModel::<VectorField>::from_spec(&case_spec);` in tests/ashrae_140_cases_800_810.rs

# Existing Infrastructure

From 18-06-SUMMARY: Multi-reference database already populated with comprehensive reference ranges for all 11 HVAC equipment cases (800-810), including equipment_efficiency metrics (COP, EER) and cycling_losses.

From CONTEXT.md: Existing ASHRAE140Case enum with Cases 800-810 variants, CaseBuilder methods for all 11 cases, and comprehensive reference data in docs/ashrae_140_references.json.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix HVAC equipment test compilation errors</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>
    Replace all instances of incorrect `apply_case_spec()` method calls with correct `from_spec()` constructor usage in tests/ashrae_140_cases_800_810.rs.

    For each HVAC equipment test (Cases 800, 801, 802, 803, 804, 805, 806, 807, 808, 809, 810):

    BEFORE (incorrect):
    ```rust
    let case_spec = ASHRAE140Case::Case800.spec();
    let mut model = ThermalModel::<VectorField>::new(1);
    model.apply_case_spec(&case_spec);
    ```

    AFTER (correct):
    ```rust
    let case_spec = ASHRAE140Case::Case800.spec();
    let mut model = ThermalModel::<VectorField>::from_spec(&case_spec);
    ```

    Impact: Fixes all 11 test functions with compilation errors at lines 24, 84, 144, 204, 258, 312, 366, 420, 480, 540, 600 (total 11 occurrences).

    Reference: src/sim/engine.rs line 791 shows `ThermalModel::from_spec(spec: &CaseSpec) -> Self` is the correct constructor method.
  </action>
  <verify>
    <automated>cargo build --tests 2>&1 | grep -E "(error|warning|Compiling fluxion)" | head -20</automated>
  </verify>
  <done>
    All 11 HVAC equipment tests compile without errors. cargo build --tests completes successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify HVAC equipment tests execute and pass</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>
    Run all HVAC equipment tests to verify they execute correctly and produce energy values within reference ranges from multi-reference database.

    Run full test suite for Cases 800-810:
    ```bash
    cargo test test_ashrae_800 --lib
    cargo test test_ashrae_801 --lib
    # ... (all 11 cases)
    ```

    Expected behavior per VERIFICATION.md:
    - All 11 tests compile and execute
    - Energy values within reference ranges (e.g., Case 800: heating 8-12 MWh, cooling 6-10 MWh)
    - Equipment efficiency metrics validate (COP 3.0-4.0, EER 10.0-14.0)
    - No 0.00 MWh energy values (addressing Gap 2 if caused by compilation errors)

    Test pattern from existing tests (Case 800 example):
    - Create model from spec: `let mut model = ThermalModel::<VectorField>::from_spec(&case_spec);`
    - Run 1-year simulation with SurrogateManager
    - Validate total energy within reference range [14,000, 22,000] kWh
    - Validate equipment efficiency (COP, EER) within reference ranges
  </action>
  <verify>
    <automated>cargo test test_ashrae_800 test_ashrae_801 test_ashrae_802 test_ashrae_803 test_ashrae_804 test_ashrae_805 test_ashrae_806 test_ashrae_807 test_ashrae_808 test_ashrae_809 test_ashrae_810 --lib 2>&1 | grep -E "(test result|PASS|FAIL)"</automated>
  </verify>
  <done>
    All 11 HVAC equipment tests pass with energy values within reference ranges. Test output shows "test result: ok. 11 passed" for the full HVAC equipment test suite.
  </done>
</task>

</tasks>

<verification>
Overall verification checks:

1. **Compilation Verification**: Ensure tests/ashrae_140_cases_800_810.rs compiles without errors
   - Command: `cargo build --tests`
   - Expected: No compilation errors, all 11 tests compile successfully

2. **Test Execution Verification**: Ensure all HVAC equipment tests execute and pass
   - Command: `cargo test test_ashrae_800 --lib` through `cargo test test_ashrae_810 --lib`
   - Expected: All 11 tests pass with energy values within reference ranges

3. **Energy Value Verification**: Ensure CLI `validate-case 800` returns meaningful energy values (not 0.00 MWh)
   - Command: `./target/release/fluxion validate-case 800`
   - Expected: Returns heating 8-12 MWh, cooling 6-10 MWh (per reference ranges in docs/ashrae_140_references.json)

4. **Requirement Coverage**: Verify DIAG-02 requirement is satisfied
   - Evidence: All 11 Cases 800-810 tests pass with comprehensive validation of equipment efficiency and control strategies
</verification>

<success_criteria>
1. All 11 HVAC equipment tests (Cases 800-810) compile without errors
2. All 11 tests execute and pass with energy values within reference ranges
3. Equipment efficiency metrics (COP, EER) validate correctly against reference data
4. CLI `validate-case 800` returns meaningful energy values (not 0.00 MWh)
5. DIAG-02 requirement marked as satisfied in verification report
</success_criteria>

<output>
After completion, create `.planning/phases/18-diagnostic-cases/18-08-SUMMARY.md`
</output>
