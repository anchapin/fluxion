---
phase: 18-diagnostic-cases
plan: 14
type: execute
wave: 5
depends_on: [18-12, 18-13]
files_modified: [.planning/phases/18-diagnostic-cases/18-VERIFICATION.md, docs/KNOWN_ISSUES.md]
autonomous: true
requirements: [DIAG-02]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Thermal load calculation bug is documented with root cause analysis"
    - "Impact on Cases 802-810 is quantified and understood"
    - "Decision on whether to fix in Phase 18 or defer to future phase is made"
  artifacts:
    - path: "docs/KNOWN_ISSUES.md"
      provides: "Documented thermal load calculation bug with analysis"
      contains: "calc_analytical_loads() unrealistic values investigation"
    - path: ".planning/phases/18-diagnostic-cases/18-VERIFICATION.md"
      provides: "Updated verification status with bug decision"
      contains: "Thermal load bug marked as deferred or fixed"
  key_links:
    - from: "docs/KNOWN_ISSUES.md"
      to: "src/sim/engine.rs"
      via: "calc_analytical_loads() function reference"
      pattern: "calc_analytical_loads\\("
---

<objective>
Investigate thermal load calculation bug and document findings or implement fix.

Purpose: Thermal load calculation bug causes required_load to reach 155 MW instead of ~7.5 kW average for Cases 802-810, resulting in unrealistic thermal energy values (65 MWh vs expected 10-20 MWh). Previous commit 5762952 noted this as "out of scope". This plan determines whether to fix in Phase 18 or defer to a future phase.

Output: Either (1) thermal load calculation bug fixed, or (2) comprehensive documentation of bug with recommendation for future phase.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-diagnostic-cases/18-VERIFICATION.md
@.planning/phases/18-diagnostic-cases/18-11-SUMMARY.md
@src/sim/engine.rs

# Gap Context
From 18-VERIFICATION.md:
- Thermal load calculation bug causes required_load to reach 155 MW instead of ~7.5 kW average
- Results in unrealistic thermal energy values (65 MWh vs expected 10-20 MWh)
- Commit 5762952 notes this as "out of scope for this plan and needs separate investigation"
- Root cause: calc_analytical_loads() returns unrealistic values for certain case specifications

# Impact Assessment
Affected Cases: 802-810 (all HVAC equipment cases except 800-801)
Current Behavior:
- required_load reaches 155 MW (should be ~7.5 kW average)
- Thermal energy: 65 MWh (should be 10-20 MWh electrical)
- Tests fail even after fixing electrical energy calculation (Plan 18-12)

Expected Behavior:
- required_load should be ~7.5 kW average for Denver climate
- Electrical energy should be 10-20 MWh for HVAC equipment cases
- Cases 802-810 should pass after Plan 18-12 fixes

# Decision Criteria
Fix in Phase 18 if:
- Root cause is simple unit conversion or parameter issue
- Fix is isolated to calc_analytical_loads() without affecting other cases
- Fix can be completed in 2-3 tasks (30-45 minutes)

Defer to future phase if:
- Root cause requires deep thermal network analysis
- Fix affects multiple cases beyond 802-810
- Fix requires extensive validation and testing
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Investigate thermal load calculation bug</name>
  <files>src/sim/engine.rs</files>
  <decision>Should thermal load calculation bug be fixed in Phase 18 or deferred?</decision>
  <context>After investigating calc_analytical_loads(), determine if bug is simple fix or deep architectural issue. Commit 5762952 marked this as "out of scope" - needs reassessment.</context>
  <options>
    <option id="option-a">
      <name>Option A: Fix thermal load calculation bug in Phase 18</name>
      <pros>Completes DIAG-02 requirement fully, Cases 802-810 will pass, no technical debt</pros>
      <cons>May require debugging time, could uncover deeper thermal network issues</cons>
    </option>
    <option id="option-b">
      <name>Option B: Document bug and defer to Phase 19/20</name>
      <pros>Phase 18 remains focused on HVAC equipment validation, thermal load issue addressed in data quality phase</pros>
      <cons>Cases 802-810 may still fail despite Plan 18-12 fixes, DIAG-02 remains partially complete</cons>
    </option>
    <option id="option-c">
      <name>Option C: Investigate first, then decide</name>
      <pros>Creates comprehensive root cause analysis, informed decision on fix complexity</pros>
      <cons>Adds investigation time before deciding on approach</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Trace calc_analytical_loads() for Case 802</name>
  <files>src/sim/engine.rs, tests/ashrae_140_cases_800_810.rs</files>
  <action>Add debug output to trace calc_analytical_loads() behavior for Case 802:

1. Add temporary debug prints in calc_analytical_loads() to show:
   - Input parameters (zone temperature, setpoint, outdoor temperature)
   - Intermediate calculation values (temperature differences, conductances)
   - Output required_load value in Watts

2. Run Case 802 with debug output:
```bash
cargo test test_ashrae_802 --test ashrae_140_cases_800_810 -- --nocapture
```

3. Analyze output to identify:
   - At what timestep required_load spikes to 155 MW
   - Which input parameter causes the spike (extreme outdoor temp? wrong conductance?)
   - Whether calculation logic is correct or has unit conversion error

4. Compare with Case 800 (working) to identify difference:
   - Run Case 800 with same debug output
   - Compare input parameters and calculation values
   - Identify what causes Case 802 to fail while Case 800 succeeds</action>
  <verify>Debug output shows calc_analytical_loads() execution trace for Case 802 and Case 800</verify>
  <done>Root cause of thermal load calculation bug identified through trace analysis</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: Implement fix or document bug</name>
  <files>src/sim/engine.rs, docs/KNOWN_ISSUES.md</files>
  <decision>Based on investigation results, implement fix or document bug for future phase</decision>
  <context>After tracing calc_analytical_loads(), determine if bug is simple fix (e.g., unit conversion, missing division) or complex thermal network issue requiring deeper investigation.</context>
  <options>
    <option id="option-fix-simple">
      <name>Option: Implement simple fix now</name>
      <pros>Root cause is simple (unit error, missing division, incorrect parameter), fix can be completed quickly</pros>
      <cons>None - simple fix should be done now</cons>
    </option>
    <option id="option-fix-complex">
      <name>Option: Implement complex fix now</name>
      <pros>Root cause identified but fix requires multiple changes, still doable in Phase 18</pros>
      <cons>May exceed 30-45 minute task time, risk of regressions</cons>
    </option>
    <option id="option-defer">
      <name>Option: Document and defer</name>
      <pros>Root cause is complex thermal network issue, better addressed in Phase 20 (data quality) with comprehensive validation</pros>
      <cons>Diagnostic Cases 802-810 may not pass until future phase, DIAG-02 remains partial</cons>
    </option>
  </options>
  <resume-signal>Select: option-fix-simple, option-fix-complex, or option-defer</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Implement thermal load fix (if simple/complex fix selected)</name>
  <files>src/sim/engine.rs</files>
  <action>Based on root cause identified in Task 2, implement fix in calc_analytical_loads():

Common fix patterns for thermal load bugs:
1. Unit conversion: Add missing * 1000.0 or / 1000.0
2. Division by zero: Add check for small denominators
3. Parameter error: Fix incorrect conductance or temperature value
4. Missing factor: Apply missing thermal mass or area factor

Example fix for unit conversion:
```rust
// BEFORE (incorrect - Watts vs kW)
let required_load = temp_diff * conductance;

// AFTER (correct - convert Watts to kW)
let required_load = (temp_diff * conductance) / 1000.0;
```

After implementing fix:
1. Run Case 802 test to verify required_load is now ~7.5 kW average
2. Verify thermal energy is now 10-20 MWh (not 65 MWh)
3. Run all Cases 800-810 to ensure no regressions
4. Commit fix with clear description of root cause</action>
  <verify>cargo test --test ashrae_140_cases_800_810 && cargo test --lib</verify>
  <done>Thermal load calculation bug fixed, Cases 802-810 pass with realistic energy values</done>
</task>

<task type="auto">
  <name>Task 5: Document thermal load bug (if defer selected)</name>
  <files>docs/KNOWN_ISSUES.md, .planning/phases/18-diagnostic-cases/18-VERIFICATION.md</files>
  <action>Create comprehensive documentation of thermal load calculation bug:

1. Create or update docs/KNOWN_ISSUES.md with:
   - Bug title: "Thermal load calculation returns unrealistic values for HVAC equipment cases"
   - Affected cases: 802-810 (all HVAC equipment except 800-801)
   - Root cause: [detailed description from Task 2 investigation]
   - Impact: required_load reaches 155 MW instead of ~7.5 kW, thermal energy 65 MWh vs expected 10-20 MWh
   - Recommended action: Fix in Phase 20 (data quality) after Phase 19 statistical validation
   - Workaround: Cases 802-810 tests skipped until bug is fixed

2. Update 18-VERIFICATION.md to mark thermal load bug as "deferred to Phase 20":
   - Add to gaps_remaining: "Thermal load calculation bug deferred to Phase 20 (see docs/KNOWN_ISSUES.md)"
   - Update human_verification section to reference KNOWN_ISSUES.md

3. Update ROADMAP.md Phase 20 description to include thermal load fix:
   - Add to Phase 20 requirements: "Fix thermal load calculation bug for HVAC equipment cases 802-810"

4. Commit documentation with reference to commit 5762952 (original out-of-scope note)</action>
  <verify>docs/KNOWN_ISSUES.md exists with thermal load bug documentation, ROADMAP.md Phase 20 updated</verify>
  <done>Thermal load bug comprehensively documented and deferred to Phase 20 with clear action plan</done>
</task>

</tasks>

<verification>
1. If fix implemented: Run all HVAC equipment tests `cargo test --test ashrae_140_cases_800_810`
2. If fix implemented: Verify Cases 802-810 pass with electrical energy 10-20 MWh
3. If deferred: Verify docs/KNOWN_ISSUES.md exists with comprehensive bug documentation
4. If deferred: Verify ROADMAP.md Phase 20 includes thermal load fix task
5. Run full test suite: `cargo test --lib` to ensure no regressions
</verification>

<success_criteria>
Either:
- Thermal load calculation bug fixed and Cases 802-810 pass, OR
- Bug comprehensively documented in KNOWN_ISSUES.md and deferred to Phase 20 with clear action plan
</success_criteria>

<output>
After completion, create `.planning/phases/18-diagnostic-cases/18-14-SUMMARY.md`
</output>
