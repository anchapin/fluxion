---
phase: 18-diagnostic-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ashrae_140/diagnostics.rs
  - tests/ashrae_140_case_195_470.rs
  - tests/ashrae_140_case_non_residential.rs
  - tests/ashrae_140_solid_conduction_variants.rs
  - tests/ashrae_140_solar_gain_variants.rs
autonomous: true
requirements:
  - DIAG-01
  - DIAG-03
  - DIAG-04
  - DIAG-05
user_setup: []

must_haves:
  truths:
    - "Test stubs exist for all diagnostic case ranges (195-470, non-residential, solid conduction, solar gain)"
    - "Consolidated diagnostics.rs module provides helper functions for validation"
    - "All test stubs compile and can be executed with cargo test"
    - "Diagnostic validation logic is centralized in one module for maintainability"
  artifacts:
    - path: "tests/ashrae_140/diagnostics.rs"
      provides: "Consolidated validation logic module for diagnostic cases"
      min_lines: 50
    - path: "tests/ashrae_140_case_195_470.rs"
      provides: "Test stubs for Cases 195-470 range"
      min_lines: 30
    - path: "tests/ashrae_140_case_non_residential.rs"
      provides: "Test stubs for non-residential building types"
      min_lines: 30
    - path: "tests/ashrae_140_solid_conduction_variants.rs"
      provides: "Test stubs for solid conduction variants"
      min_lines: 30
    - path: "tests/ashrae_140_solar_gain_variants.rs"
      provides: "Test stubs for solar gain variants"
      min_lines: 30
  key_links:
    - from: "tests/ashrae_140/diagnostics.rs"
      to: "tests/ashrae_140_case_195_470.rs"
      via: "validate_diagnostic_range() helper function"
      pattern: "pub fn run_cases_195_470"
    - from: "tests/ashrae_140/diagnostics.rs"
      to: "src/validation/ashrae_140_validator.rs"
      via: "ASHRAE140Validator for validation logic"
      pattern: "ASHRAE140Validator::new"
---

<objective>
Create Wave 0 infrastructure for diagnostic case testing, including consolidated validation logic module and test stubs for all diagnostic case ranges (Cases 195-470, non-residential, solid conduction, solar gain).

Purpose: Establish test infrastructure and scaffolding for diagnostic case implementation, following hybrid structure decision from CONTEXT.md (consolidated validation logic + public case spec functions).

Output: Test stubs in place, diagnostics.rs module with helper functions, all tests compile and run.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-diagnostic-cases/18-CONTEXT.md
@.planning/phases/18-diagnostic-cases/18-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/validation/ashrae_140_validator.rs
@tests/ashrae_140_case_195_solid_conduction.rs
@tests/ashrae_140_cases_800_810.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create consolidated diagnostics.rs module</name>
  <files>tests/ashrae_140/diagnostics.rs</files>
  <action>
    Create new module `tests/ashrae_140/diagnostics.rs` with:

    1. **Module documentation**: Explain consolidated validation logic pattern for diagnostic cases

    2. **Helper functions**:
       - `validate_diagnostic_range(start, end, validator)` - Generic function to validate case range and return DiagnosticRangeResult with summary
       - `run_cases_195_470()` - Specific helper for Cases 195-470 (calls validate_diagnostic_range(195, 470))
       - `run_cases_800_810()` - Specific helper for Cases 800-810 (calls validate_diagnostic_range(800, 810))

    3. **DiagnosticRangeResult struct**:
       - `range: String` (e.g., "195-470", "800-810")
       - `total_cases: usize`
       - `passed: usize`
       - `results: Vec<(String, ValidationResult)>`

    4. **Integration with ASHRAE140Validator**:
       - Use existing ASHRAE140Validator from src/validation/ashrae_140_validator.rs
       - Call `validator.validate_case(&case_id)` for each case in range

    5. **Error handling**:
       - Handle case validation errors gracefully (continue processing, log warning)
       - Return comprehensive summary even if individual cases fail

    Follow existing pattern from Phase 17 (building_profiles.json) and Phase 15 (HVAC equipment integration).
  </action>
  <verify>cargo test --lib diagnostics:: 2>&1 | head -20</verify>
  <done>diagnostics.rs module exists with validate_diagnostic_range(), run_cases_195_470(), run_cases_800_810() helpers, DiagnosticRangeResult struct defined</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for Cases 195-470</name>
  <files>tests/ashrae_140_case_195_470.rs</files>
  <action>
    Create new test file `tests/ashrae_140_case_195_470.rs` with:

    1. **Module documentation**: Explain Cases 195-470 as in-depth diagnostics for lighting, equipment, thermal mass behavior

    2. **Test stub functions** for representative cases:
       - `test_case_196_lighting_diagnostics()` - Placeholder for lighting diagnostic case
       - `test_case_200_equipment_diagnostics()` - Placeholder for equipment diagnostic case
       - `test_case_250_thermal_mass_diagnostics()` - Placeholder for thermal mass diagnostic case
       - `test_cases_195_470_integration()` - Integration test calling `diagnostics::run_cases_195_470()`

    3. **Placeholder assertions**:
       - Use simple assertions like `assert!(true)` or TODO markers
       - Document that full implementations will reference ASHRAE 140 specifications
       - Follow pattern from existing Cases 800-810 test stubs (tests/ashrae_140_cases_800_810.rs)

    4. **Integration with diagnostics module**:
       - Import `diagnostics` module from tests/ashrae_140/diagnostics.rs
       - Call `run_cases_195_470()` for integration test

    Note: These are Wave 0 stubs - full implementations will be added in Plan 18-02.
  </action>
  <verify>cargo test --lib test_case_196 2>&1 | head -10</verify>
  <done>test_case_195_470.rs exists with representative test stubs (196, 200, 250, integration test), all tests compile</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for non-residential cases</name>
  <files>tests/ashrae_140_case_non_residential.rs</files>
  <action>
    Create new test file `tests/ashrae_140_case_non_residential.rs` with:

    1. **Module documentation**: Explain non-residential building types (Office, Retail, School) extending validation beyond lightweight residential assumptions

    2. **Test stub functions** for building types:
       - `test_case_office_building()` - Office building non-residential case
       - `test_case_retail_building()` - Retail building non-residential case
       - `test_case_school_building()` - School building non-residential case
       - `test_non_residential_integration()` - Integration test for all non-residential cases

    3. **Placeholder implementations**:
       - Use simple model creation: `ThermalModel::<VectorField>::new(1)`
       - Set reasonable building parameters (different from residential)
       - Apply building profiles from Phase 17 (load_building_profile)
       - Placeholder assertions: `assert!(energy > 0.0)`

    4. **Integration with building profiles**:
       - Import ProfileBundle and load_building_profile from src/sim/profiles.rs
       - Use Office, Retail, School profiles from data/building_profiles.json

    Note: Full implementations will be added in Plan 18-04 after ASHRAE140Case enum is extended.
  </action>
  <verify>cargo test --lib test_case_office 2>&1 | head -10</verify>
  <done>test_case_non_residential.rs exists with Office/Retail/School test stubs, all tests compile with profile loading</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for solid conduction variants</name>
  <files>tests/ashrae_140_solid_conduction_variants.rs</files>
  <action>
    Create new test file `tests/ashrae_140_solid_conduction_variants.rs` with:

    1. **Module documentation**: Explain solid conduction variants for testing thermal mass behavior, inter-zone conduction, thermal bridge effects

    2. **Test stub functions** for variants:
       - `test_case_195_high_mass_walls()` - High-mass construction variant
       - `test_case_195_no_internal_loads()` - Zero internal loads variant
       - `test_case_195_no_solar_gains()` - Zero solar gains variant (baseline)
       - `test_case_195_thermal_bridge()` - Thermal bridge effects variant

    3. **Placeholder implementations**:
       - Use Case 195 as baseline (existing in src/validation/ashrae_140_cases.rs)
       - Modify construction type (high-mass walls)
       - Set internal loads to zero
       - Disable solar gains (zero SHGC)
       - Add thermal bridge conductance

    4. **Integration with existing Case 195**:
       - Import ASHRAE140Case::Case195 from src/validation/ashrae_140_cases.rs
       - Use Case195.spec() as baseline for variants

    Note: Full implementations will be added in Plan 18-05 after Case 195 is used as baseline in Plan 18-02.
  </action>
  <verify>cargo test --lib test_case_195_high_mass 2>&1 | head -10</verify>
  <done>test_case_solid_conduction_variants.rs exists with high-mass/zero-loads/zero-solar/thermal-bridge stubs, all tests compile</done>
</task>

<task type="auto">
  <name>Task 5: Create test stubs for solar gain variants</name>
  <files>tests/ashrae_140_solar_gain_variants.rs</files>
  <action>
    Create new test file `tests/ashrae_140_solar_gain_variants.rs` with:

    1. **Module documentation**: Explain solar gain variants for testing window properties (SHGC, albedo) and their impact on cooling loads

    2. **Test stub functions** for variants:
       - `test_case_195_shgc_low()` - Low SHGC variant (0.3)
       - `test_case_195_shgc_medium()` - Medium SHGC variant (0.6)
       - `test_case_195_shgc_high()` - High SHGC variant (0.9)
       - `test_case_195_albedo_low()` - Low albedo variant (0.1)
       - `test_case_195_albedo_medium()` - Medium albedo variant (0.5)
       - `test_case_195_albedo_high()` - High albedo variant (0.9)

    3. **Placeholder implementations**:
       - Use Case 195 as baseline
       - Modify WindowSpec SHGC values (0.3, 0.6, 0.9)
       - Modify surface albedo values (0.1, 0.5, 0.9)
       - Validate cooling energy differences between variants

    4. **Integration with WindowSpec**:
       - Import WindowSpec from src/validation/ashrae_140_cases.rs
       - Use WindowSpec::new(u_value, shgc, normal_transmittance, glass_type) for variant creation

    Note: Full implementations will be added in Plan 18-06 after Case 195 is used as baseline.
  </action>
  <verify>cargo test --lib test_case_195_shgc 2>&1 | head -10</verify>
  <done>test_case_solar_gain_variants.rs exists with SHGC and albedo variant stubs, all tests compile</done>
</task>

</tasks>

<verification>
Run all diagnostic case test stubs to verify compilation:
```bash
cargo test --lib test_case_195_470
cargo test --lib test_case_office
cargo test --lib test_case_195_high_mass
cargo test --lib test_case_195_shgc
```

All tests should compile and execute (even if placeholder assertions).
</verification>

<success_criteria>
1. All 5 test stub files exist (diagnostics.rs, 195_470, non_residential, solid_conduction, solar_gain)
2. diagnostics.rs module provides validate_diagnostic_range(), run_cases_195_470(), run_cases_800_810() helper functions
3. All test stubs compile without errors
4. Integration tests call diagnostics module helpers
5. DiagnosticRangeResult struct defined with range, total_cases, passed, results fields
</success_criteria>

<output>
After completion, create `.planning/phases/18-diagnostic-cases/18-01-SUMMARY.md`
</output>
