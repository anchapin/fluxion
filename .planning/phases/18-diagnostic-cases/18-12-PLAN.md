---
phase: 18-diagnostic-cases
plan: 12
type: execute
wave: 4
depends_on: [18-11]
files_modified: [tests/ashrae_140_cases_800_810.rs]
autonomous: true
requirements: [DIAG-02]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Cases 802-810 use get_electrical_energy_kwh() for validation"
    - "Cases 802-810 tests pass with electrical energy within reference ranges"
    - "HVAC equipment cases 800-810 all use consistent energy calculation method"
  artifacts:
    - path: "tests/ashrae_140_cases_800_810.rs"
      provides: "HVAC equipment tests with correct electrical energy validation"
      contains: "get_electrical_energy_kwh() for Cases 802-810"
  key_links:
    - from: "tests/ashrae_140_cases_800_810.rs"
      to: "src/sim/engine.rs"
      via: "get_electrical_energy_kwh() method"
      pattern: "model\\.get_electrical_energy_kwh\\(\\)"
---

<objective>
Fix HVAC equipment Cases 802-810 to use electrical energy calculation method instead of thermal energy sum.

Purpose: Previous commit 5762952 only fixed Cases 800-801; Cases 802-810 still incorrectly use thermal energy (heating + cooling) for validation instead of electrical energy, causing test failures with unrealistic energy values (65 MWh vs expected 10-20 MWh).

Output: Cases 802-810 tests pass with electrical energy within reference ranges.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-diagnostic-cases/18-VERIFICATION.md
@.planning/phases/18-diagnostic-cases/18-11-SUMMARY.md
@tests/ashrae_140_cases_800_810.rs

# Gap Context
From 18-VERIFICATION.md:
- Lines 166, 228, 284, 340, 396, 452, 514, 576, 638 use `get_heating_energy_kwh() + get_cooling_energy_kwh()`
- Should use `get_electrical_energy_kwh()` like Cases 800-801 (lines 50, 104)
- Commit 5762952 only fixed Cases 800-801, not 802-810

# Pattern from Cases 800-801 (CORRECT)
Case 800 (line 50):
```rust
let total_energy = model.get_electrical_energy_kwh();
```

Case 801 (line 104):
```rust
let total_energy = model.get_electrical_energy_kwh();
```

# Pattern from Cases 802-810 (INCORRECT)
Case 802 (line 166):
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

Case 803 (line 228):
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

(Same pattern repeats for Cases 804-810 at lines 284, 340, 396, 452, 514, 576, 638)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Case 802 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 166 in test_ashrae_802():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 (lines 50, 104) and correctly validates electrical energy consumption for heat pump equipment.</action>
  <verify>cargo test test_ashrae_802 --test ashrae_140_cases_800_810</verify>
  <done>Case 802 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 2: Fix Case 803 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 228 in test_ashrae_803():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for chiller equipment.</action>
  <verify>cargo test test_ashrae_803 --test ashrae_140_cases_800_810</verify>
  <done>Case 803 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 3: Fix Case 804 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 284 in test_ashrae_804():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for boiler equipment.</action>
  <verify>cargo test test_ashrae_804 --test ashrae_140_cases_800_810</verify>
  <done>Case 804 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 4: Fix Case 805 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 340 in test_ashrae_805():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for VAV system equipment.</action>
  <verify>cargo test test_ashrae_805 --test ashrae_140_cases_800_810</verify>
  <done>Case 805 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 5: Fix Case 806 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 396 in test_ashrae_806():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for CAV system equipment.</action>
  <verify>cargo test test_ashrae_806 --test ashrae_140_cases_800_810</verify>
  <done>Case 806 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 6: Fix Case 807 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 452 in test_ashrae_807():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for heat pump with economizer.</action>
  <verify>cargo test test_ashrae_807 --test ashrae_140_cases_800_810</verify>
  <done>Case 807 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 7: Fix Case 808 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 514 in test_ashrae_808():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for chiller with economizer.</action>
  <verify>cargo test test_ashrae_808 --test ashrae_140_cases_800_810</verify>
  <done>Case 808 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 8: Fix Case 809 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 576 in test_ashrae_809():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for VAV with economizer.</action>
  <verify>cargo test test_ashrae_809 --test ashrae_140_cases_800_810</verify>
  <done>Case 809 test uses electrical energy calculation and passes</done>
</task>

<task type="auto">
  <name>Task 9: Fix Case 810 energy calculation method</name>
  <files>tests/ashrae_140_cases_800_810.rs</files>
  <action>Replace line 638 in test_ashrae_810():

FROM:
```rust
let total_energy = model.get_heating_energy_kwh() + model.get_cooling_energy_kwh();
```

TO:
```rust
let total_energy = model.get_electrical_energy_kwh();
```

This matches the pattern used in Cases 800-801 and correctly validates electrical energy consumption for CAV with economizer.</action>
  <verify>cargo test test_ashrae_810 --test ashrae_140_cases_800_810</verify>
  <done>Case 810 test uses electrical energy calculation and passes</done>
</task>

</tasks>

<verification>
1. Run all HVAC equipment tests: `cargo test --test ashrae_140_cases_800_810`
2. Verify Cases 800-810 all pass with electrical energy within reference ranges
3. Verify no test uses `get_heating_energy_kwh() + get_cooling_energy_kwh()` pattern
</verification>

<success_criteria>
All Cases 800-810 tests pass with electrical energy consumption within ASHRAE 140 reference ranges. Cases 802-810 no longer use thermal energy sum for validation.
</success_criteria>

<output>
After completion, create `.planning/phases/18-diagnostic-cases/18-12-SUMMARY.md`
</output>
