---
phase: 18-diagnostic-cases
plan: 05
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - src/validation/ashrae_140_cases.rs
  - tests/ashrae_140_solid_conduction_variants.rs
  - tests/ashrae_140_solar_gain_variants.rs
autonomous: true
requirements:
  - DIAG-04
  - DIAG-05
user_setup: []
must_haves:
  truths:
    - "Solid conduction variants (high-mass, no loads, no solar, thermal bridge) implemented and validated"
    - "Solar gain variants (SHGC 0.3/0.6/0.9, albedo 0.1/0.5/0.9) implemented and validated"
    - "ASHRAE140Case enum extended with solar gain variants"
    - "Solid conduction variants expose thermal mass and heat transfer edge cases"
    - "Solar gain variants validate window properties and albedo effects on cooling loads"
  artifacts:
    - path: "src/validation/ashrae_140_cases.rs"
      provides: "Case195SHGC03, Case195SHGC06, Case195SHGC09, Case195Albedo01, Case195Albedo05, Case195Albedo09 enum variants"
      min_lines: 100
    - path: "tests/ashrae_140_solid_conduction_variants.rs"
      provides: "Full implementations for solid conduction variants"
      min_lines: 150
    - path: "tests/ashrae_140_solar_gain_variants.rs"
      provides: "Full implementations for solar gain variants"
      min_lines: 150
  key_links:
    - from: "tests/ashrae_140_solid_conduction_variants.rs"
      to: "src/validation/ashrae_140_cases.rs"
      via: "ASHRAE140Case enum usage for Case195 variants"
      pattern: "ASHRAE140Case::Case195HighMass.spec()"
---

    - from: "tests/ashrae_140_solar_gain_variants.rs"
      to: "src/validation/ashrae_140_cases.rs"
      via: "WindowSpec usage for SHGC and albedo variations"
      pattern: "WindowSpec::new(u_value, shgc, normal_transmittance, glass_type)"
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
From src/validation/ashrae_140_cases.rs:
```rust
pub struct WindowSpec {
    pub u_value: f64,        // W/m²K
    pub shgc: f64,            // Solar Heat Gain Coefficient (0-1)
    pub normal_transmittance: f64,  // (0-1)
    pub glass_type: GlassType,
    pub emissivity: f64,       // (0-1)
}
impl WindowSpec {
    pub fn new(u_value: f64, shgc: f64, normal_transmittance: f64, glass_type: GlassType) -> Self;
    pub fn double_clear_glass() -> Self {
        WindowSpec::new(3.0, 0.789, 0.86156, GlassType::DoubleClear)
    }
    pub fn double_low_e() -> Self {
        WindowSpec::new(2.0, 0.65, 0.70, GlassType::DoubleLowE)
    }
}
// Case195 already exists (from Plan 18-02):
ASHRAE140Case::Case195 => CaseBuilder::case_195_solid_conduction()
```
</interfaces>
</context>
</context>
<tasks>
<task type="auto">
  <name>Task 1: Extend ASHRAE140Case enum with solar gain variants</name>
  <files>src/validation/ashrae_140_cases.rs</files>
  <action>
    Extend ASHRAE140Case enum in src/validation/ashrae_140_cases.rs with solar gain variants:
    1. **Add SHGC variant enum variants**:
       - Case195SHGC03: Low SHGC variant (0.3)
       - Case195SHGC06: Medium SHGC variant (0.6)
       - Case195SHGC09: High SHGC variant (0.9)
    2. **Add albedo variant enum variants**:
       - Case195Albedo01: Low albedo variant (0.1 - dark surface)
       - Case195Albedo05: Medium albedo variant (0.5 - gray surface)
       - Case195Albedo09: High albedo variant (0.9 - reflective surface)
    3. **Implement spec() method** for SHGC variants (use Case195 as baseline):
       - **Case195SHGC03**: Same as Case195 but WindowSpec with SHGC=0.3
         - WindowSpec::new(3.0, 0.3, 0.5, GlassType::DoubleClear)  // Low solar transmittance
         - All other parameters: Same as Case195
       - **Case195SHGC06**: Same as Case195 but WindowSpec with SHGC=0.6
         - WindowSpec::new(3.0, 0.6, 0.7, GlassType::DoubleClear)  // Medium solar transmittance
         - All other parameters: Same as Case195
       - **Case195SHGC09**: Same as Case195 but WindowSpec with SHGC=0.9
         - WindowSpec::new(3.0, 0.9, 0.95, GlassType::DoubleClear)  // High solar transmittance
         - All other parameters: Same as Case195
    4. **Implement spec() method** for albedo variants (use Case195 as baseline):
       - **Case195Albedo01**: Same as Case195 but surface albedo=0.1
         - Albedo: 0.1 (dark surface absorbs solar radiation)
         - All other parameters: Same as Case195
       - **Case195Albedo05**: Same as Case195 but surface albedo=0.5
         - Albedo: 0.5 (gray surface reflects 50%)
         - All other parameters: Same as Case195
       - **Case195Albedo09**: Same as Case195 but surface albedo=0.9
         - Albedo: 0.9 (reflective surface reflects 90%)
         - All other parameters: Same as Case195
    5. **Implement number() method** for each new case:
       - Case195SHGC03: "195-SHGC0.3"
       - Case195SHGC06: "195-SHGC0.6"
       - Case195SHGC09: "195-SHGC0.9"
       - Case195Albedo01: "195-ALB0.1"
       - Case195Albedo05: "195-ALB0.5"
       - Case195Albedo09: "195-ALB0.9"
    6. **Implement description() method** for each new case:
       - Return human-readable description (e.g., "Case195 with low SHGC (0.3)")
    7. **Update all match statements**:
       - Ensure spec(), number(), description() have exhaustive matches for all new cases
    Note: Albedo affects solar radiation absorption/reflection in weather data processing. Case195 variants use Case195 as baseline from Plan 18-02.
  </action>
  <verify>cargo build --lib 2>&1 | grep -i error || echo "No errors found"</verify>
  <done>ASHRAE140Case enum extended with SHGC and albedo variants, all match statements updated</done>
</task>
<task type="auto">
  <name>Task 2: Add CaseBuilder methods for solar gain variants</name>
  <files>src/validation/ashrae_140_cases.rs</files>
  <action>
    Add CaseBuilder methods to src/validation/ashrae_140_cases.rs for solar gain variants:
    1. **case_195_shgc_low()**:
       - Same as Case195 (solid conduction baseline)
       - WindowSpec: SHGC=0.3 (low solar heat gain)
       - Normal transmittance: 0.5 (matches low SHGC)
       - Glass type: DoubleClear
       - All other parameters: Same as Case195
    2. **case_195_shgc_medium()**:
       - Same as Case195
       - WindowSpec: SHGC=0.6 (medium solar heat gain)
       - Normal transmittance: 0.7 (matches medium SHGC)
       - Glass type: DoubleClear
       - All other parameters: Same as Case195
    3. **case_195_shgc_high()**:
       - Same as Case195
       - WindowSpec: SHGC=0.9 (high solar heat gain)
       - Normal transmittance: 0.95 (matches high SHGC)
       - Glass type: DoubleClear
       - All other parameters: Same as Case195
    4. **case_195_albedo_low()**:
       - Same as Case195
       - Surface albedo: 0.1 (dark surface absorbs solar radiation)
       - All other parameters: Same as Case195
    5. **case_195_albedo_medium()**:
       - Same as Case195
       - Surface albedo: 0.5 (gray surface reflects 50%)
       - All other parameters: Same as Case195
    6. **case_195_albedo_high()**:
       - Same as Case195
       - Surface albedo: 0.9 (reflective surface reflects 90%)
       - All other parameters: Same as Case195
    Follow existing CaseBuilder pattern and WindowSpec usage from existing cases.
  </action>
  <verify>cargo test --lib test_case_195_shgc 2>&1 | head -20</verify>
  <done>CaseBuilder methods exist for SHGC and albedo variants, all create valid CaseSpec structs</done>
</task>
<task type="auto">
  <name>Task 3: Implement solid conduction variant tests</name>
  <files>tests/ashrae_140_solid_conduction_variants.rs</files>
  <action>
    Update tests/ashrae_140_solid_conduction_variants.rs with full implementations (replace TODO placeholders):
    1. **test_case_195_high_mass_walls()**:
       - Use ASHRAE140Case::Case195HighMass.spec() to get case spec
       - Create ThermalModel from spec
       - Simulate 1 year: model.simulate_year()
       - Assert annual heating lower than Case195 baseline (thermal mass reduces heating demand)
       - Assert peak cooling lower than Case195 baseline (thermal mass reduces temperature swings)
       - Validate construction type is high-mass
    2. **test_case_195_no_internal_loads()**:
       - Use ASHRAE140Case::Case195NoLoads.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual cooling much lower than Case195 baseline (no internal heat gains)
       - Assert annual heating similar or slightly higher than Case195 (no internal heat gains to offset)
       - Validate internal loads are zero (lighting=0, equipment=0, occupancy=0)
    3. **test_case_195_no_solar_gains()**:
       - Use ASHRAE140Case::Case195NoSolar.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual cooling much lower than Case195 baseline (no solar heat gains)
       - Assert annual heating similar or slightly higher than Case195 (no solar gains to offset)
       - Validate SHGC=0.0 (zero solar transmittance)
    4. **test_case_195_thermal_bridge()**:
       - Use ASHRAE140Case::Case195ThermalBridge.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual heating higher than Case195 baseline (thermal bridges increase heat loss)
       - Assert annual cooling higher than Case195 baseline (thermal bridges increase heat gain in summer)
       - Validate thermal bridge conductance is present (additional h_tr_em)
    5. **test_solid_conduction_variants_integration()**:
       - Run all four solid conduction variants
       - Verify pass rate > 80% (at least 3/4 cases pass validation)
       - Print summary: "Solid conduction variants: 3/4 passed (HighMass ✓, NoLoads ✓, NoSolar ✓, ThermalBridge ✗)"
    Follow existing test pattern and Case195 baseline from Plan 18-02.
  </action>
  <verify>cargo test --lib test_case_195_high_mass 2>&1 | tail -5</verify>
  <done>All solid conduction variant tests implemented with full validation against Case195 baseline</done>
</task>
<task type="auto">
  <name>Task 4: Implement solar gain variant tests</name>
  <files>tests/ashrae_140_solar_gain_variants.rs</files>
  <action>
    Update tests/ashrae_140_solar_gain_variants.rs with full implementations (replace TODO placeholders):
    1. **test_case_195_shgc_low()**:
       - Use ASHRAE140Case::Case195SHGC03.spec() to get case spec
       - Create ThermalModel from spec
       - Simulate 1 year: model.simulate_year()
       - Assert annual cooling lower than Case195 baseline (low SHGC reduces solar heat gain)
       - Assert annual heating similar or slightly higher than Case195 (less solar gain to offset)
       - Validate SHGC=0.3 (low solar transmittance)
    2. **test_case_195_shgc_medium()**:
       - Use ASHRAE140Case::Case195SHGC06.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual cooling similar to Case195 baseline (medium SHGC similar to default 0.789)
       - Validate SHGC=0.6 (medium solar transmittance)
    3. **test_case_195_shgc_high()**:
       - Use ASHRAE140Case::Case195SHGC09.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual cooling higher than Case195 baseline (high SHGC increases solar heat gain)
       - Assert annual heating lower than Case195 (more solar gain to offset heating)
       - Validate SHGC=0.9 (high solar transmittance)
    4. **test_case_195_albedo_low()**:
       - Use ASHRAE140Case::Case195Albedo01.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual cooling higher than Case195 baseline (low albedo absorbs solar radiation)
       - Assert annual heating lower than Case195 (absorbed solar reduces heating)
       - Validate albedo=0.1 (dark surface)
    5. **test_case_195_albedo_medium()**:
       - Use ASHRAE140Case::Case195Albedo05.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual cooling similar to Case195 baseline (medium albedo reflects 50%)
       - Validate albedo=0.5 (gray surface)
    6. **test_case_195_albedo_high()**:
       - Use ASHRAE140Case::Case195Albedo09.spec()
       - Create ThermalModel from spec
       - Simulate 1 year
       - Assert annual cooling lower than Case195 baseline (high albedo reflects 90%)
       - Assert annual heating higher than Case195 (reflected solar reduces heating)
       - Validate albedo=0.9 (reflective surface)
    7. **test_solar_gain_variants_integration()**:
       - Run all six solar gain variants (3 SHGC + 3 albedo)
       - Verify pass rate > 80% (at least 5/6 cases pass validation)
       - Print summary: "Solar gain variants: 5/6 passed (SHGC0.3 ✓, SHGC0.6 ✓, SHGC0.9 ✗, ALB0.1 ✓, ALB0.5 ✓, ALB0.9 ✗)"
       - Validate SHGC trend: Cooling decreases as SHGC decreases (0.9 > 0.6 > 0.3)
       - Validate albedo trend: Cooling decreases as albedo increases (0.1 < 0.5 < 0.9)
    Follow existing test pattern and Case195 baseline.
  </action>
  <verify>cargo test --lib test_case_195_shgc 2>&1 | tail -5</verify>
  <done>All solar gain variant tests (SHGC 0.3/0.6/0.9, albedo 0.1/0.5/0.9) implemented with full validation</done>
</task>
</tasks>
<verification>
Run solid conduction and solar gain variant tests to verify implementations:
```bash
cargo test --lib test_case_195_high_mass
cargo test --lib test_case_195_no_loads
cargo test --lib test_case_195_shgc
cargo test --lib test_case_195_albedo
cargo test --lib test_solid_conduction_variants_integration
cargo test --lib test_solar_gain_variants_integration
```
All tests should pass with energy values showing expected trends (SHGC lower → cooling lower, albedo higher → cooling lower).
</verification>
<success_criteria>
1. ASHRAE140Case enum has SHGC variants (Case195SHGC03/06/09) and albedo variants (Case195Albedo01/05/09)
2. All match statements updated for new variants
3. CaseBuilder methods exist for all solar gain variants
4. All solid conduction variant tests pass (high-mass, no loads, no solar, thermal bridge)
5. All solar gain variant tests pass (SHGC 0.3/0.6/0.9, albedo 0.1/0.5/0.9)
6. SHGC trend validated: Cooling decreases as SHGC decreases
7. Albedo trend validated: Cooling decreases as albedo increases
8. Integration tests show >80% pass rate for both variant groups
</success_criteria>
<output>
After completion, create `.planning/phases/18-diagnostic-cases/18-05-SUMMARY.md`
</output>
