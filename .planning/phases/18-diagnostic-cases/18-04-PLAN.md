---
phase: 18-diagnostic-cases
plan: 04
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - src/validation/ashrae_140_cases.rs
  - tests/ashrae_140_case_non_residential.rs
autonomous: true
requirements:
  - DIAG-03
  - DIAG-04
user_setup: []
must_haves:
  truths:
    - "Non-residential building types (Office, Retail, School) implemented and validated"
    - "Solid conduction variants (high-mass, zero loads, zero solar, thermal bridge) implemented"
    - "ASHRAE140Case enum extended with non-residential and solid conduction variants"
    - "Non-residential cases extend validation beyond lightweight residential assumptions"
    - "Solid conduction variants expose thermal mass and heat transfer edge cases"
  artifacts:
    - path: "src/validation/ashrae_140_cases.rs"
      provides: "Office, Retail, School, Case195HighMass, Case195NoLoads, Case195NoSolar, Case195ThermalBridge enum variants"
      min_lines: 150
    - path: "tests/ashrae_140_case_non_residential.rs"
      provides: "Full implementations for Office, Retail, School non-residential cases"
      min_lines: 150
  key_links:
    - from: "tests/ashrae_140_case_non_residential.rs"
      to: "src/sim/profiles.rs"
      via: "load_building_profile() for Office/Retail/School profiles"
      pattern: "load_building_profile(BuildingType::Office)"
    - from: "tests/ashrae_140_case_non_residential.rs"
      to: "src/validation/ashrae_140_cases.rs"
      via: "ASHRAE140Case enum usage for non-residential specs"
      pattern: "ASHRAE140Case::Office.spec()"
---
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
From src/sim/occupancy.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum BuildingType {
    /// Office building type with standard occupancy and schedule
    Office,
    /// Retail building type with customer-facing occupancy
    Retail,
    /// School building type with educational occupancy and schedule
    School,
    // ... more types ...
}
```
From src/sim/profiles.rs (Phase 17):
```rust
pub struct ProfileBundle {
    pub lighting: f64,  // W/m² power density
    pub equipment: Vec<Box<dyn Equipment + Send + Sync>>,
    pub occupancy: Option<OccupancyProfile>,
}
pub fn load_building_profile(building_type: BuildingType) -> Result<ProfileBundle, String> {
    // Loads from data/building_profiles.json
    // Returns ProfileBundle for Office, Retail, School
}
```
</interfaces>
</context>
</context>
<tasks>
<task type="auto">
  <name>Task 1: Extend ASHRAE140Case enum with non-residential and solid conduction variants</name>
  <files>src/validation/ashrae_140_cases.rs</files>
  <action>
    Extend ASHRAE140Case enum in src/validation/ashrae_140_cases.rs with:
    1. **Add non-residential enum variants**:
       - Office: Office building (medium-mass, typical office schedule)
       - Retail: Retail building (medium-mass, retail schedule with extended hours)
       - School: School building (high-mass, educational schedule)
    2. **Add solid conduction variant enum variants**:
       - Case195HighMass: High-mass walls (concrete instead of lightweight)
       - Case195NoLoads: Zero internal loads (lighting=0, equipment=0, occupancy=0)
       - Case195NoSolar: Zero solar gains (SHGC=0, no windows)
       - Case195ThermalBridge: Thermal bridge effects (additional conductance through structural elements)
    3. **Implement spec() method** for non-residential cases:
       - **Office**: Use CaseBuilder to create office building spec
         - Construction: Medium-mass (mixed concrete/lightweight walls)
         - Dimensions: 20.0 × 15.0 × 3.0 m (larger than residential)
         - Windows: 40.0 m² total (south, east, west facades)
         - HVAC: Standard HVAC system ( IdealHVACController or equipment from Cases 800-810)
         - Internal loads: Office profile (lighting 10 W/m², equipment 20 W/m², occupancy 0.05 people/m²)
         - Schedule: Office schedule (8am-6pm weekdays, off weekends)
       - **Retail**: Use CaseBuilder to create retail building spec
         - Construction: Medium-mass (similar to office)
         - Dimensions: 25.0 × 20.0 × 4.0 m (larger than office)
         - Windows: 60.0 m² total (extensive glazing for display)
         - HVAC: Standard HVAC system
         - Internal loads: Retail profile (lighting 12 W/m², equipment 10 W/m², occupancy 0.1 people/m²)
         - Schedule: Retail schedule (9am-9pm weekdays, 10am-6pm weekends)
       - **School**: Use CaseBuilder to create school building spec
         - Construction: High-mass (concrete walls for thermal mass)
         - Dimensions: 30.0 × 25.0 × 3.5 m (largest building)
         - Windows: 50.0 m² total (classroom glazing)
         - HVAC: Standard HVAC system
         - Internal loads: School profile (lighting 8 W/m², equipment 15 W/m², occupancy 0.2 people/m²)
         - Schedule: School schedule (8am-3pm weekdays, off weekends)
    4. **Implement spec() method** for solid conduction variants (use Case195 as baseline):
       - **Case195HighMass**: Same as Case195 but use high-mass construction (concrete walls)
       - **Case195NoLoads**: Same as Case195 but set internal loads to zero (lighting=0, equipment=0, occupancy=0)
       - **Case195NoSolar**: Same as Case195 but set SHGC=0 (zero solar gains)
       - **Case195ThermalBridge**: Same as Case195 but add thermal bridge conductance (additional h_tr_em for structural elements)
    5. **Implement number() method** for each new case:
       - Office: "OFFICE"
       - Retail: "RETAIL"
       - School: "SCHOOL"
       - Case195HighMass: "195-HM"
       - Case195NoLoads: "195-NL"
       - Case195NoSolar: "195-NS"
       - Case195ThermalBridge: "195-TB"
    6. **Implement description() method** for each new case:
       - Return human-readable description
    7. **Update all match statements**:
       - Ensure spec(), number(), description() have exhaustive matches for all new cases
       - Add to construction_type() if needed (non-residential may use different types)
    Note: Non-residential cases use building profiles from Phase 17 (data/building_profiles.json). Solid conduction variants use Case195 as baseline.
  </action>
  <verify>cargo build --lib 2>&1 | grep -i error || echo "No errors found"</verify>
  <done>ASHRAE140Case enum extended with Office/Retail/School and Case195 variants, all match statements updated</done>
</task>
<task type="auto">
  <name>Task 2: Add CaseBuilder methods for non-residential and solid conduction variants</name>
  <files>src/validation/ashrae_140_cases.rs</files>
  <action>
    Add CaseBuilder methods to src/validation/ashrae_140_cases.rs for new cases:
    1. **office_building()**:
       - Construction: Medium-mass (mixed concrete/lightweight)
       - Dimensions: 20.0 × 15.0 × 3.0 m
       - Windows: 40.0 m² total (south 15m², east 10m², west 10m², north 5m²)
       - WindowSpec: double clear glass (U=3.0, SHGC=0.789)
       - HVAC: Standard HVAC (setpoints 20°C/27°C)
       - Internal loads: Office profile (lighting 10 W/m², equipment 20 W/m², occupancy 0.05 people/m²)
       - Schedule: Office schedule (8am-6pm weekdays, off weekends)
    2. **retail_building()**:
       - Construction: Medium-mass
       - Dimensions: 25.0 × 20.0 × 4.0 m
       - Windows: 60.0 m² total (south 20m², east 15m², west 15m², north 10m²)
       - WindowSpec: double clear glass
       - HVAC: Standard HVAC (setpoints 20°C/27°C)
       - Internal loads: Retail profile (lighting 12 W/m², equipment 10 W/m², occupancy 0.1 people/m²)
       - Schedule: Retail schedule (9am-9pm weekdays, 10am-6pm weekends)
    3. **school_building()**:
       - Construction: High-mass (concrete walls)
       - Dimensions: 30.0 × 25.0 × 3.5 m
       - Windows: 50.0 m² total (south 20m², east 10m², west 10m², north 10m²)
       - WindowSpec: double clear glass
       - HVAC: Standard HVAC (setpoints 20°C/27°C)
       - Internal loads: School profile (lighting 8 W/m², equipment 15 W/m², occupancy 0.2 people/m²)
       - Schedule: School schedule (8am-3pm weekdays, off weekends)
    4. **case_195_high_mass()**:
       - Same as Case195 but use high-mass construction
       - Construction: High-mass (concrete walls with high thermal mass)
       - All other parameters: Same as Case195
    5. **case_195_no_loads()**:
       - Same as Case195 but zero internal loads
       - Lighting: 0 W/m²
       - Equipment: 0 W/m²
       - Occupancy: 0 people/m²
       - All other parameters: Same as Case195
    6. **case_195_no_solar()**:
       - Same as Case195 but zero solar gains
       - WindowSpec: SHGC = 0.0 (zero solar transmittance)
       - All other parameters: Same as Case195
    7. **case_195_thermal_bridge()**:
       - Same as Case195 but add thermal bridge conductance
       - Add extra h_tr_em (exterior to mass conductance) for thermal bridges
       - Thermal bridge conductance: 2.0 W/K (structural elements)
       - All other parameters: Same as Case195
    Follow existing CaseBuilder pattern and Phase 17 building profile integration.
  </action>
  <verify>cargo test --lib test_case_office 2>&1 | head -20</verify>
  <done>CaseBuilder methods exist for Office/Retail/School and Case195 variants, all create valid CaseSpec structs</done>
</task>
<task type="auto">
  <name>Task 3: Implement non-residential case tests</name>
  <files>tests/ashrae_140_case_non_residential.rs</files>
  <action>
    Update tests/ashrae_140_case_non_residential.rs with full implementations (replace TODO placeholders):
    1. **test_case_office_building()**:
       - Use ASHRAE140Case::Office.spec() to get case spec
       - Load office profile: load_building_profile(BuildingType::Office)
       - Apply internal loads from profile to ThermalModel
       - Simulate 1 year: model.simulate_year()
       - Assert annual heating and cooling within reasonable ranges (non-residential typically higher than residential)
       - Validate schedule: energy consumption should follow office pattern (high weekdays, low weekends)
       - Validate internal loads: lighting + equipment + occupancy heat gains present
    2. **test_case_retail_building()**:
       - Use ASHRAE140Case::Retail.spec()
       - Load retail profile: load_building_profile(BuildingType::Retail)
       - Simulate 1 year
       - Assert annual heating and cooling higher than office (larger building, extended hours)
       - Validate schedule: energy consumption follows retail pattern (high weekends, extended hours)
    3. **test_case_school_building()**:
       - Use ASHRAE140Case::School.spec()
       - Load school profile: load_building_profile(BuildingType::School)
       - Simulate 1 year
       - Assert annual heating and cooling moderate (school hours limited, thermal mass reduces swings)
       - Validate schedule: energy consumption follows school pattern (daytime only, weekends off)
    4. **test_non_residential_integration()**:
       - Run all three non-residential cases
       - Verify pass rate > 80% (at least 2/3 cases pass validation)
       - Print summary: "Non-residential: 2/3 passed (Office ✓, Retail ✓, School ✗)"
    5. **Validate building profiles**:
       - Assert ProfileBundle loads correctly (lighting, equipment, occupancy)
       - Assert equipment count matches profile (office: 52, retail: 5, school: 30)
       - Assert occupancy max_occupancy matches profile (office: 100, retail: 50, school: 200)
    Follow existing test pattern and Phase 17 profile integration.
  </action>
  <verify>cargo test --lib test_case_office 2>&1 | tail -5</verify>
  <done>All non-residential case tests (Office, Retail, School) implemented with profile loading and validation</done>
</task>
</tasks>
<verification>
Run non-residential case tests to verify implementations:
```bash
cargo test --lib test_case_office
cargo test --lib test_case_retail
cargo test --lib test_case_school
cargo test --lib test_non_residential_integration
```
All tests should pass with energy values within expected ranges and profiles loaded correctly.
</verification>
<success_criteria>
1. ASHRAE140Case enum has Office/Retail/School and Case195HighMass/NoLoads/NoSolar/ThermalBridge variants
2. All match statements updated for new cases
3. CaseBuilder methods exist for non-residential and solid conduction variants
4. Non-residential cases use building profiles from Phase 17
5. All non-residential case tests pass with energy within expected ranges
6. Profile loading validated (lighting, equipment, occupancy)
7. Schedule validation confirms non-residential occupancy patterns
</success_criteria>
<output>
After completion, create `.planning/phases/18-diagnostic-cases/18-04-SUMMARY.md`
</output>
