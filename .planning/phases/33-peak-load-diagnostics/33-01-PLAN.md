---
phase: 33-peak-load-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/case_900_peak_diagnostic.rs, scripts/compare_peak_profiles.py]
autonomous: true
requirements: [PEAK-03, PEAK-04]
must_haves:
  truths:
    - "Hourly profiles for Case 900 are exported to CSV for direct comparison"
    - "Comparison script quantifies peak load overestimation and timing shifts"
  artifacts:
    - path: "tests/case_900_peak_diagnostic.rs"
      provides: "High-resolution hourly data export"
    - path: "scripts/compare_peak_profiles.py"
      provides: "Residual analysis and visualization"
  key_links:
    - from: "tests/case_900_peak_diagnostic.rs"
      to: "case_900_peak_hourly.csv"
      via: "CSV export"
---

<objective>
Establish a high-resolution diagnostic suite for Case 900 peak load analysis.
This plan focuses on exporting internal state variables (temperatures, fluxes) and comparing them against reference programs (EnergyPlus) to identify where the overestimation occurs.
</objective>

<execution_context>
The current v0.7.0 implementation achieved annual energy compliance but failed on peak loads.
Diagnostics must reveal if the error is in the timing (phase shift) or magnitude (damping).
</execution_context>

<context>
@.planning/STATE.md
@src/sim/engine.rs
@src/validation/diagnostics.rs
@tests/case_900_cooling_diagnostic.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement high-resolution Case 900 peak diagnostic test</name>
  <files>tests/case_900_peak_diagnostic.rs</files>
  <action>
    Create a new integration test that runs Case 900 for a full year.
    Use `SimulationDiagnostics` to record hourly data.
    Ensure it records:
    - Zone temperature
    - Mass temperature (Tm)
    - Surface temperature (Ts)
    - Heating/Cooling power (Watts)
    - Solar gains (Watts)
    - Outdoor temperature
    - Ground temperature
    Export these to `case_900_peak_hourly.csv`.
    Include logic to find and report the peak heating/cooling values and their timestamps.
  </action>
  <verify>
    <automated>cargo test --test case_900_peak_diagnostic -- --nocapture</automated>
  </verify>
  <done>CSV file exists with all required columns and 8760 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Create profile comparison script</name>
  <files>scripts/compare_peak_profiles.py</files>
  <action>
    Create a Python script that:
    1. Loads `case_900_peak_hourly.csv`.
    2. Loads reference hourly data (if available in the codebase, otherwise provide a way to mock or placeholder it).
    3. Calculates peak magnitude error and peak timing shift (hours).
    4. Generates a plot (using matplotlib/plotly) comparing Fluxion vs Reference for the peak heating day (typically Jan 4) and peak cooling day (typically Jul 27).
  </action>
  <verify>
    <automated>python3 scripts/compare_peak_profiles.py --help</automated>
  </verify>
  <done>Script successfully parses CSV and outputs peak metrics.</done>
</task>

</tasks>

<verification>
Identify the exact day and hour where Fluxion deviates most from reference peak loads.
</verification>

<success_criteria>
Diagnostic suite provides a clear quantification of:
1. Peak magnitude error (kW)
2. Peak timing shift (hours)
3. Average hourly residual during peak 24-hour periods
</success_criteria>

<output>
After completion, create `.planning/phases/33-peak-load-diagnostics/33-01-SUMMARY.md`
</output>
