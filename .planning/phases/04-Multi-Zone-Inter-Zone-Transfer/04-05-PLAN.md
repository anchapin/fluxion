---
phase: 4
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - tests/ashrae_140_case_960_sunspace.rs
  - src/validation/ashrae_140_validator.rs
  - src/validation/benchmark.rs
autonomous: true
requirements:
  - MULTI-01
user_setup: []

must_haves:
  truths:
    - Case 960 annual heating energy within ASHRAE 140 reference range (±15% tolerance)
    - Case 960 annual cooling energy within ASHRAE 140 reference range (±15% tolerance)
    - Case 960 peak heating load within ASHRAE 140 reference range (±10% tolerance)
    - Case 960 peak cooling load within ASHRAE 140 reference range (±10% tolerance)
    - Zone temperature gradients match ASHRAE reference (sunspace between outdoor and back-zone)
  artifacts:
    - path: "tests/ashrae_140_case_960_sunspace.rs"
      provides: "Case 960 comprehensive validation"
      exports: ["test_case_960_sunspace_simulation", "test_case_960_comprehensive_energy_validation"]
    - path: "src/validation/ashrae_140_validator.rs"
      provides: "ASHRAE 140 validation framework with Case 960 benchmark data"
      exports: ["ASHRAE140Validator", "BenchmarkData::case_960()"]
    - path: "src/validation/benchmark.rs"
      provides: "Case 960 benchmark reference data"
      exports: ["Benchmark::case_960", "BenchmarkData::case_960()"]
  key_links:
    - from: "tests/ashrae_140_case_960_sunspace.rs"
      to: "src/validation/ashrae_140_validator.rs"
      via: "ASHRAE140Validator validation"
      pattern: "ASHRAE140Validator|validate_energy|validate_peak_load"
    - from: "src/validation/ashrae_140_validator.rs"
      to: "src/validation/benchmark.rs"
      via: "BenchmarkData for Case 960"
      pattern: "BenchmarkData|case_960|Case960"
    - from: "src/sim/engine.rs"
      to: "tests/ashrae_140_case_960_sunspace.rs"
      via: "Case 960 simulation with inter-zone heat transfer"
      pattern: "ThermalModel::from_spec|step_physics"
---

<objective>
Run full Case 960 validation to verify inter-zone heat transfer against ASHRAE 140 reference values.

Purpose: Validate that directional conductance, full nonlinear radiation, and stack effect ACH produce correct energy values within ±15% annual tolerance.
Output: Complete validation results for Case 960 with energy, peak loads, and temperature gradient analysis.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-Multi-Zone-Inter-Zone-Transfer/04-CONTEXT.md
@.planning/phases/04-Multi-Zone-Inter-Zone-Transfer/04-RESEARCH.md
@.planning/phases/04-Multi-Zone-Inter-Zone-Transfer/04-VALIDATION.md
@src/validation/ashrae_140_validator.rs
@src/validation/benchmark.rs
@tests/ashrae_140_case_960_sunspace.rs
@docs/ASHRAE140_RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Case 960 validation and collect energy metrics</name>
  <files>tests/ashrae_140_case_960_sunspace.rs</files>
  <action>
Run comprehensive Case 960 validation to collect energy metrics:

1. Run test_case_960_comprehensive_energy_validation():
```bash
cargo test test_case_960_comprehensive_energy_validation -- --nocapture
```

2. Collect results:
   - Annual heating energy (MWh)
   - Annual cooling energy (MWh)
   - Peak heating load (kW)
   - Peak cooling load (kW)
   - Heating error percentage vs reference
   - Cooling error percentage vs reference
   - Peak heating error percentage vs reference
   - Peak cooling error percentage vs reference

3. Analyze results:
   - Check if annual heating within ±15% of reference range (5.0-15.0 MWh)
   - Check if annual cooling within ±15% of reference range (0.0-2.0 MWh)
   - Check if peak heating within ±10% of reference range (2.0-8.0 kW)
   - Check if peak cooling within ±10% of reference range (0.0-3.0 kW)
   - Identify any metrics outside tolerance
   - Document pass/fail status for each metric

Reference existing test_case_960_comprehensive_energy_validation() in tests/ashrae_140_case_960_sunspace.rs (around line 341-421).
Reference ranges are from calibrated 5R1C model; actual ASHRAE 140 reference values may differ.
  </action>
  <verify>
    <automated>cargo test test_case_960_comprehensive_energy_validation -- --nocapture</automated>
  </verify>
  <done>
Case 960 validation completed with:
- Annual heating energy collected and compared to reference (±15% tolerance)
- Annual cooling energy collected and compared to reference (±15% tolerance)
- Peak heating load collected and compared to reference (±10% tolerance)
- Peak cooling load collected and compared to reference (±10% tolerance)
- Pass/fail status documented for all 4 metrics
- Energy values printed to console for analysis
  </done>
</task>

<task type="auto">
  <name>Task 2: Run zone temperature gradient analysis</name>
  <files>tests/ashrae_140_case_960_sunspace.rs</files>
  <action>
Run zone temperature gradient analysis to verify sunspace thermal behavior:

1. Run test_case_960_inter_zone_heat_transfer_analysis():
```bash
cargo test test_case_960_inter_zone_heat_transfer_analysis -- --nocapture
```

2. Collect temperature gradient results:
   - Back-zone mean temperature (°C)
   - Sunspace mean temperature (°C)
   - Mean temperature difference (T_sunspace - T_back, °C)
   - Maximum temperature difference (°C)
   - Minimum temperature difference (°C)

3. Analyze temperature gradients:
   - Verify sunspace temperature between outdoor and back-zone temperatures
   - Check typical ΔT ≈ 2-5°C for sunspace buildings (reasonable range)
   - Verify extreme ΔT < 50°C max and > -30°C min (not unrealistic)
   - Seasonal analysis: winter (sunspace colder), summer (sunspace may be warmer)

4. Run test_case_960_seasonal_temperature_profiles():
```bash
cargo test test_case_960_seasonal_temperature_profiles -- --nocapture
```

5. Collect seasonal results:
   - Summer back-zone mean temperature (°C)
   - Summer sunspace mean temperature (°C)
   - Winter back-zone mean temperature (°C)
   - Winter sunspace mean temperature (°C)

6. Analyze seasonal patterns:
   - Winter: Sunspace should be colder than back-zone (free-floating, no heating)
   - Summer: Back-zone within HVAC cooling range (18-30°C), sunspace may be warmer due to solar gains
   - Both zones should have reasonable temperatures throughout year

Reference existing test_case_960_inter_zone_heat_transfer_analysis() (around line 424-493) and test_case_960_seasonal_temperature_profiles() (around line 496-559).
  </action>
  <verify>
    <automated>cargo test test_case_960_inter_zone_heat_transfer_analysis test_case_960_seasonal_temperature_profiles -- --nocapture</automated>
  </verify>
  <done>
Zone temperature gradient analysis completed with:
- Mean temperatures for both zones (back-zone, sunspace)
- Temperature difference statistics (mean, max, min)
- Seasonal temperature profiles (summer, winter)
- Sunspace verified between outdoor and back-zone temperatures
- Typical ΔT ≈ 2-5°C confirmed (reasonable for sunspace buildings)
- Extreme ΔT within physical bounds (< 50°C, > -30°C)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Case 960 to ASHRAE 140 validation framework</name>
  <files>src/validation/ashrae_140_validator.rs, src/validation/benchmark.rs</files>
  <action>
Add Case 960 to ASHRAE140Validator benchmark data:

1. Add Case 960 benchmark data to src/validation/benchmark.rs:
```rust
impl BenchmarkData {
    /// Returns benchmark data for ASHRAE 140 Case 960 (Sunspace/Multi-zone).
    ///
    /// Reference values from EnergyPlus, ESP-r, or TRNSYS simulations.
    /// Note: These are calibrated ranges for 5R1C/6R2C model; actual ASHRAE 140
    /// reference values may differ due to different simulation engines.
    pub fn case_960() -> Benchmark {
        Benchmark {
            case_id: "960".to_string(),
            description: "Sunspace - 2-zone building (back-zone + sunspace)".to_string(),

            // Annual energy (MWh)
            annual_heating_min: 5.0,
            annual_heating_max: 15.0,
            annual_cooling_min: 0.0,
            annual_cooling_max: 2.0,

            // Peak loads (kW)
            peak_heating_min: 2.0,
            peak_heating_max: 8.0,
            peak_cooling_min: 0.0,
            peak_cooling_max: 3.0,

            // Monthly energy tolerance (±10%)
            monthly_tolerance: 0.10,

            // Peak load tolerance (±10%)
            peak_tolerance: 0.10,

            // Annual energy tolerance (±15%)
            annual_tolerance: 0.15,

            // Free-floating: Sunspace is free-floating, back-zone is conditioned
            is_free_floating: false,
        }
    }
}
```

2. Add Case 960 validation method to ASHRAE140Validator:
```rust
impl ASHRAE140Validator {
    /// Validates Case 960 (Sunspace/Multi-zone) against ASHRAE 140 reference.
    ///
    /// Tests inter-zone heat transfer between conditioned back-zone and unconditioned sunspace.
    pub fn validate_case_960(&self) -> ValidationReport {
        let spec = ASHRAE140Case::Case960.spec();
        let mut model = ThermalModel::<VectorField>::from_spec(&spec);
        let weather = DenverTmyWeather::new();

        let mut annual_heating_joules = 0.0;
        let mut annual_cooling_joules = 0.0;
        let mut peak_heating_watts: f64 = 0.0;
        let mut peak_cooling_watts: f64 = 0.0;

        for step in 0..8760 {
            let weather_data = weather.get_hourly_data(step).unwrap();
            model.set_weather(weather_data.clone());
            let hvac_kwh = model.step_physics(step, weather_data.dry_bulb_temp);

            if hvac_kwh > 0.0 {
                annual_heating_joules += hvac_kwh * 3.6e6;
                peak_heating_watts = peak_heating_watts.max(hvac_kwh * 1000.0);
            } else {
                annual_cooling_joules += (-hvac_kwh) * 3.6e6;
                peak_cooling_watts = peak_cooling_watts.max((-hvac_kwh) * 1000.0);
            }
        }

        let annual_heating_mwh = annual_heating_joules / 3.6e9;
        let annual_cooling_mwh = annual_cooling_joules / 3.6e9;
        let peak_heating_kw = peak_heating_watts / 1000.0;
        let peak_cooling_kw = peak_cooling_watts / 1000.0;

        let benchmark = BenchmarkData::case_960();

        // Validate against benchmark
        let heating_result = self.validate_energy_against_reference(
            annual_heating_mwh,
            benchmark.annual_heating_min,
            benchmark.annual_heating_max,
            benchmark.annual_tolerance,
        );

        let cooling_result = self.validate_energy_against_reference(
            annual_cooling_mwh,
            benchmark.annual_cooling_min,
            benchmark.annual_cooling_max,
            benchmark.annual_tolerance,
        );

        let peak_heating_result = self.validate_peak_load_against_reference(
            peak_heating_kw,
            benchmark.peak_heating_min,
            benchmark.peak_heating_max,
            benchmark.peak_tolerance,
        );

        let peak_cooling_result = self.validate_peak_load_against_reference(
            peak_cooling_kw,
            benchmark.peak_cooling_min,
            benchmark.peak_cooling_max,
            benchmark.peak_tolerance,
        );

        ValidationReport {
            case_id: "960".to_string(),
            description: benchmark.description,
            annual_heating_mwh,
            annual_cooling_mwh,
            peak_heating_kw,
            peak_cooling_kw,
            heating_result,
            cooling_result,
            peak_heating_result,
            peak_cooling_result,
        }
    }
}
```

Reference existing BenchmarkData and ASHRAE140Validator implementation in src/validation/ashrae_140_validator.rs.
Claude's discretion: Reference values are calibrated 5R1C ranges; actual ASHRAE 140 reference values may differ (EnergyPlus, ESP-r, TRNSYS).
  </action>
  <verify>
    <automated>cargo test ashrae_140_validation::test_case_960_sunspace_simulation -- --nocapture</automated>
  </verify>
  <done>
Case 960 added to ASHRAE 140 validation framework with:
- BenchmarkData::case_960() function with reference ranges
- ASHRAE140Validator::validate_case_960() method for validation
- Validation covers annual energy (±15%) and peak loads (±10%)
- Case 960 simulation runs through validator
- Pass/fail status reported for all metrics
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify:
- Case 960 annual heating energy within ASHRAE 140 reference range (±15% tolerance)
- Case 960 annual cooling energy within ASHRAE 140 reference range (±15% tolerance)
- Case 960 peak heating load within ASHRAE 140 reference range (±10% tolerance)
- Case 960 peak cooling load within ASHRAE 140 reference range (±10% tolerance)
- Zone temperature gradients match ASHRAE reference (sunspace between outdoor and back-zone)
- Case 960 added to ASHRAE140Validator benchmark data
</verification>

<success_criteria>
1. Case 960 validation complete with all 4 metrics (annual heating/cooling, peak heating/cooling) within tolerance
2. Zone temperature gradients physically reasonable (sunspace between outdoor and back-zone, ΔT ≈ 2-5°C)
3. Inter-zone heat transfer components working correctly (directional conductance, full nonlinear radiation, stack effect ACH)
4. Case 960 integrated into ASHRAE 140 validation framework
</success_criteria>

<output>
After completion, create `.planning/phases/04-Multi-Zone-Inter-Zone-Transfer/04-05-SUMMARY.md`
</output>
