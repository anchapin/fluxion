---
phase: 03-Solar-Radiation
plan: 01
type: execute
wave: 1
depends_on: [03-00]
files_modified:
  - src/sim/engine.rs
  - tests/ashrae_140_case_900.rs
  - tests/ashrae_140_free_floating.rs
  - tests/solar_calculation_validation.rs
autonomous: true
requirements:
  - SOLAR-01
  - SOLAR-02
  - SOLAR-03
  - SOLAR-04

must_haves:
  truths:
    - "Solar gains calculated by calculate_hourly_solar() are added to 5R1C thermal network energy balance"
    - "Beam-to-mass distribution (0.7/0.3) correctly applied to solar gains"
    - "Hourly DNI/DHI solar radiation values calculated correctly for all orientations (north, south, east, west)"
    - "Window transmittance (SHGC) and normal transmittance values applied correctly for all ASHRAE 140 cases"
    - "Solar incidence angle effects validated for all orientations using ASHRAE 140 SHGC angular dependence lookup table"
    - "Beam/diffuse decomposition validated: Perez sky model correctly separates components (SOLAR-04)"
    - "Case 900 annual cooling energy increases from 0.70 MWh to within [2.13, 3.67] MWh reference"
    - "Case 900 peak cooling load increases from 0.60 kW to within [2.10, 3.50] kW reference"
    - "Free-floating max temperature increases from 37.22°C to within [41.80, 46.40]°C reference"
  artifacts:
    - path: "src/sim/engine.rs"
      provides: "Solar gains integrated into 5R1C thermal network energy balance"
      contains: "solar_gains added to phi_i internal heat source term"
    - path: "tests/ashrae_140_case_900.rs"
      provides: "Validation tests for solar gain integration"
      exports: ["test_case_900_annual_cooling_energy", "test_case_900_peak_cooling_load"]
    - path: "tests/ashrae_140_free_floating.rs"
      provides: "Free-floating solar gain validation"
      exports: ["test_case_900ff_max_temperature"]
    - path: "tests/solar_calculation_validation.rs"
      provides: "Unit tests validating DNI/DHI calculations and window properties"
      exports: ["test_hourly_solar_irradiance_for_orientations", "test_window_shgc_normal_transmittance", "test_incidence_angle_effects", "test_beam_diffuse_decomposition"]
  key_links:
    - from: "src/sim/engine.rs::step_physics()"
      to: "src/sim/solar.rs::calculate_hourly_solar()"
      via: "solar_gains VectorField integration"
      pattern: "calculate_hourly_solar\\(.*orientation.*\\)"
    - from: "src/sim/engine.rs::step_physics()"
      to: "thermal network energy balance"
      via: "internal heat source term addition"
      pattern: "phi_i_total = phi_i_internal.clone\\(\\) \\+ phi_i_solar.clone\\(\\)"
    - from: "tests/solar_calculation_validation.rs"
      to: "src/sim/solar.rs"
      via: "direct calls to solar calculation functions"
      pattern: "calculate_hourly_solar\\(\\)"
---

<objective>
Integrate solar gain calculations into the 5R1C thermal network energy balance to fix cooling load under-prediction and annual cooling energy discrepancies.

Purpose: Research identified that solar gains are calculated correctly but never applied to the thermal network, causing 67% under-prediction of annual cooling energy and 74% under-prediction of peak cooling loads for Case 900.

Output: Solar gains properly integrated into energy balance equation, with beam-to-mass distribution (0.7 to mass, 0.3 interior surface), resulting in Case 900 validation passing cooling metrics.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-Solar-Radiation/03-RESEARCH.md
@.planning/phases/03-Solar-Radiation/03-VALIDATION.md
@.planning/phases/03-Solar-Radiation/03-00-SUMMARY.md
@.planning/phases/02-Thermal-Mass-Dynamics/02-03-SUMMARY.md
@.planning/phases/02-Thermal-Mass-Dynamics/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate solar gains into 5R1C thermal network energy balance</name>
  <files>src/sim/engine.rs</files>
  <behavior>
    - Test 1: Solar gains (phi_i_solar) calculated from self.solar_gains are non-zero during daytime hours with DNI > 0
    - Test 2: Solar gains are added to internal heat source phi_i_total = phi_i_internal + phi_i_solar
    - Test 3: Beam-to-mass distribution splits gains: phi_m_solar = solar_gains * solar_beam_to_mass_fraction, phi_st_solar = solar_gains * (1.0 - fraction)
    - Test 4: Energy balance includes solar contribution: phi_i = (phi_i_total + phi_si + phi_mi + phi_m_int_solar) * T_a + phi_m_env_solar * T_m
  </behavior>
  <action>
    Modify src/sim/engine.rs::step_physics() method to integrate solar gains into the 5R1C thermal network energy balance:

    1. Add hourly solar calculation to step_physics() by calling calculate_hourly_solar() with correct API:
       ```rust
       use crate::sim::solar::{calculate_hourly_solar, WindowProperties};

       // Inside step_physics() loop for each timestep:
       // Extract DNI and DHI from weather data (INPUT to solar calculation)
       let dni = self.weather_data.hor_irradiance_beam[hour];
       let dhi = self.weather_data.hor_irradiance_diffuse[hour];

       // Call solar calculation with weather inputs
       let (_, irradiance, solar_gain) = calculate_hourly_solar(
           self.latitude,           // latitude_deg
           self.longitude,          // longitude_deg
           year,
           month,
           day,
           hour as f64,             // hour as float
           dni,                     // Direct Normal Irradiance from weather
           dhi,                     // Diffuse Horizontal Irradiance from weather
           &self.window_properties[0], // Window properties (first zone)
           Some(&geometry),         // Window area geometry
           Some(&overhang),         // Overhang shading
           &fins,                   // Shade fins array
           self.orientation,        // Surface orientation
           Some(0.2)                // Ground reflectance (albedo)
       );

       // Store solar gains for energy balance
       self.solar_gains = VectorField::from(vec![solar_gain.total_watts]);
       ```

    2. Convert solar gains to heat flux per unit area (W/m²):
       ```rust
       let phi_i_solar = self.solar_gains.clone() / self.zone_area.clone();
       ```

    3. Apply beam-to-mass distribution (ASHRAE 140 specification):
       ```rust
       let solar_beam_to_mass_fraction = 0.7; // 70% of beam to thermal mass
       let phi_st_solar = solar_gains_watts.clone() * (1.0 - solar_beam_to_mass_fraction);
       let phi_m_solar = solar_gains_watts * solar_beam_to_mass_fraction;
       ```

    4. Split mass gains to exterior and interior surfaces:
       ```rust
       let phi_m_env_solar = phi_m_solar * 0.7; // 70% to exterior mass surface
       let phi_m_int_solar = phi_m_solar * 0.3; // 30% to interior mass surface
       ```

    5. Add solar gains to internal heat source term in energy balance:
       ```rust
       let phi_i_internal = self.internal_loads * self.zone_area.clone();
       let phi_i_solar_flux = self.solar_gains.clone() / self.zone_area.clone();
       let phi_i_total = phi_i_internal.clone() + phi_i_solar_flux.clone();

       // Energy balance with solar contribution
       let phi_i = (phi_i_total.clone() + phi_si.clone()
           + phi_mi.clone() + phi_m_int_solar.clone())
           * T_a.clone()
           + phi_m_env_solar.clone() * T_m.clone();
       ```

    6. Ensure solar gains are VectorField types to maintain CTA compatibility:
       ```rust
       use crate::physics::cta::VectorField;
       ```

    Key constraints:
    - Use calculate_hourly_solar() with 12 required parameters: latitude_deg, longitude_deg, year, month, day, hour, dni, dhi, window, geometry, overhang, fins, orientation, ground_reflectance
    - Extract dni and dhi from self.weather_data (weather_data.hor_irradiance_beam, weather_data.hor_irradiance_diffuse) - these are INPUT parameters to the solar calculation, not returned values
    - Apply ASHRAE 140 beam-to-mass distribution: 0.7 to mass, 0.3 to interior surface
    - Solar gains added to phi_i internal heat source term (similar to internal_loads)
    - Maintain VectorField type for all solar gain variables
    - Do NOT modify existing solar calculation code (solar.rs, shading.rs are correct)
  </action>
  <verify>
    <automated>cargo test solar_integration --lib</automated>
  </verify>
  <done>Solar gains integrated into 5R1C energy balance equation in step_physics(), with beam-to-mass distribution applied correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate solar gain integration with Case 900 cooling metrics</name>
  <files>tests/ashrae_140_case_900.rs</files>
  <behavior>
    - Test 1: Case 900 annual cooling energy increases to within [2.13, 3.67] MWh reference (currently 0.70 MWh)
    - Test 2: Case 900 peak cooling load increases to within [2.10, 3.50] kW reference (currently 0.60 kW)
    - Test 3: Case 900 peak heating load increases to within [1.10, 2.10] kW reference (currently 0.83 kW)
    - Test 4: Solar gain contribution is non-zero during summer months (June-August) for Case 900
  </behavior>
  <action>
    Update tests/ashrae_140_case_900.rs to validate solar gain integration:

    1. Run Case 900 full year simulation with solar gains enabled

    2. Verify annual cooling energy passes:
       ```rust
       #[test]
       fn test_case_900_annual_cooling_energy_with_solar() {
           let model = ThermalModel::from_spec(&ASHRAE140Case::Case900.spec());
           let energy_kwh = model.solve_timesteps(8760, false, false);
           let cooling_mwh = energy_kwh / 1000.0;
           assert!(cooling_mwh >= 2.13 && cooling_mwh <= 3.67,
               "Annual cooling energy {:.2} MWh not in reference range [2.13, 3.67] MWh", cooling_mwh);
       }
       ```

    3. Verify peak cooling load passes:
       ```rust
       #[test]
       fn test_case_900_peak_cooling_load_with_solar() {
           let model = ThermalModel::from_spec(&ASHRAE140Case::Case900.spec());
           // Simulate and track peak cooling load
           // Peak should be in [2.10, 3.50] kW (currently 0.60 kW without solar)
           assert!(peak_cooling_kw >= 2.10 && peak_cooling_kw <= 3.50,
               "Peak cooling load {:.2} kW not in reference range [2.10, 3.50] kW", peak_cooling_kw);
       }
       ```

    4. Verify peak heating load improves:
       ```rust
       #[test]
       fn test_case_900_peak_heating_load_with_solar() {
           // Peak should be in [1.10, 2.10] kW (currently 0.83 kW without solar)
           assert!(peak_heating_kw >= 1.10 && peak_heating_kw <= 2.10,
               "Peak heating load {:.2} kW not in reference range [1.10, 2.10] kW", peak_heating_kw);
       }
       ```

    5. Add diagnostic output for solar gain contribution:
       ```rust
       #[test]
       fn test_case_900_solar_gain_contribution() {
           let model = ThermalModel::from_spec(&ASHRAE140Case::Case900.spec());
           // Track solar gains over year
           println!("=== Solar Gain Contribution ===");
           println!("Total annual solar gains: {:.2} MWh", total_solar_mwh);
           println!("Peak solar gain: {:.2} kW", peak_solar_kw);
           println!("Summer average solar gain: {:.2} kW", summer_avg_solar_kw);
       }
       ```

    6. Update existing test assertions to reflect solar gain improvements:
       - test_case_900_annual_cooling_energy: Expect [2.13, 3.67] MWh
       - test_case_900_peak_cooling_load: Expect [2.10, 3.50] kW
       - test_case_900_peak_heating_load: Expect [1.10, 2.10] kW

    Key constraints:
    - Use existing ThermalModel::from_spec() for Case 900 setup
    - Validate against ASHRAE 140 reference ranges
    - Provide diagnostic output to track solar gain contribution
    - Maintain existing test structure (just update expected values)
  </action>
  <verify>
    <automated>cargo test --test ashrae_140_case_900 -- --nocapture</automated>
  </verify>
  <done>Case 900 cooling metrics validated with solar gains: annual cooling [2.13, 3.67] MWh, peak cooling [2.10, 3.50] kW, peak heating [1.10, 2.10] kW</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate solar gains with free-floating temperature tests</name>
  <files>tests/ashrae_140_free_floating.rs</files>
  <behavior>
    - Test 1: Case 900FF max temperature increases to within [41.80, 46.40]°C reference (currently 37.22°C)
    - Test 2: Solar gains contribute to temperature rise during peak solar hours (12-3 PM)
    - Test 3: Temperature swing remains realistic (mass damping effect still present)
    - Test 4: All free-floating tests (10/10) continue to pass with solar gains
  </behavior>
  <action>
    Update tests/ashrae_140_free_floating.rs to validate solar gain effects on free-floating temperatures:

    1. Update Case 900FF max temperature test:
       ```rust
       #[test]
       fn test_case_900ff_max_temperature_with_solar() {
           let spec = ASHRAE140Case::Case900FF.spec();
           let mut model = ThermalModel::from_spec(&spec);
           let temperatures = simulate_free_float_with_time_series(&mut model, &spec);

           let max_temp = temperatures.iter().cloned().fold(f64::NEG_INFINITY, f64::max);
           assert!(max_temp >= 41.80 && max_temp <= 46.40,
               "Max temperature {:.2}°C not in reference range [41.80, 46.40]°C", max_temp);
       }
       ```

    2. Add test for solar gain timing:
       ```rust
       #[test]
       fn test_solar_gain_timing_effect() {
           let spec = ASHRAE140Case::Case900FF.spec();
           let mut model = ThermalModel::from_spec(&spec);
           let temperatures = simulate_free_float_with_time_series(&mut model, &spec);

           // Solar gains should peak 1-2 hours after solar noon (2-4 PM)
           // Max temp should occur in late afternoon (3-5 PM)
           let max_temp_hour = temperatures.iter().enumerate()
               .max_by(|a, b| a.1.partial_cmp(b.1).unwrap())
               .unwrap().0;

           let hour_of_day = max_temp_hour % 24;
           assert!(hour_of_day >= 14 && hour_of_day <= 17,
               "Max temp at hour {} not in expected afternoon range [14, 17]", hour_of_day);
       }
       ```

    3. Verify thermal mass damping still present:
       ```rust
       #[test]
       fn test_thermal_mass_damping_with_solar() {
           // Compare 600FF (low mass) vs 900FF (high mass)
           let low_mass_swing = calculate_temperature_swing(&case_600ff_temperatures);
           let high_mass_swing = calculate_temperature_swing(&case_900ff_temperatures);

           let swing_reduction = (low_mass_swing - high_mass_swing) / low_mass_swing * 100.0;
           assert!(swing_reduction >= 15.0 && swing_reduction <= 30.0,
               "Swing reduction {:.1}% not in expected range [15, 30]%", swing_reduction);
       }
       ```

    4. Ensure all existing free-floating tests still pass:
       - Case 600FF free-floating
       - Case 650FF night ventilation
       - Case 900FF free-floating (updated max temp assertion)
       - Case 950FF night ventilation
       - Thermal mass effect on swing (should still show damping)
       - Night ventilation effect (should still reduce temps)
       - All other free-floating tests (7 remaining)

    5. Add diagnostic output for solar gain timing:
       ```rust
       #[test]
       fn test_solar_gain_timing_diagnostics() {
           println!("=== Solar Gain Timing ===");
           println!("Max temp occurs at hour {} ({}:00)", max_temp_hour, hour_of_day);
           println!("Max temperature: {:.2}°C", max_temp);
           println!("Solar gain contribution at peak: {:.2} kW", peak_solar_kw);
       }
       ```

    Key constraints:
    - Update max temperature assertion to [41.80, 46.40]°C (from 37.22°C without solar)
    - Verify thermal mass damping still works (swing reduction ~15-30%)
    - Ensure solar gains peak at correct time (afternoon, 2-4 PM)
    - Maintain all existing free-floating test passes (10/10)
  </action>
  <verify>
    <automated>cargo test --test ashrae_140_free_floating -- --nocapture</automated>
  </verify>
  <done>Case 900FF max temperature within [41.80, 46.40]°C, thermal mass damping still present, all 10 free-floating tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Validate hourly surface irradiance calculations for all orientations (SOLAR-01)</name>
  <files>tests/solar_calculation_validation.rs</files>
  <behavior>
    - Test 1: Surface irradiance components (beam_wm2, diffuse_wm2, ground_reflected_wm2) calculated correctly for each orientation (north, south, east, west)
    - Test 2: Beam irradiance > 0 for south-facing surface at solar noon (physical behavior validation)
    - Test 3: Beam irradiance exceeds diffuse irradiance at solar noon for south-facing surface
    - Test 4: Ground-reflected irradiance calculated with correct formula: GHI * albedo * (1-cos(tilt))/2
    - Test 5: Diffuse irradiance within ±20% of expected clear-sky values (100-200 W/m² at noon)
  </behavior>
  <action>
    Update tests/solar_calculation_validation.rs to validate hourly surface irradiance calculations using correct API:

    1. Create unit tests for hourly surface irradiance per orientation:
       ```rust
       use fluxion::sim::solar::{calculate_hourly_solar, calculate_surface_irradiance};
       use fluxion::sim::solar::WindowProperties;

       #[test]
       fn test_hourly_solar_irradiance_for_orientations() {
           let orientations = [Orientation::North, Orientation::South, Orientation::East, Orientation::West];
           let window = WindowProperties::double_clear(12.0);

           // Test with known weather data (DNI=900, DHI=150 for clear sky at noon)
           // DNI and DHI are INPUT parameters extracted from weather data
           for &orientation in orientations {
               let (_sun_pos, irradiance, _solar_gain) = calculate_hourly_solar(
                   39.7392, -104.9903, 2023, 7, 21, 12.0,
                   900.0, 150.0, &window, None, None, &[], orientation, Some(0.2)
               );

               // Validate irradiance components are calculated correctly from DNI/DHI inputs
               assert!(irradiance.beam_wm2 >= 0.0, "Beam irradiance should be non-negative for {}", orientation);
               assert!(irradiance.diffuse_wm2 > 0.0, "Diffuse irradiance should be positive for {}", orientation);
               assert!(irradiance.ground_reflected_wm2 >= 0.0, "Ground reflected irradiance should be non-negative for {}", orientation);

               // At solar noon, beam should be positive for south-facing surface
               if orientation == Orientation::South {
                   assert!(irradiance.beam_wm2 > 0.0,
                       "Beam irradiance should be positive for south-facing surface at solar noon");

                   // Beam should exceed diffuse at solar noon for clear sky
                   assert!(irradiance.beam_wm2 > irradiance.diffuse_wm2,
                       "Beam {:.2} W/m² should exceed diffuse {:.2} W/m² at solar noon",
                       irradiance.beam_wm2, irradiance.diffuse_wm2);

                   // Diffuse should be within ±20% of expected clear-sky values (100-200 W/m²)
                   assert!(irradiance.diffuse_wm2 >= 80.0 && irradiance.diffuse_wm2 <= 240.0,
                       "Diffuse {:.2} W/m² should be within ±20% of clear-sky range [80, 240] W/m²",
                       irradiance.diffuse_wm2);
               }
           }
       }
       ```

    2. Validate DNI/DHI calculation methods exist in solar.rs:
       ```rust
       #[test]
       fn test_solar_calculation_methods_exist() {
           // Verify that the solar module has the required calculation methods
           // This test ensures the integration points are available
           use fluxion::sim::solar::{calculate_hourly_solar, calculate_surface_irradiance};
           // Test compiles if methods exist
           assert!(true);
       }
       ```

    3. Add diagnostic output for irradiance values:
       ```rust
       #[test]
       fn test_irradiance_diagnostics() {
           let window = WindowProperties::double_clear(12.0);

           println!("=== Hourly Surface Irradiance (South Orientation) ===");
           for hour in 0..24 {
               let (_sun_pos, irradiance, _solar_gain) = calculate_hourly_solar(
                   39.7392, -104.9903, 2023, 7, 15, hour as f64,
                   900.0, 150.0, &window, None, None, &[], Orientation::South, Some(0.2)
               );
               println!("Hour {}: Beam={:.2} W/m², Diffuse={:.2} W/m², Ground={:.2} W/m²",
                   hour, irradiance.beam_wm2, irradiance.diffuse_wm2, irradiance.ground_reflected_wm2);
           }
       }
       ```

    Key constraints:
    - Use correct API: calculate_hourly_solar() returns (SolarPosition, SurfaceIrradiance, SolarGain) tuple
    - DNI and DHI are INPUT parameters to calculate_hourly_solar() (extracted from self.weather_data), not returned values
    - Extract SurfaceIrradiance from tuple and verify beam_wm2, diffuse_wm2, ground_reflected_wm2 (not DNI/DHI themselves)
    - Validate all four orientations: north, south, east, west
    - Verify physical behavior with specific acceptance criteria: beam > 0 at noon, beam exceeds diffuse, diffuse within ±20% of clear-sky
    - Test methods must exist in solar.rs module (check API availability)
    - REQUIRES 03-00 Task 2 to create tests/solar_calculation_validation.rs first
  </action>
  <verify>
    <automated>cargo test --test solar_calculation_validation -- --nocapture</automated>
  </verify>
  <done>Hourly surface irradiance calculations validated for all orientations, validating beam/diffuse/ground-reflected components derived from DNI/DHI inputs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Validate window SHGC and normal transmittance values (SOLAR-03)</name>
  <files>tests/solar_calculation_validation.rs</files>
  <behavior>
    - Test 1: Window SHGC (Solar Heat Gain Coefficient) values match ASHRAE 140 case specifications
    - Test 2: Window normal transmittance values match ASHRAE 140 case specifications
    - Test 3: SHGC and normal transmittance applied correctly in solar gain calculations
    - Test 4: All ASHRAE 140 cases (600, 610, 620, 630, 640, 650) have correct window properties
  </behavior>
  <action>
    Add tests to tests/solar_calculation_validation.rs to validate window SHGC and normal transmittance values using correct API:

    1. Create unit tests for SHGC values per ASHRAE 140 case:
       ```rust
       #[test]
       fn test_window_shgc_ashrae_140_cases() {
           let case_specs = [
               (ASHRAE140Case::Case600, 0.789, 0.799),
               (ASHRAE140Case::Case610, 0.789, 0.799),
               (ASHRAE140Case::Case620, 0.789, 0.799),
               (ASHRAE140Case::Case630, 0.789, 0.799),
               (ASHRAE140Case::Case640, 0.789, 0.799),
               (ASHRAE140Case::Case650, 0.789, 0.799),
           ];

           for (case, expected_shgc, expected_trans) in case_specs.iter() {
               let spec = case.spec();
               let model = ThermalModel::from_spec(&spec);

               // Access window_properties vector (not direct fields)
               let actual_shgc = model.window_properties[0].shgc;
               let actual_trans = model.window_properties[0].normal_transmittance;

               // Use ±0.005 tolerance for SHGC as specified in CONTEXT.md
               assert!((actual_shgc - expected_shgc).abs() < 0.005,
                   "{} SHGC {:.3} should be {:.3} (±0.005 tolerance)", case, actual_shgc, expected_shgc);

               // Use ±0.005 tolerance for normal transmittance
               assert!((actual_trans - expected_trans).abs() < 0.005,
                   "{} transmittance {:.3} should be {:.3} (±0.005 tolerance)", case, actual_trans, expected_trans);
           }
       }
       ```

    2. Validate SHGC and transmittance are applied in solar gain calculations:
       ```rust
       #[test]
       fn test_shgc_applied_in_solar_gains() {
           // Compare Case 600 (no shading) vs Case 610 (with overhang)
           // Both have same window properties, but 610 should have lower peak gain
           let spec_600 = ASHRAE140Case::Case600.spec();
           let spec_610 = ASHRAE140Case::Case610.spec();

           let model_600 = ThermalModel::from_spec(&spec_600);
           let model_610 = ThermalModel::from_spec(&spec_610);

           // Verify window properties are identical
           let window_props_600 = model_600.window_properties[0];
           let window_props_610 = model_610.window_properties[0];

           assert_eq!(window_props_600.shgc, window_props_610.shgc, "SHGC should be same");
           assert_eq!(window_props_600.normal_transmittance, window_props_610.normal_transmittance,
               "Transmittance should be same");

           // Shading effect verified by comparing summer peak gains
           // (This tests that calculate_window_solar_gain() applies overhang correctly)
       }
       ```

    3. Verify window property fields exist in ThermalModel:
       ```rust
       #[test]
       fn test_window_properties_exist_in_model() {
           let model = ThermalModel::from_spec(&ASHRAE140Case::Case900.spec());

           // Verify window_properties vector is accessible
           let _shgc = model.window_properties[0].shgc;
           let _transmittance = model.window_properties[0].normal_transmittance;

           // Verify values are in reasonable ranges
           assert!(model.window_properties[0].shgc > 0.0 && model.window_properties[0].shgc <= 1.0,
               "SHGC {} should be in range (0, 1]", model.window_properties[0].shgc);
           assert!(model.window_properties[0].normal_transmittance > 0.0 && model.window_properties[0].normal_transmittance <= 1.0,
               "Normal transmittance {} should be in range (0, 1]", model.window_properties[0].normal_transmittance);
       }
       ```

    4. Add diagnostic output for window properties:
       ```rust
       #[test]
       fn test_window_properties_diagnostics() {
           println!("=== Window Properties (ASHRAE 140 Cases) ===");
           for case in [ASHRAE140Case::Case600, ASHRAE140Case::Case610, ASHRAE140Case::Case620,
                        ASHRAE140Case::Case630, ASHRAE140Case::Case640, ASHRAE140Case::Case650] {
               let spec = case.spec();
               let model = ThermalModel::from_spec(&spec);
               println!("{}: SHGC={:.3}, Normal Transmittance={:.3}",
                   case, model.window_properties[0].shgc, model.window_properties[0].normal_transmittance);
           }
       }
       ```

    Key constraints:
    - Validate all ASHRAE 140 cases (600-650) for window properties
    - Access ThermalModel window properties via window_properties[0].shgc and window_properties[0].normal_transmittance (not direct fields)
    - Verify SHGC and transmittance are applied in solar gain calculations
    - Compare against ASHRAE 140 specification values from research phase (SHGC=0.789 with ±0.005 tolerance per CONTEXT.md line 90)
    - Values should be in range (0, 1] for SHGC and transmittance
    - REQUIRES 03-00 Task 2 to create tests/solar_calculation_validation.rs first
  </action>
  <verify>
    <automated>cargo test --test solar_calculation_validation -- --nocapture</automated>
  </verify>
  <done>Window SHGC and normal transmittance values validated for all ASHRAE 140 cases against specifications using correct API</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Validate solar incidence angle effects for all orientations (SOLAR-02)</name>
  <files>tests/solar_calculation_validation.rs</files>
  <behavior>
    - Test 1: ASHRAE 140 SHGC angular dependence lookup table applied correctly for all orientations
    - Test 2: Incidence angle calculations produce correct values for north, south, east, west surfaces
    - Test 3: Beam radiation varies correctly with sun position across orientations
    - Test 4: SHGC ratio (angular-dependent / normal) decreases as incidence angle increases
  </behavior>
  <action>
    Add tests to tests/solar_calculation_validation.rs to validate solar incidence angle effects using ASHRAE 140 SHGC lookup table:

    1. Create unit tests for incidence angle calculations:
       ```rust
       use fluxion::sim::solar::{calculate_solar_position, calculate_hourly_solar, ashrae_140_window_shgc_ratio};
       use fluxion::sim::solar::Orientation;

       #[test]
       fn test_incidence_angle_effects_for_orientations() {
           let orientations = [Orientation::North, Orientation::South, Orientation::East, Orientation::West];
           let window = WindowProperties::double_clear(12.0);

           // Test at summer solstice noon (max solar elevation)
           for &orientation in orientations {
               let sun_pos = calculate_solar_position(39.7392, -104.9903, 2023, 7, 21, 12.0);

               // Calculate incidence cosine for each orientation
               let incidence_cos = sun_pos.incidence_cosine(90.0, orientation.to_azimuth());

               // Validate incidence angle is in valid range [0, 90] degrees
               let incidence_angle = incidence_cos.acos().to_degrees();
               assert!(incidence_angle >= 0.0 && incidence_angle <= 90.0,
                   "Incidence angle {:.2}° should be in [0, 90] for {}", incidence_angle, orientation);

               // At noon, south-facing surface should have smallest incidence angle (most direct)
               if orientation == Orientation::South {
                   assert!(incidence_angle < 45.0,
                       "South-facing surface incidence angle {:.2}° should be < 45° at noon", incidence_angle);
               }

               // Validate SHGC ratio lookup
               let shgc_ratio = ashrae_140_window_shgc_ratio(incidence_angle);
               assert!(shgc_ratio > 0.0 && shgc_ratio <= 1.0,
                   "SHGC ratio {:.3} should be in (0, 1] for {}", shgc_ratio, orientation);

               // SHGC ratio decreases as incidence angle increases
               if incidence_angle > 30.0 {
                   assert!(shgc_ratio < 1.0,
                       "SHGC ratio {:.3} should be < 1.0 at {:.2}° incidence", shgc_ratio, incidence_angle);
               }
           }
       }
       ```

    2. Validate ASHRAE 140 SHGC angular dependence behavior:
       ```rust
       #[test]
       fn test_shgc_angular_dependence_curve() {
           // Test SHGC ratio at various incidence angles
           let incidence_angles = [0.0, 15.0, 30.0, 45.0, 60.0, 75.0, 85.0];

           println!("=== ASHRAE 140 SHGC Angular Dependence ===");
           let mut previous_ratio = 1.0;
           for angle in incidence_angles.iter() {
               let ratio = ashrae_140_window_shgc_ratio(*angle);
               println!("Incidence {:3.0}°: SHGC ratio = {:.3}", angle, ratio);

               // SHGC ratio should decrease monotonically with incidence angle
               assert!(ratio <= previous_ratio,
                   "SHGC ratio {:.3} at {:.0}° should be <= {:.3} at previous angle",
                   ratio, angle, previous_ratio);
               previous_ratio = ratio;

               // At normal incidence (0°), ratio should be 1.0
               if *angle == 0.0 {
                   assert!((ratio - 1.0).abs() < 0.001,
                       "SHGC ratio at normal incidence should be 1.0, got {:.3}", ratio);
               }
           }
       }
       ```

    3. Validate beam radiation variation with sun position:
       ```rust
       #[test]
       fn test_beam_radiation_variation_with_sun_position() {
           let orientations = [Orientation::South, Orientation::East, Orientation::West];
           let window = WindowProperties::double_clear(12.0);

           // Track beam irradiance over daytime hours
           for &orientation in orientations {
               let mut max_beam = 0.0;
               let mut max_beam_hour = 0.0;

               for hour in 6..18 { // Daytime hours
                   let (_sun_pos, irradiance, _solar_gain) = calculate_hourly_solar(
                       39.7392, -104.9903, 2023, 7, 21, hour as f64,
                       900.0, 150.0, &window, None, None, &[], orientation, Some(0.2)
                   );

                   if irradiance.beam_wm2 > max_beam {
                       max_beam = irradiance.beam_wm2;
                       max_beam_hour = hour as f64;
                   }
               }

               // Validate beam irradiance peaks at reasonable time
               if orientation == Orientation::South {
                   assert!(max_beam_hour >= 11.0 && max_beam_hour <= 14.0,
                       "South-facing beam should peak near noon, got hour {:.0}", max_beam_hour);
               } else if orientation == Orientation::East {
                   assert!(max_beam_hour >= 8.0 && max_beam_hour <= 11.0,
                       "East-facing beam should peak in morning, got hour {:.0}", max_beam_hour);
               } else if orientation == Orientation::West {
                   assert!(max_beam_hour >= 13.0 && max_beam_hour <= 16.0,
                       "West-facing beam should peak in afternoon, got hour {:.0}", max_beam_hour);
               }

               println!("{}: Max beam irradiance {:.2} W/m² at {:.0} hours",
                   orientation, max_beam, max_beam_hour);
           }
       }
       ```

    4. Validate incidence angle calculations are integrated into solar gains:
       ```rust
       #[test]
       fn test_incidence_angle_integrated_in_solar_gains() {
           let window = WindowProperties::double_clear(12.0);

           // Compare solar gains at different incidence angles
           // High angle (sun near horizon) vs low angle (sun near zenith)
           let (_, irradiance_zenith, gain_zenith) = calculate_hourly_solar(
               39.7392, -104.9903, 2023, 7, 21, 12.0, // Noon (sun near zenith)
               900.0, 150.0, &window, None, None, &[], Orientation::South, Some(0.2)
           );

           let (_, irradiance_horizon, gain_horizon) = calculate_hourly_solar(
               39.7392, -104.9903, 2023, 7, 21, 7.0, // Morning (sun near horizon)
               900.0, 150.0, &window, None, None, &[], Orientation::South, Some(0.2)
           );

           // Beam irradiance should be higher when sun is near zenith
           assert!(irradiance_zenith.beam_wm2 > irradiance_horizon.beam_wm2,
               "Beam irradiance {:.2} at zenith should exceed {:.2} at horizon",
               irradiance_zenith.beam_wm2, irradiance_horizon.beam_wm2);

           // Solar gains should follow beam irradiance pattern
           assert!(gain_zenith.total_watts > gain_horizon.total_watts,
               "Solar gain {:.2} W at zenith should exceed {:.2} W at horizon",
               gain_zenith.total_watts, gain_horizon.total_watts);
       }
       ```

    Key constraints:
    - Validate ASHRAE 140 SHGC angular dependence lookup table is called correctly
    - Test incidence angle calculations for all orientations: north, south, east, west
    - Verify SHGC ratio decreases monotonically with incidence angle
    - Verify beam radiation varies correctly with sun position (peaks at appropriate times per orientation)
    - Validate incidence effects are integrated into solar gain calculations
    - REQUIRES 03-00 Task 2 to create tests/solar_calculation_validation.rs first
  </action>
  <verify>
    <automated>cargo test --test solar_calculation_validation -- --nocapture</automated>
  </verify>
  <done>Solar incidence angle effects validated for all orientations using ASHRAE 140 SHGC angular dependence lookup table</done>
</task>

</tasks>

<verification>
After all tasks complete, run full ASHRAE 140 validation suite to verify solar gain integration:

```bash
fluxion validate --all
```

Expected results:
- Case 900 annual cooling: [2.13, 3.67] MWh (from 0.70 MWh baseline)
- Case 900 peak cooling: [2.10, 3.50] kW (from 0.60 kW baseline)
- Case 900 peak heating: [1.10, 2.10] kW (from 0.83 kW baseline)
- Case 900FF max temp: [41.80, 46.40]°C (from 37.22°C baseline)
- All free-floating tests: 10/10 passing
- Case 900 overall: 6/8 tests passing (was 4/8)

All solar requirements (SOLAR-01 through SOLAR-04) validated:
- SOLAR-01: Hourly DNI/DHI solar radiation calculated for all orientations (validated in Task 4)
- SOLAR-02: Solar incidence angle effects modeled for all orientations (validated in Task 6)
- SOLAR-03: Window transmittance (SHGC) and normal transmittance applied correctly (validated in Task 5)
- SOLAR-04: Solar radiation modeling supports beam/diffuse decomposition (validated by existing Perez sky model in src/sim/solar.rs per RESEARCH.md line 22, and Task 4 validates that beam/diffuse components are correctly calculated and separated from weather inputs)
</verification>

<success_criteria>
1. Solar gains integrated into 5R1C thermal network energy balance in step_physics()
2. Beam-to-mass distribution (0.7/0.3) correctly applied to solar gains
3. Hourly DNI/DHI calculations validated for all orientations (north, south, east, west) against ASHRAE 140 references
4. Solar incidence angle effects validated for all orientations using ASHRAE 140 SHGC angular dependence lookup table
5. Window SHGC and normal transmittance values validated for all ASHRAE 140 cases (600-650)
6. Case 900 annual cooling energy within [2.13, 3.67] MWh reference
7. Case 900 peak cooling load within [2.10, 3.50] kW reference
8. Case 900 peak heating load within [1.10, 2.10] kW reference
9. Case 900FF max temperature within [41.80, 46.40]°C reference
10. All 10 free-floating tests passing
11. Case 900 validation: 6/8 tests passing (up from 4/8)
12. MAE reduction: from 49.21% to <35% (solar integration addresses major failure mode)
</success_criteria>

<output>
After completion, create `.planning/phases/03-Solar-Radiation/03-01-SUMMARY.md` with:
- Solar gain integration details (phi_i_solar term, beam-to-mass distribution)
- DNI/DHI validation results (all orientations)
- Incidence angle validation results (ASHRAE 140 SHGC lookup table)
- Window property validation results (SHGC, normal transmittance)
- Validation results (Case 900 cooling metrics, free-floating max temp)
- MAE improvement metrics
- Requirements SOLAR-01 through SOLAR-04 completion status
</output>
