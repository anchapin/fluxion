---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Cargo.toml]
autonomous: true
requirements: [TEST-01, TEST-02]
user_setup:
  - service: cargo-tarpaulin
    why: "Code coverage measurement for >80% target"
    installation: "cargo install cargo-tarpaulin"
  - service: proptest
    why: "Property-based testing framework for thermal invariants"
    env_vars: []

must_haves:
  truths:
    - "Coverage can be measured and reported for library code"
    - "Property-based test framework is available and configured"
    - "Coverage baseline is established before improvements"
  artifacts:
    - path: "Cargo.toml"
      provides: "proptest dependency"
      contains: "proptest in dev-dependencies"
    - path: "coverage/baseline.xml"
      provides: "Initial coverage report"
      min_lines: 10
  key_links:
    - from: "tests/properties/thermal_invariants.rs"
      to: "proptest"
      via: "use proptest::prelude::*"
      pattern: "use proptest"
    - from: "cargo tarpaulin"
      to: "coverage/baseline.xml"
      via: "Coverage measurement"
      pattern: "cargo tarpaulin.*xml"
---

<objective>
Install testing infrastructure (tarpaulin, proptest) and establish baseline coverage

Purpose: Enable coverage measurement and property-based testing infrastructure before adding new tests
Output: Updated Cargo.toml with proptest, baseline coverage report
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-Quality-Testing/10-RESEARCH.md
@.planning/phases/10-Quality-Testing/10-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install testing infrastructure and establish coverage baseline</name>
  <files>Cargo.toml, coverage/baseline.xml</files>
  <action>
    1. Add proptest 1.0+ to Cargo.toml [dev-dependencies] section:
       ```toml
       proptest = "1.0"
       ```

    2. Run cargo build to verify dependency resolves

    3. Create coverage directory if not exists:
       ```bash
       mkdir -p coverage
       ```

    4. Run baseline coverage measurement (without tarpaulin installed, use cargo test --lib first):
       - If tarpaulin is available: `cargo tarpaulin --lib --out xml --output-dir coverage/baseline.xml`
       - If tarpaulin not available: Run `cargo test --lib` to establish baseline test passing status
       - Document current coverage percentage in summary

    5. Verify proptest is available by running a simple test:
       ```bash
       cargo test --lib -- --list | grep -i proptest || echo "Proptest tests will be added in subsequent tasks"
       ```

    Rationale: Installing proptest first enables property-based testing infrastructure. Establishing baseline coverage provides a measurement point for TEST-01 (>80% target).
  </action>
  <verify>
    <automated>cargo build --quiet && cargo test --lib --quiet</automated>
  </verify>
  <done>proptest added to Cargo.toml, tests compile and pass, baseline coverage established</done>
</task>

</tasks>

<verification>
Coverage infrastructure installed and baseline established
</verification>

<success_criteria>
- proptest dependency added to Cargo.toml
- All existing tests still pass (cargo test --lib)
- Baseline coverage measured and documented
</success_criteria>

<output>
After completion, create `.planning/phases/10-Quality-Testing/10-01-SUMMARY.md`
</output>
