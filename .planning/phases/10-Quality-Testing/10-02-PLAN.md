---
phase: 10
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified: [tests/properties/thermal_invariants.rs]
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Thermal energy conservation holds across random parameter inputs"
    - "Temperature bounds are respected in all scenarios"
    - "Conductance calculations are consistent with physical constraints"
  artifacts:
    - path: "tests/properties/thermal_invariants.rs"
      provides: "Property-based tests for thermal invariants"
      min_lines: 80
  key_links:
    - from: "tests/properties/thermal_invariants.rs"
      to: "src/sim/engine.rs"
      via: "ThermalModel methods"
      pattern: "use fluxion::sim::engine::ThermalModel"
    - from: "tests/properties/thermal_invariants.rs"
      to: "src/physics/cta.rs"
      via: "VectorField operations"
      pattern: "use fluxion::physics::cta::VectorField"
---

<objective>
Add property-based tests for thermal network invariants

Purpose: Use proptest to verify energy conservation, temperature bounds, and conductance consistency across random inputs
Output: Property-based test file with coverage of thermal invariants
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-Quality-Testing/10-RESEARCH.md
@.planning/phases/10-Quality-Testing/10-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md

# Key types from ThermalModel
From src/sim/engine.rs:
```rust
pub struct ThermalModel<T: ContinuousTensor> {
    pub num_zones: usize,
    pub temperatures: T,
    pub mass_temperatures: T,
    pub loads: T,
    pub window_u_value: f64,
    pub hvac_setpoint: f64,
    pub h_tr_em: T,
    pub h_tr_ms: T,
    pub h_tr_is: T,
    pub h_tr_w: T,
    pub h_ve: T,
}
```

From .planning/codebase/TESTING.md:
```rust
// Floating-point comparison pattern
const EPSILON: f64 = 1e-9;
assert!((actual - expected).abs() < EPSILON);
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write property-based tests for thermal invariants</name>
  <files>tests/properties/thermal_invariants.rs</files>
  <behavior>
    Property 1: Energy conservation across parameter changes
    - Generate random parameter vectors (window_u_value: 0.1-5.0, hvac_setpoint: 15-30)
    - Apply parameters to ThermalModel
    - Run single timestep
    - Verify energy balance: energy_before + load = energy_after (within epsilon 1e-6)
    - Input space: All valid parameter combinations

    Property 2: Temperature bounds remain physical
    - Generate random zone counts (1-100)
    - Generate random initial temperatures (-50 to 100°C)
    - Generate random loads (-1000 to 1000 W)
    - Run physics step
    - Verify all temperatures stay within [-273.15, 5000] K
    - Input space: All reasonable zone counts and loads

    Property 3: Conductance consistency
    - Generate random window U-values (0.1-5.0)
    - Calculate expected h_tr_w conductance
    - Verify h_tr_em scales appropriately with U-value
    - Verify conductance symmetry (h_tr_is ≈ h_tr_si)
    - Input space: Valid U-value range

    Property 4: Vector field operations preserve size
    - Generate random zone counts (1-1000)
    - Create VectorFields for temperatures and loads
    - Apply VectorField operations (+, -, *, /)
    - Verify result vectors maintain correct length
    - Input space: All zone counts
  </behavior>
  <action>
    1. Create tests/properties directory if not exists

    2. Create tests/properties/thermal_invariants.rs with proptest tests implementing the behaviors above:
       - Use proptest strategies: 0.1..5.0_f64, 15.0..30.0_f64, 1..1000usize
       - For each property: define strategy, setup model, run operation, assert invariant
       - Use approx crate for floating-point comparisons with appropriate tolerances
       - Add module declaration to tests/properties/mod.rs if needed

    3. Follow TDD workflow:
       - RED: Write tests first, run to confirm they fail (should fail without implementation)
       - GREEN: Implement minimal code to pass tests
       - REFACTOR: Clean up if needed

    Rationale: Property-based testing with proptest covers the full input space rather than hardcoded examples, catching edge cases that unit tests miss.
  </action>
  <verify>
    <automated>cargo test --test thermal_invariants</automated>
  </verify>
  <done>Property-based tests for thermal invariants implemented and passing with proptest default configuration (100 runs per test)</done>
</task>

</tasks>

<verification>
Property-based tests pass with proptest standard configuration (100 runs per test)
</verification>

<success_criteria>
- tests/properties/thermal_invariants.rs created with 4+ property tests
- All property tests pass with proptest default settings
- Tests use proptest strategies for comprehensive input coverage
- Floating-point comparisons use appropriate epsilon tolerances
</success_criteria>

<output>
After completion, create `.planning/phases/10-Quality-Testing/10-02-SUMMARY.md`
</output>
