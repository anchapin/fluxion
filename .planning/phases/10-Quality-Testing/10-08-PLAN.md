---
phase: 10
plan: 08
type: execute
wave: 3
depends_on: [10-01, 10-04, 10-07]
files_modified:
  - src/validation/ashrae_140_validator.rs
  - src/sim/thermal_model.rs
  - src/ai/surrogate.rs
  - tests/test_coverage_enhancement.rs
  - coverage/final.xml
  - coverage/cobertura.xml
  - docs/PHASE10_TEST_COVERAGE.md
autonomous: true
requirements:
  - TEST-01
gap_closure: true

must_haves:
  truths:
    - "Unit test coverage increases from 56.79% toward 80% target"
    - "Coverage is re-measured after adding 49 new tests"
    - "Coverage report reflects actual improvement from gap closure tests"
  artifacts:
    - path: "tests/test_coverage_enhancement.rs"
      provides: "49 comprehensive tests (896 lines) covering validator, thermal model, surrogate manager"
      status: "created_passing"
    - path: "coverage/final.xml"
      provides: "Updated coverage report with new measurement after 49 tests"
      must_be: "timestamp after 2026-03-13"
    - path: "coverage/cobertura.xml"
      provides: "Updated cobertura report with new measurement"
      must_be: "timestamp after 2026-03-13"
    - path: "docs/PHASE10_TEST_COVERAGE.md"
      provides: "Updated coverage documentation with new metrics"
      min_lines: 1
  key_links:
    - from: "tests/test_coverage_enhancement.rs"
      to: "coverage/final.xml"
      via: "cargo tarpaulin coverage measurement"
      pattern: "cargo tarpaulin --lib --out xml"
    - from: "coverage/final.xml"
      to: ".planning/REQUIREMENTS.md"
      via: "Coverage progress verification"
      pattern: "TEST-01 coverage improvement"
---

<objective>
Re-run test coverage measurement after adding 49 new tests in Plan 10-08 to verify actual coverage improvement toward the 80% target.

Purpose: Close the primary verification gap for TEST-01 where coverage was measured at 56.79% BEFORE the 49 new tests were added. The tests exist and pass, but coverage needs to be re-measured to verify the actual improvement.
Output: Updated coverage reports (final.xml, cobertura.xml) with new measurement timestamp, and updated PHASE10_TEST_COVERAGE.md documentation.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-Quality-Testing/10-VERIFICATION.md
@.planning/phases/10-Quality-Testing/10-07-SUMMARY.md
@.planning/phases/10-Quality-Testing/10-08-SUMMARY.md
@docs/PHASE10_TEST_COVERAGE.md

# Gap Context from VERIFICATION.md
Gap 1: TEST-01 Coverage Not Re-Measured
- Status: PARTIAL
- 49 new tests (896 lines) were added in Plan 10-08 covering:
  - ASHRAE 140 validator orchestrator (18 tests)
  - Thermal model builder patterns (16 tests)
  - Surrogate manager error handling (15 tests)
- Coverage/final.xml still shows 56.79% with timestamp 2026-03-12 19:16 (BEFORE gap closure tests)
- Missing: Re-run `cargo tarpaulin --lib --out xml --output-dir coverage/` to measure actual coverage improvement

# Existing Implementation from 10-08-SUMMARY.md
49 tests created in tests/test_coverage_enhancement.rs:
1. ASHRAE 140 validator tests (18): Initialization, case loading, validation execution, report generation
2. Thermal model builder tests (16): Builder patterns, parameter application, configuration validation, zone initialization
3. Surrogate manager tests (15): Model loading, inference errors, batched inference, session pool

All 49 tests passing successfully.

# Current Coverage Status
- Baseline: 56.79% (4985/8778 lines) - measured 2026-03-12 19:16 (before gap closure tests)
- Expected improvement: 5-10 percentage points from 49 new tests
- Target: 80% (may not be reached in this plan, but progress should be verified)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run coverage measurement after 49 new tests</name>
  <files>coverage/final.xml, coverage/cobertura.xml</files>
  <action>
    Re-run cargo tarpaulin to measure test coverage after the 49 new tests from Plan 10-08 were added.

    Execute coverage measurement:
    ```bash
    # Run coverage measurement with tarpaulin
    cargo tarpaulin --lib --out xml --output-dir coverage/

    # Verify new reports were created with current timestamp
    ls -la coverage/final.xml coverage/cobertura.xml

    # Extract coverage percentage from new report
    grep "line-rate=" coverage/cobertura.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/' | awk '{printf "New Coverage: %.2f%%\n", $1 * 100}'
    ```

    The coverage measurement will:
    1. Compile and run all tests (including the 49 new tests from test_coverage_enhancement.rs)
    2. Generate updated coverage reports with current timestamp (2026-03-13)
    3. Provide actual coverage percentage reflecting the new tests

    Expected outcome:
    - Coverage should increase from 56.79% toward the 80% target
    - Timestamp on coverage files should be after 2026-03-13
    - Test count should include the 49 new tests from Plan 10-08
  </action>
  <verify>
    # Verify new coverage report exists and has current timestamp
    ls -la coverage/final.xml | grep "Mar 13" && \
    # Extract and display coverage percentage
    grep "line-rate=" coverage/cobertura.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/' | awk '{printf "Coverage: %.2f%%\n", $1 * 100}'
  </verify>
  <done>Coverage re-measured with new reports (final.xml, cobertura.xml) showing actual improvement from 49 new tests</done>
</task>

<task type="auto">
  <name>Task 2: Update coverage documentation with new metrics</name>
  <files>docs/PHASE10_TEST_COVERAGE.md</files>
  <action>
    Update docs/PHASE10_TEST_COVERAGE.md to reflect the new coverage measurement after adding 49 tests.

    Read the new coverage percentage from coverage/cobertura.xml:
    ```bash
    COVERAGE=$(grep "line-rate=" coverage/cobertura.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/')
    echo "New Coverage: $(echo "$COVERAGE * 100" | bc -l | xargs printf "%.2f")%"
    ```

    Update PHASE10_TEST_COVERAGE.md with:
    1. New overall coverage percentage (replace 56.79% with actual new value)
    2. Updated baseline section noting the gap closure tests
    3. New module-level coverage for the three targeted modules (validator, thermal model, surrogate)
    4. Progress toward 80% target (e.g., "XX% of target achieved")
    5. Remaining gaps to reach 80%

    Format the update as:
    ```markdown
    ## Post-Gap Closure Coverage (2026-03-13)

    After adding 49 coverage enhancement tests in Phase 10 Plan 08:

    - **Overall Coverage:** XX.XX% (up from 56.79%)
    - **Progress toward 80% target:** YY%
    - **Tests Added:** 49 (896 lines) in test_coverage_enhancement.rs

    ### Module Coverage Improvements

    | Module | Previous | Current | Change | Target |
    |--------|----------|---------|--------|--------|
    | ASHRAE 140 Validator | 19.4% | ZZ.ZZ% | +YY.Y% | 80% |
    | Thermal Model | 37.1% | ZZ.ZZ% | +YY.Y% | 80% |
    | Surrogate Manager | 31.2% | ZZ.ZZ% | +YY.Y% | 80% |
    ```

    The documentation should clearly show the progress made toward the 80% target and identify any remaining gaps.
  </action>
  <verify>
    # Verify documentation was updated with new coverage percentage
    grep -E "Overall Coverage|After adding.*49.*tests" docs/PHASE10_TEST_COVERAGE.md && \
    # Verify timestamp in documentation
    grep "2026-03-13" docs/PHASE10_TEST_COVERAGE.md
  </verify>
  <done>PHASE10_TEST_COVERAGE.md updated with new coverage metrics showing actual improvement from 49 tests</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify coverage/final.xml has timestamp after 2026-03-13
2. Verify coverage/cobertura.xml has timestamp after 2026-03-13
3. Verify new coverage percentage is >56.79% (actual improvement from 49 tests)
4. Verify PHASE10_TEST_COVERAGE.md documents new coverage metrics
5. Verify all 49 tests in test_coverage_enhancement.rs still pass
</verification>

<success_criteria>
1. Coverage reports (final.xml, cobertura.xml) re-generated with current timestamp (2026-03-13)
2. Coverage percentage increased from 56.79% baseline
3. PHASE10_TEST_COVERAGE.md updated with new metrics and progress toward 80%
4. All 49 new tests continue to pass
5. Coverage improvement is measurable and documented
</success_criteria>

<output>
After completion, create `.planning/phases/10-Quality-Testing/10-08-SUMMARY.md`
</output>
