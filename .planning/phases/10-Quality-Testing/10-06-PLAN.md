---
phase: 10
plan: 06
type: execute
wave: 3
depends_on: [10-01]
files_modified: [tests/test_isolation.rs]
autonomous: true
requirements: [TEST-06]

must_haves:
  truths:
    - "Tests run independently without relying on shared state"
    - "Tests can be executed in any order without failures"
    - "No mutable static variables or global state pollution between tests"
  artifacts:
    - path: "tests/test_isolation.rs"
      provides: "Test isolation verification suite"
      min_lines: 60
    - path: "docs/TEST_ISOLATION_REPORT.md"
      provides: "Audit report of shared state issues"
      min_lines: 30
  key_links:
    - from: "tests/test_isolation.rs"
      to: "src/sim/engine.rs"
      via: "ThermalModel state isolation"
      pattern: "ThermalModel::new.*each test"
    - from: "tests/test_isolation.rs"
      to: "cargo test"
      via: "Individual test execution"
      pattern: "cargo test.*test_name"
---

<objective>
Verify and enforce test isolation across all test files

Purpose: Ensure tests are independent and can run in any order without side effects
Output: Test isolation verification suite and audit report
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-Quality-Testing/10-RESEARCH.md
@.planning/phases/10-Quality-Testing/10-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md

# From 10-RESEARCH.md (anti-patterns):
- "Shared static state in tests: Tests should not depend on mutable static variables, causes race conditions"
- "Assuming test execution order: Tests must be independent and runnable in any order"

# From .planning/codebase/TESTING.md:
```rust
// Floating-Point Comparison
const EPSILON: f64 = 1e-9;
assert!((actual - expected).abs() < EPSILON);
```

# Current test count: 88 test files
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit test files for shared state</name>
  <files>docs/TEST_ISOLATION_REPORT.md</files>
  <action>
    1. Scan all 88 test files for shared state patterns:
       ```bash
       # Check for static mut
       grep -r "static mut" tests/ src/**/tests.rs src/**/test*.rs > /tmp/static_mut.txt

       # Check for lazy_static with mutable state
       grep -r "lazy_static" tests/ src/**/tests.rs > /tmp/lazy_static.txt

       # Check for global mutable variables
       grep -r "pub static.*mut" src/ > /tmp/global_mut.txt

       # Check for once_cell with mutable state
       grep -r "once_cell.*Mutex\|once_cell.*RwLock" tests/ src/**/tests.rs > /tmp/once_cell_mut.txt
       ```

    2. Identify problematic patterns:
       - `static mut` variables (most dangerous)
       - `lazy_static!` with `Mutex<RwLock<T>>`
       - Global mutable variables accessible from tests
       - File system artifacts not cleaned up

    3. Create docs/TEST_ISOLATION_REPORT.md with findings:
       - List files with shared state issues
       - Categorize severity (critical, warning, info)
       - Recommend fixes for each issue

    4. Review existing test files for state cleanup:
       - Check if tests use `tempfile` crate for file isolation
       - Verify tests create fresh instances (not reuse)
       - Look for test fixtures with proper teardown

    Rationale: Auditing identifies sources of shared state before writing isolation tests. Documentation tracks findings for future reference.
  </action>
  <verify>
    <automated>test -f docs/TEST_ISOLATION_REPORT.md && grep -q "Test Isolation Audit" docs/TEST_ISOLATION_REPORT.md</automated>
  </verify>
  <done>Test isolation audit completed, report documents findings</done>
</task>

<task type="auto">
  <name>Task 2: Create test isolation verification suite</name>
  <files>tests/test_isolation.rs</files>
  <action>
    Create tests/test_isolation.rs with isolation verification tests:

    1. **Individual Test Execution Test**:
       - Select 10 representative tests from different modules
       - Run each test individually via cargo test
       - Verify all pass when run alone
       - Tests to check:
         - ThermalModel creation and solve
         - VectorField operations
         - BatchOracle evaluation
         - SurrogateManager (mock)
         - ASHRAE 140 validation (subset)

    2. **Random Order Execution Test**:
       - Run full test suite multiple times with different --test-threads settings
       - Verify all tests pass regardless of thread count (1, 2, 4, 8)
       - Test execution order shouldn't affect results

    3. **State Pollution Detection**:
       - Create test that sets global state (if any exists)
       - Create second test that checks for pollution
       - Verify second test doesn't see state from first test

    4. **File System Isolation Test**:
       - Create test that writes temp file
       - Verify file doesn't exist before test
       - Verify file is cleaned up after test
       - Use tempfile crate for proper isolation

    5. **ThermalModel Instance Isolation**:
       - Create test that modifies ThermalModel state
       - Create second test with fresh instance
       - Verify second test doesn't see modifications from first

    For each test:
    - Use descriptive test names indicating what's being verified
    - Add diagnostic output for debugging failures
    - Use assert! to verify isolation properties
    - Document any assumptions about test framework behavior

    Rationale: Verification tests prove tests can run independently and in any order without side effects.
  </action>
  <verify>
    <automated>cargo test --test test_isolation</automated>
  </verify>
  <done>Test isolation verification suite passes, proving independence</done>
</task>

<task type="auto">
  <name>Task 3: Fix identified shared state issues</name>
  <files>docs/TEST_ISOLATION_REPORT.md, src/**/*.rs, tests/**/*.rs</files>
  <action>
    1. Review docs/TEST_ISOLATION_REPORT.md for critical issues:
       - Fix `static mut` variables first (highest priority)
       - Replace with thread-safe alternatives or remove global state
       - Add #[cfg(test)] guards to prevent test-only globals from leaking

    2. Fix lazy_static/once_cell mutable state:
       - Replace with test-local initialization
       - Use test fixtures with setup/teardown
       - Consider moving initialization to test bodies

    3. Fix file system pollution:
       - Use tempfile crate for temporary files
       - Add cleanup code in test teardown
       - Verify files don't exist before test runs

    4. Fix ThermalModel state sharing:
       - Ensure each test creates fresh instance with `ThermalModel::new()`
       - Avoid static or global model instances
       - Clone models if needed (ThermalModel is Clone)

    5. For SurrogateManager:
       - Each test should create new instance: `SurrogateManager::new()`
       - Avoid global SessionPool sharing between tests
       - Use mock predictions for test isolation

    6. Re-run test isolation verification after fixes:
       ```bash
       cargo test --test test_isolation
       ```

    Rationale: Fixing shared state issues ensures tests are truly independent and deterministic.
  </action>
  <verify>
    <automated>cargo test --test test_isolation && cargo test --lib --quiet</automated>
  </verify>
  <done>All shared state issues fixed, tests run independently</done>
</task>

</tasks>

<verification>
All tests pass independently and in any order
</verification>

<success_criteria>
- docs/TEST_ISOLATION_REPORT.md documents audit findings
- tests/test_isolation.rs created with 5+ isolation tests
- All critical shared state issues fixed
- Tests pass when run individually
- Tests pass with different --test-threads settings (1, 2, 4, 8)
- No file system pollution between test runs
</success_criteria>

<output>
After completion, create `.planning/phases/10-Quality-Testing/10-06-SUMMARY.md`
</output>
