---
phase: 10-Quality-Testing
plan: 13
type: execute
wave: 1
depends_on: [10-12]
files_modified:
  - coverage/final.xml
  - coverage/cobertura.xml
  - docs/PHASE10_TEST_COVERAGE.md
autonomous: true
requirements: [TEST-01]
gap_closure: true

must_haves:
  truths:
    - "Coverage successfully re-measured after 49 new tests using alternative tool (cargo-llvm-cov)"
    - "Actual coverage percentage documented with timestamp showing measurement after Plan 10-08"
    - "Coverage improvement quantified and documented in PHASE10_TEST_COVERAGE.md"
    - "TEST-01 requirement status updated based on new coverage measurement"
  artifacts:
    - path: "coverage/final.xml"
      provides: "Updated coverage report with new timestamp after 49 new tests"
      contains: "line-rate attribute reflecting post-Plan 10-08 coverage"
    - path: "coverage/cobertura.xml"
      provides: "Cobertura-format coverage report for CI/CD integration"
      contains: "line-rate attribute reflecting post-Plan 10-08 coverage"
    - path: "docs/PHASE10_TEST_COVERAGE.md"
      provides: "Comprehensive coverage analysis with before/after comparison"
      contains: "Section documenting coverage improvement from 49 new tests"
  key_links:
    - from: "cargo llvm-cov --lib"
      to: "coverage/final.xml"
      via: "XML output generation"
      pattern: "cargo llvm-cov --lib --xml --output-path"
    - from: "cargo llvm-cov --lib"
      to: "docs/PHASE10_TEST_COVERAGE.md"
      via: "Coverage percentage extraction and documentation"
      pattern: "grep line-rate|awk"
---

<objective>
Re-measure test coverage after 49 new tests using cargo-llvm-cov (alternative to tarpaulin) to close TEST-01 gap

Purpose: Plan 10-12 attempted coverage re-measurement but was blocked by persistent tarpaulin timeout issues. This plan uses the recommended alternative (cargo-llvm-cov) to successfully measure coverage and document the improvement from the 49 new tests added in Plan 10-08.

Output: Updated coverage reports (final.xml, cobertura.xml) with new timestamp, updated PHASE10_TEST_COVERAGE.md documenting coverage improvement
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-Quality-Testing/10-VERIFICATION.md
@.planning/phases/10-Quality-Testing/10-RESEARCH.md
@.planning/phases/10-Quality-Testing/10-12-SUMMARY.md
@.planning/phases/10-Quality-Testing/10-08-SUMMARY.md

# Context from Plan 10-12 (Blocked by tarpaulin timeout)
Plan 10-12 attempted to re-measure coverage after 49 new tests (Plan 10-08) using cargo tarpaulin, but was blocked by persistent timeout issues:
- 3+ attempts over 30 minutes all failed with "Timed out waiting for test response"
- Increased timeout to 600s: Still timed out
- Test suite size: 422 library tests with complex rayon parallel execution

Current coverage files show 56.79% from 2026-03-12, BEFORE the 49 new tests were added.

# Recommended Alternative from Plan 10-12
Use cargo-llvm-cov which is faster and more reliable for large test suites.

# Context from Plan 10-08 (49 new tests added)
Plan 10-08 successfully added 49 comprehensive tests (896 lines) covering:
- ASHRAE 140 validator orchestrator (18 tests)
- Thermal model builder patterns (16 tests)
- Surrogate manager error handling (15 tests)

All 49 tests pass successfully in tests/test_coverage_enhancement.rs.

# Expected Coverage Improvement
Based on 49 new tests (896 lines) targeting critical gaps, expected improvement is 5-10 percentage points:
- Current: 56.79% (before 49 new tests)
- Expected: 61.79% - 66.79% (after 49 new tests)
- Target: 80% (TEST-01 requirement)

Even if coverage doesn't reach 80%, documenting actual improvement is critical for gap closure.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure cargo-llvm-cov</name>
  <files>Cargo.toml</files>
  <action>
    Install cargo-llvm-cov tool if not already installed:
    ```bash
    cargo install cargo-llvm-cov
    ```

    Initialize llvm-cov for the project:
    ```bash
    cargo llvm-cov clean
    ```

    Reference: From 10-12-SUMMARY.md, Option 1 is recommended. cargo-llvm-cov is faster and more reliable for large test suites than tarpaulin.
  </action>
  <verify>cargo llvm-cov --version succeeds</verify>
  <done>cargo-llvm-cov installed and ready to use</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure coverage using cargo-llvm-cov</name>
  <files>coverage/final.xml, coverage/cobertura.xml</files>
  <action>
    Run coverage measurement using cargo-llvm-cov (library tests only, matching tarpaulin's --lib flag):

    ```bash
    # Generate XML coverage report
    cargo llvm-cov --lib --xml --output-path coverage/final.xml

    # Generate Cobertura report for CI/CD
    cargo llvm-cov --lib --cobertura --output-path coverage/cobertura.xml
    ```

    This generates coverage reports for library code (--lib), matching the scope of the original tarpaulin measurement. The XML format allows comparison with existing coverage reports.

    Reference: From 10-12-SUMMARY.md, cargo-llvm-cov is the recommended alternative to tarpaulin for large test suites.
  </action>
  <verify>coverage/final.xml exists and has timestamp newer than 2026-03-12</verify>
  <done>New coverage reports generated with post-Plan 10-08 timestamp</done>
</task>

<task type="auto">
  <name>Task 3: Extract and document new coverage percentage</name>
  <files>docs/PHASE10_TEST_COVERAGE.md</files>
  <action>
    Extract coverage percentage from the newly generated coverage/final.xml:

    ```bash
    # Extract line-rate from XML and convert to percentage
    NEW_COVERAGE=$(grep "line-rate=" coverage/final.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/' | awk '{printf "%.2f%%", $1 * 100}')
    echo "New coverage: $NEW_COVERAGE"
    ```

    Update docs/PHASE10_TEST_COVERAGE.md with a new section documenting the coverage improvement:

    ## Coverage Re-measurement (Plan 10-13 - 2026-03-13)

    ### Method: cargo-llvm-cov (Alternative to tarpaulin)

    Plan 10-12 was blocked by persistent tarpaulin timeout issues with the 422-test library suite. Plan 10-13 uses cargo-llvm-cov as the recommended alternative (faster, more reliable for large test suites).

    ### Coverage Results

    | Metric | Before (Plan 10-08) | After (Plan 10-13) | Change |
    |--------|---------------------|-------------------|---------|
    | Line coverage | 56.79% | ${NEW_COVERAGE} | +${CALCULATED_DIFF}% |
    | Tests added | 0 | 49 (896 lines) | - |
    | Date | 2026-03-12 | 2026-03-13 | - |

    ### 49 New Tests (Plan 10-08)

    Coverage improvement from 49 comprehensive tests in tests/test_coverage_enhancement.rs:
    - ASHRAE 140 validator orchestrator: 18 tests
    - Thermal model builder patterns: 16 tests
    - Surrogate manager error handling: 15 tests

    ### TEST-01 Requirement Status

    Target: >80% line coverage
    Achieved: ${NEW_COVERAGE}
    Status: ${PASSING_OR_FAILING}

    ${IF_PASSING: TEST-01 requirement satisfied ✅
    ${IF_FAILING: Coverage improved but below 80% target. Additional coverage gaps identified in original PHASE10_TEST_COVERAGE.md analysis.
  </action>
  <verify>docs/PHASE10_TEST_COVERAGE.md updated with coverage comparison section</verify>
  <done>Coverage improvement documented with before/after comparison</done>
</task>

<task type="auto">
  <name>Task 4: Update TEST-01 requirement status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Check if coverage reached 80% target:

    ```bash
    COVERAGE=$(grep "line-rate=" coverage/final.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/' | awk '{printf "%.2f", $1 * 100}')
    if (( $(echo "$COVERAGE >= 80" | bc -l) )); then
        echo "PASSING: Coverage $COVERAGE% meets 80% target"
    else
        echo "BELOW TARGET: Coverage $COVERAGE% below 80% target"
    fi
    ```

    Update TEST-01 in .planning/REQUIREMENTS.md based on coverage result:

    IF coverage >= 80%:
    ```
    - [x] **TEST-01**: Increase unit test coverage to >80%
      Completed: 2026-03-13
      Details: Coverage reached ${COVERAGE}% using cargo-llvm-cov (Plan 10-13). 49 new tests added in Plan 10-08 (ASHRAE validator: 18, thermal model: 16, surrogate manager: 15).
    ```

    IF coverage < 80%:
    ```
    - [ ] **TEST-01**: Increase unit test coverage to >80%
      Status: PARTIAL - Coverage improved to ${COVERAGE}% but below 80% target
      Details: 49 new tests added in Plan 10-08, coverage re-measured in Plan 10-13 using cargo-llvm-cov. Coverage improvement documented in docs/PHASE10_TEST_COVERAGE.md. Remaining gaps identified in original analysis.
    ```

    Reference: From 10-VERIFICATION.md, TEST-01 requirement is the gap being closed by this plan.
  </action>
  <verify>REQUIREMENTS.md updated with TEST-01 status based on coverage result</verify>
  <done>TEST-01 requirement status reflects actual coverage measurement</done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md with Plan 10-13 status</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update .planning/ROADMAP.md Phase 10 section to include Plan 10-13:

    ```
    **Plans:**
    12/13 plans complete (10-13 in progress)
    - [x] 10-01-PLAN.md — Test infrastructure setup
    - [x] 10-02-PLAN.md — Add property-based tests for thermal invariants
    - [x] 10-03-PLAN.md — Add integration tests for edge cases
    - [x] 10-04-PLAN.md — Fix flaky tests and ensure deterministic parallel execution
    - [x] 10-05-PLAN.md — Implement performance regression tests with baseline gating
    - [x] 10-06-PLAN.md — Verify and enforce test isolation across all test files
    - [x] 10-07-PLAN.md — Achieve >80% unit test coverage and document gaps
    - [x] 10-08-PLAN.md — Add 49 coverage enhancement tests (gap closure)
    - [x] 10-09-PLAN.md — Clarify and complete TEST-03 edge case coverage (gap closure)
    - [x] 10-10-PLAN.md — Finalize BUG-04 flaky test elimination and verification (gap closure)
    - [x] 10-11-PLAN.md — Mark TEST-04 as complete (gap closure - identical to BUG-04)
    - [ ] 10-12-PLAN.md — Re-measure coverage after 49 new tests (blocked by tarpaulin timeout)
    * [ ] 10-13-PLAN.md — Re-measure coverage using cargo-llvm-cov (gap closure follow-up)
    ```

    Update "Progress Tracking" section:
    ```
    | 10. Quality & Testing | 12/13 | In Progress | 10-01 through 10-11 complete, 10-12 blocked, 10-13 in progress |
    ```
  </action>
  <verify>ROADMAP.md updated with Plan 10-13 entry</verify>
  <done>ROADMAP.md reflects Plan 10-13 gap closure work</done>
</task>

<task type="auto">
  <name>Task 6: Commit coverage reports and documentation updates</name>
  <files>coverage/final.xml, coverage/cobertura.xml, docs/PHASE10_TEST_COVERAGE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Commit the updated coverage reports and documentation with a clear commit message:

    ```bash
    git add coverage/final.xml coverage/cobertura.xml docs/PHASE10_TEST_COVERAGE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md
    git commit -m "feat(10-13): re-measure coverage using cargo-llvm-cov to close TEST-01 gap

    - Installed and configured cargo-llvm-cov (alternative to tarpaulin)
    - Re-measured coverage after 49 new tests (Plan 10-08)
    - Generated new coverage reports (final.xml, cobertura.xml)
    - Documented coverage improvement in PHASE10_TEST_COVERAGE.md
    - Updated TEST-01 requirement status in REQUIREMENTS.md
    - Updated ROADMAP.md with Plan 10-13 status

    Plan 10-12 was blocked by persistent tarpaulin timeout issues with 422-test library suite.
    cargo-llvm-cov provides faster, more reliable coverage measurement for large test suites.

    Coverage: ${NEW_COVERAGE}% (${BEFORE} → ${AFTER}, +${DIFF}%)
    Tests added: 49 (ASHRAE validator: 18, thermal model: 16, surrogate manager: 15)

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
    ```
  </action>
  <verify>git log shows commit with coverage re-measurement changes</verify>
  <done>All coverage and documentation updates committed</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. Coverage reports have timestamps newer than 2026-03-12 (before 49 new tests):
   ```bash
   ls -l coverage/final.xml coverage/cobertura.xml
   ```

2. Coverage percentage extracted and documented:
   ```bash
   grep "line-rate=" coverage/final.xml | head -1
   ```

3. PHASE10_TEST_COVERAGE.md updated with coverage comparison section

4. REQUIREMENTS.md TEST-01 status reflects actual coverage result

5. ROADMAP.md includes Plan 10-13 entry

6. Git commit created with coverage re-measurement changes
</verification>

<success_criteria>
- Coverage successfully measured using cargo-llvm-cov (tarpaulin alternative)
- New coverage reports (final.xml, cobertura.xml) generated with post-Plan 10-08 timestamp
- Coverage improvement documented in PHASE10_TEST_COVERAGE.md with before/after comparison
- TEST-01 requirement status updated in REQUIREMENTS.md based on actual coverage measurement
- ROADMAP.md reflects Plan 10-13 gap closure work
- All changes committed to git

If coverage reaches >= 80%: TEST-01 gap closed (requirement satisfied)
If coverage is < 80%: TEST-01 gap narrowed (coverage improved, documented remaining gaps)
</success_criteria>

<output>
After completion, create `.planning/phases/10-Quality-Testing/10-13-SUMMARY.md`

Summary should include:
- Coverage measurement method (cargo-llvm-cov vs tarpaulin)
- Before/after coverage comparison (56.79% → new%)
- Coverage improvement quantification
- TEST-01 requirement status (PASSING or BELOW TARGET)
- Files updated (coverage reports, documentation)
- Gap closure status (CLOSED if >=80%, NARROWED if <80%)
</output>
