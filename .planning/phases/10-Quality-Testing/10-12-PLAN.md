---
phase: 10-Quality-Testing
plan: 12
type: execute
wave: 1
depends_on: [10-08]
files_modified: [coverage/final.xml, coverage/cobertura.xml, docs/PHASE10_TEST_COVERAGE.md]
autonomous: true
requirements: [TEST-01]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Coverage is re-measured after adding 49 new tests from Plan 10-08"
    - "New coverage percentage reflects actual improvement from 49 tests"
    - "Coverage report is updated with current timestamp"
    - "PHASE10_TEST_COVERAGE.md documents the actual coverage improvement"
  artifacts:
    - path: "coverage/final.xml"
      provides: "Updated coverage report with new measurement"
      contains: "Coverage data measured after 49 new tests"
    - path: "coverage/cobertura.xml"
      provides: "Cobertura-formatted coverage report"
      contains: "line-rate attribute with updated percentage"
    - path: "docs/PHASE10_TEST_COVERAGE.md"
      provides: "Documentation of coverage improvement"
      min_lines: 450
  key_links:
    - from: "tests/test_coverage_enhancement.rs"
      to: "coverage/final.xml"
      via: "cargo tarpaulin execution"
      pattern: "cargo tarpaulin --lib --out xml"
    - from: "coverage/cobertura.xml"
      to: "docs/PHASE10_TEST_COVERAGE.md"
      via: "Coverage percentage extraction and documentation"
      pattern: "line-rate.*[0-9.]+"
---

<objective>
Re-measure test coverage after adding 49 new tests in Plan 10-08 to verify actual improvement toward the 80% target

Purpose: Plan 10-08 added 49 comprehensive tests (896 lines) covering ASHRAE validator, thermal model, and surrogate manager, but coverage was not re-measured. The existing coverage reports (final.xml, cobertura.xml) still show 56.79% from March 12, before the gap closure tests were added. This plan re-measures coverage to verify the actual improvement from the 49 new tests.

Output: Updated coverage reports (final.xml, cobertura.xml) with current timestamp and actual coverage percentage, plus updated documentation in PHASE10_TEST_COVERAGE.md
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-Quality-Testing/10-08-SUMMARY.md
@.planning/phases/10-Quality-Testing/10-VERIFICATION.md
@.planning/REQUIREMENTS.md

# Key context from Plan 10-08
Plan 10-08 created 49 tests in tests/test_coverage_enhancement.rs (896 lines):
- 18 ASHRAE 140 validator tests
- 16 thermal model builder tests
- 15 surrogate manager error handling tests

All tests pass, but coverage was not re-measured after creation. Coverage reports still show 56.79% from 2026-03-12 19:16 (final.xml) and 2026-03-12 20:32 (cobertura.xml).

Expected improvement: 5-10 percentage points from 49 new tests (896 lines of test code)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure coverage with cargo tarpaulin</name>
  <files>coverage/final.xml, coverage/cobertura.xml</files>
  <action>
    Run cargo tarpaulin to generate updated coverage reports:
    ```bash
    cargo tarpaulin --lib --out xml --output-dir coverage/
    ```

    This will:
    - Execute all tests including the 49 new tests from Plan 10-08
    - Generate coverage/final.xml with current timestamp
    - Generate coverage/cobertura.xml with current timestamp
    - Measure actual line coverage across all library code

    Note: Use --lib flag to focus on library code (src/), not integration tests (tests/), to match the 56.79% baseline which was also measured with --lib.
  </action>
  <verify>
    <automated>ls -lh coverage/final.xml coverage/cobertura.xml && grep -E "line-rate=|timestamp" coverage/cobertura.xml | head -3</automated>
  </verify>
  <done>
    - coverage/final.xml exists with current timestamp (after 2026-03-13)
    - coverage/cobertura.xml exists with current timestamp
    - Both files are >200KB (comprehensive coverage data)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract new coverage percentage</name>
  <files>coverage/cobertura.xml</files>
  <action>
    Extract the updated coverage percentage from cobertura.xml:
    ```bash
    grep 'line-rate=' coverage/cobertura.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/' | awk '{printf "Coverage: %.2f%%\n", $1 * 100}'
    ```

    This extracts the line-rate attribute from the root coverage element and converts from decimal to percentage.
  </action>
  <verify>
    <automated>grep 'line-rate=' coverage/cobertura.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/' | awk '{printf "Coverage: %.2f%%\n", $1 * 100}'</automated>
  </verify>
  <done>
    - New coverage percentage extracted and displayed
    - Coverage >56.79% (showing improvement from 49 new tests)
    - Document the percentage for Task 3
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PHASE10_TEST_COVERAGE.md with new metrics</name>
  <files>docs/PHASE10_TEST_COVERAGE.md</files>
  <action>
    Update docs/PHASE10_TEST_COVERAGE.md to reflect the re-measured coverage:

    1. Read existing coverage documentation (433 lines, module-by-module analysis)
    2. Update the "Overall Coverage" section with:
       - New total coverage percentage (from Task 2)
       - Measurement date (current date)
       - Improvement from baseline (new% - 56.79%)
       - Note about 49 new tests from Plan 10-08
    3. Add a "Coverage Re-Measurement (Gap Closure)" section documenting:
       - Date of re-measurement
       - Coverage before (56.79%) and after (new percentage)
       - Improvement percentage
       - Tests added: 49 tests in tests/test_coverage_enhancement.rs (Plan 10-08)
       - Assessment of progress toward 80% target

    Format should match existing documentation style in PHASE10_TEST_COVERAGE.md.
  </action>
  <verify>
    <automated>grep -E "Coverage:|Coverage Re-Measurement|Plan 10-08|56.79%|%" docs/PHASE10_TEST_COVERAGE.md | head -10</automated>
  </verify>
  <done>
    - docs/PHASE10_TEST_COVERAGE.md updated with new coverage metrics
    - "Overall Coverage" section shows new percentage and date
    - "Coverage Re-Measurement (Gap Closure)" section added documenting before/after
    - 49 new tests from Plan 10-08 acknowledged as source of improvement
    - Document reflects actual coverage improvement (not outdated 56.79%)
  </done>
</task>

</tasks>

<verification>
Overall verification for this gap closure plan:

1. Coverage re-measured with current timestamp (after 49 new tests added)
2. New coverage percentage >56.79% baseline (showing improvement)
3. Coverage reports (final.xml, cobertura.xml) updated with current timestamp
4. PHASE10_TEST_COVERAGE.md documents the actual coverage improvement
5. Gap in VERIFICATION.md (TEST-01 coverage not re-measured) is closed

Automated verification commands:
```bash
# Verify coverage files exist with current timestamp
ls -lh coverage/final.xml coverage/cobertura.xml

# Extract and display new coverage percentage
grep 'line-rate=' coverage/cobertura.xml | head -1 | sed 's/.*line-rate="\([0-9.]*\)".*/\1/' | awk '{printf "Coverage: %.2f%%\n", $1 * 100}'

# Verify documentation updated
grep -E "Coverage Re-Measurement|Plan 10-08" docs/PHASE10_TEST_COVERAGE.md
```
</verification>

<success_criteria>
1. Coverage reports (final.xml, cobertura.xml) re-generated with current timestamp (2026-03-13 or later)
2. New coverage percentage reflects actual improvement from 49 new tests (should be >56.79%)
3. PHASE10_TEST_COVERAGE.md updated with new metrics and "Coverage Re-Measurement" section
4. TEST-01 gap in VERIFICATION.md is closed (coverage now accurately measured after gap closure tests)
5. Progress toward 80% target documented (even if target not fully reached)
</success_criteria>

<output>
After completion, create `.planning/phases/10-Quality-Testing/10-12-SUMMARY.md` documenting:
- New coverage percentage and improvement from baseline
- Before/after comparison (56.79% → new%)
- Assessment of progress toward 80% target
- Files updated (coverage reports, documentation)
- Gap closure verification (TEST-01 now fully satisfied)
</output>
