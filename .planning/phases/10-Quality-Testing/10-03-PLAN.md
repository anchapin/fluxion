---
phase: 10
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified: [tests/test_edge_cases.rs]
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Extreme parameter values are handled without panics"
    - "Zero load scenarios produce expected behavior"
    - "Boundary conditions (min/max parameters) work correctly"
  artifacts:
    - path: "tests/test_edge_cases.rs"
      provides: "Integration tests for edge cases"
      min_lines: 100
  key_links:
    - from: "tests/test_edge_cases.rs"
      to: "src/sim/engine.rs"
      via: "ThermalModel edge case handling"
      pattern: "ThermalModel::new.*apply_parameters"
    - from: "tests/test_edge_cases.rs"
      to: "src/ai/surrogate.rs"
      via: "SurrogateManager edge cases"
      pattern: "SurrogateManager::"
---

<objective>
Add integration tests for edge cases and boundary conditions

Purpose: Ensure the system handles extreme parameters, zero loads, and boundary conditions gracefully
Output: Comprehensive edge case test coverage
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-Quality-Testing/10-RESEARCH.md
@.planning/phases/10-Quality-Testing/10-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md

# Parameter bounds from CLAUDE.md
- window_u_value: 0.1–5.0 W/m²K (MIN_U_VALUE, MAX_U_VALUE)
- hvac_setpoint: 15–30°C (MIN_SETPOINT, MAX_SETPOINT)

# From .planning/codebase/TESTING.md
```rust
// Error testing pattern
#[test]
fn test_invalid_weights() {
    let result = NeuralScalarField::new(vec![]);
    assert!(result.is_err());
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create edge case integration tests</name>
  <files>tests/test_edge_cases.rs</files>
  <action>
    Create tests/test_edge_cases.rs with the following test cases:

    1. **Extreme Parameter Values Test**:
       - Test MIN_U_VALUE (0.1) and MAX_U_VALUE (5.0)
       - Test MIN_SETPOINT (15) and MAX_SETPOINT (30)
       - Test boundary combinations (min U + min setpoint, max U + max setpoint)
       - Verify model runs without panics and produces valid results

    2. **Zero Load Scenarios**:
       - Test ThermalModel with zero loads (all loads = 0.0)
       - Verify energy conservation (no energy added/removed)
       - Verify temperatures evolve naturally (free-floating)

    3. **Extreme Temperature Initial Conditions**:
       - Test initialization at very low temperatures (-50°C)
       - Test initialization at very high temperatures (100°C)
       - Verify physics solver handles extreme temperatures without NaN/Inf

    4. **Boundary Conductance Values**:
       - Test with zero conductance (h_tr_w = 0.0)
       - Test with very high conductance (h_tr_w = 1000.0)
       - Verify solver doesn't divide by zero or produce invalid results

    5. **Single Zone Edge Case**:
       - Test ThermalModel with num_zones = 1
       - Verify all operations work for single-zone model
       - Test parameter application and physics solve

    6. **Large Zone Count Edge Case**:
       - Test ThermalModel with num_zones = 1000
       - Verify solver scales correctly
       - Check for performance degradation or memory issues

    7. **Invalid Parameter Handling**:
       - Test U-value < MIN_U_VALUE (should return NaN or error)
       - Test U-value > MAX_U_VALUE (should return NaN or error)
       - Test setpoint outside valid range (should return NaN or error)
       - Verify graceful degradation, not panics

    For each test:
    - Use assert! or assert_eq! with appropriate tolerances
    - Use approx::assert_relative_eq! for floating-point comparisons
    - Add diagnostic output (println!) for debugging failures
    - Use #[test] attribute

    Rationale: Edge cases often expose bugs that happy-path tests miss. Testing boundaries ensures robustness.
  </action>
  <verify>
    <automated>cargo test --test test_edge_cases</automated>
  </verify>
  <done>All edge case tests pass, covering extreme parameters, zero loads, and boundary conditions</done>
</task>

</tasks>

<verification>
Edge case tests pass and cover all identified scenarios
</verification>

<success_criteria>
- tests/test_edge_cases.rs created with 7+ test cases
- All tests pass without panics
- Tests cover extreme values, zero loads, boundary conditions, and invalid inputs
- Tests follow project testing conventions (assert!, approx, diagnostic output)
</success_criteria>

<output>
After completion, create `.planning/phases/10-Quality-Testing/10-03-SUMMARY.md`
</output>
