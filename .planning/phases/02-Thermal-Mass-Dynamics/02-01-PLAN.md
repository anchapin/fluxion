---
phase: 02-Thermal-Mass-Dynamics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_thermal_mass_integration.rs
  - tests/test_thermal_mass_dynamics.rs
  - tests/ashrae_140_case_900.rs
autonomous: true
requirements:
  - FREE-02
  - TEMP-01
must_haves:
  truths:
    - Thermal mass integration test module exists with unit tests
    - Mass-air coupling conductance validation tests exist
    - Case 900 reference values are documented
  artifacts:
    - path: tests/test_thermal_mass_integration.rs
      provides: Unit tests for implicit/explicit integration methods
      min_lines: 100
    - path: tests/test_thermal_mass_dynamics.rs
      provides: Unit tests for h_tr_em, h_tr_ms conductances
      min_lines: 100
    - path: tests/ashrae_140_case_900.rs
      provides: Case 900 reference values (annual heating/cooling, peak loads)
      min_lines: 50
  key_links:
    - from: tests/test_thermal_mass_integration.rs
      to: src/sim/thermal_integration.rs
      via: testing integration method implementations
      pattern: "backward_euler|crank_nicolson"
    - from: tests/test_thermal_mass_dynamics.rs
      to: src/sim/engine.rs
      via: testing mass-air coupling conductance calculations
      pattern: "h_tr_em|h_tr_ms"
---

<objective>
Create test scaffolds for thermal mass integration methods, mass-air coupling validation, and Case 900 reference values to enable TDD implementation in subsequent plans.

Purpose: Wave 0 plan creates failing tests that define expected thermal mass behavior, enabling test-driven development in Plans 02-03. These tests ensure integration methods are numerically stable and mass-air coupling conductances match ISO 13790 specifications.
Output: Three test modules with failing tests documenting expected thermal mass dynamics.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-Thermal-Mass-Dynamics/02-RESEARCH.md
@.planning/phases/01-CONTEXT.md/01-03-SUMMARY.md
@.planning/phases/01-CONTEXT.md/01-04-SUMMARY.md

# Research insights on thermal mass integration methods
From 02-RESEARCH.md:
"Explicit Euler integration (Tm_new = Tm_old + dt * Q/Cm) is unstable for high thermal capacitance (>500 J/K) with dt=3600s. Use implicit (backward Euler) or semi-implicit (Crank-Nicolson) integration, which are unconditionally stable for stiff systems."

# Research insights on mass-air coupling conductances
From 02-RESEARCH.md:
"h_tr_em = 1 / ((1 / h_tr_op) - (1 / (h_ms * a_m))) - ISO 13790 formula for exterior-to-mass conductance. Incorrect conductances cause wrong thermal lag times."

# Phase 1 context
From Phase 1 summaries:
- Phase 1 achieved 37.5% MAE improvement (78.79% to 49.21%)
- Free-floating cases 600FF, 650FF pass, but 900FF shows under-damped behavior (max 37.52°C vs reference 41.8-46.4°C)
- Heating over-prediction 37-87% remains as systematic issue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create thermal mass integration test scaffold</name>
  <files>tests/test_thermal_mass_integration.rs</files>
  <action>Create test module `tests/test_thermal_mass_integration.rs` with unit tests for thermal mass integration methods. Tests should fail initially because backward Euler and Crank-Nicolson implementations don't exist yet.

    Test structure:
    - Use rstest parameterized tests for different thermal capacitance values (low: 200 J/K, medium: 500 J/K, high: 1000 J/K)
    - Validate stability criterion: dt < Cm / (h_tr_em + h_tr_ms)
    - Compare explicit vs implicit methods on step-by-step basis
    - Run 8760-timestep simulation to check energy balance
    - Expected temperature changes should be physically reasonable (no oscillations, no divergence)

    Test cases to implement:
    - Test 1: Explicit Euler integration works for low thermal capacitance (Cm < 500 J/K)
    - Test 2: Explicit Euler integration fails stability criterion for high thermal capacitance (Cm > 500 J/K)
    - Test 3: Backward Euler integration is numerically stable for high thermal capacitance (Cm > 500 J/K)
    - Test 4: Backward Euler integration produces correct temperature updates within ±0.1°C tolerance
    - Test 5: Crank-Nicolson integration is 2nd-order accurate (smaller error than Backward Euler)
    - Test 6: Integration methods preserve energy balance (no numerical drift over 8760 timesteps)
    - Test 7: Integration methods correctly handle heat flux sign (positive = heating, negative = cooling)</action>
  <verify>cargo test --test test_thermal_mass_integration -- --nocapture</verify>
  <done>Test module created with 7 unit tests for thermal mass integration methods. Tests compile and run (expected to fail initially - TDD RED phase).</done>
</task>

<task type="auto">
  <name>Task 2: Create mass-air coupling conductance test scaffold</name>
  <files>tests/test_thermal_mass_dynamics.rs</files>
  <action>Create test module `tests/test_thermal_mass_dynamics.rs` with unit tests for mass-air coupling conductances. Tests should fail initially because conductance calculations may be incorrect.

    Test structure:
    - Use rstest parameterized tests for different thermal capacitance and area combinations
    - Validate ISO 13790 formula: h_tr_em = 1 / ((1 / h_tr_op) - (1 / (h_ms * a_m)))
    - Check that h_tr_ms_term = h_ms * a_m is calculated correctly
    - Ensure reciprocal_diff = (1/h_tr_op) - (1/h_tr_ms_term) produces positive result
    - Validate edge cases: what if h_tr_op or h_ms * a_m are very small or very large?
    - Reference values from ISO 13790 Annex C for typical building constructions

    Test cases to implement:
    - Test 1: h_tr_ms = h_ms × A_m (mass-to-surface conductance calculation)
    - Test 2: h_tr_em calculation using ISO 13790 formula with positive conductance
    - Test 3: h_tr_em within reasonable range (0.1x to 10x of h_tr_op)
    - Test 4: High thermal mass (Cm=1000, A_m=50) produces h_tr_ms = 455 W/K
    - Test 5: Medium thermal mass (Cm=500, A_m=30) produces h_tr_ms = 273 W/K
    - Test 6: Low thermal mass (Cm=200, A_m=12) produces h_tr_ms = 109.2 W/K
    - Test 7: Conductance values are finite and positive (no NaN, no negative values)</action>
  <verify>cargo test --test test_thermal_mass_dynamics -- --nocapture</verify>
  <done>Test module created with 7 unit tests for mass-air coupling conductances. Tests compile and run (expected to fail initially - TDD RED phase).</done>
</task>

<task type="auto">
  <name>Task 3: Create Case 900 reference values test scaffold</name>
  <files>tests/ashrae_140_case_900.rs</files>
  <action>Create test module `tests/ashrae_140_case_900.rs` with ASHRAE 140 Case 900 reference values. Tests should fail initially because Case 900 does not pass validation.

    Test structure:
    - Define ASHRAE 140 Case 900 specifications (high-mass concrete building)
    - Document reference values from ASHRAE Standard 140
    - Create simulation function for Case 900 and 900FF
    - Compare Fluxion results to reference ranges with ±15% annual, ±10% monthly tolerances
    - Include free-floating temperature swing validation
    - Track annual heating/cooling energy and peak loads

    Test cases to implement:
    - Test 1: Case 900 annual heating energy within reference range [1.17, 2.04] MWh
    - Test 2: Case 900 annual cooling energy within reference range [2.13, 3.67] MWh
    - Test 3: Case 900 peak heating load within reference range [1.10, 2.10] kW
    - Test 4: Case 900 peak cooling load within reference range [2.10, 3.50] kW
    - Test 5: Case 900FF min temperature within reference range [-6.40, -1.60]°C
    - Test 6: Case 900FF max temperature within reference range [41.80, 46.40]°C
    - Test 7: Case 900FF temperature swing (max-min) shows ~19.6% reduction vs 600FF

    Reference Phase 1 validation pattern: use existing ASHRAE 140 case structure from tests/ashrae_140_validation.rs</action>
  <verify>cargo test --test ashrae_140_case_900 -- --nocapture</verify>
  <done>Test module created with 7 unit tests for Case 900 reference values. Tests compile and run (expected to fail initially - TDD RED phase).</done>
</task>

</tasks>

<verification>
- All three test modules compile without errors
- Test modules follow Phase 1 TDD pattern (failing tests first)
- Test coverage for integration methods: 7 tests (explicit Euler stability, backward Euler, Crank-Nicolson, energy balance)
- Test coverage for mass-air coupling: 7 tests (h_tr_ms, h_tr_em calculation, parameterized validation)
- Test coverage for Case 900: 7 tests (annual energy, peak loads, free-floating temps, swing reduction)
</verification>

<success_criteria>
1. tests/test_thermal_mass_integration.rs created with 7 unit tests for integration methods
2. tests/test_thermal_mass_dynamics.rs created with 7 unit tests for mass-air coupling
3. tests/ashrae_140_case_900.rs created with 7 unit tests for Case 900 reference values
4. All tests compile and run (expected to fail initially - TDD RED phase)
5. Test structure follows Phase 1 patterns (rstest parameterized tests, clear assertions)
6. Test documentation includes expected behavior and physical rationale
7. Requirements covered: FREE-02 (test scaffolds for thermal mass dynamics), TEMP-01 (temperature tracking tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-Thermal-Mass-Dynamics/02-01-SUMMARY.md` documenting:
- Test modules created with failing tests (TDD RED phase)
- Test coverage for integration methods, mass-air coupling, and Case 900
- Reference values documented from ASHRAE 140 standard
- Requirements addressed: FREE-02, TEMP-01 (test scaffolds created)
- Ready for Plan 02 (implementation phase)
</output>
