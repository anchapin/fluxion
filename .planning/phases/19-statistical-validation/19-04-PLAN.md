---
phase: 19-statistical-validation
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02, 19-03]
files_modified: [src/bin/fluxion.rs, src/validation/ashrae_140_validator.rs]
autonomous: true
requirements: [STATS-01, STATS-06]
user_setup: []

must_haves:
  truths:
    - `fluxion validate --statistical` flag enables statistical validation
    - Default behavior unchanged (tolerance-based only without flag)
    - CLI accepts optional statistical arguments (alpha threshold)
    - CLI outputs statistical metrics summary to console
  artifacts:
    - path: src/bin/fluxion.rs
      provides: --statistical flag integration
      min_lines: 50
  key_links:
    - from: CLI Validate subcommand
      to: StatisticalValidator
      via: "--statistical flag triggers statistical validation"
      pattern: "statistical.*validate"
    - from: CLI output
      to: report generation
      via: "display statistical metrics in CLI summary"
      pattern: "NMBE.*CV.*RMSE"
---

<objective>
Integrate statistical validation into CLI with `--statistical` flag for opt-in ASHRAE 140 Addendum B compliance.

Purpose: Enable opt-in statistical validation via CLI without breaking existing workflows, providing immediate feedback on statistical metrics during validation runs.

Output: Updated CLI with --statistical flag, alpha argument support, and console output for statistical metrics.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-statistical-validation/19-01-PLAN.md
@.planning/phases/19-statistical-validation/19-02-PLAN.md
@.planning/phases/19-statistical-validation/19-03-PLAN.md

@src/bin/fluxion.rs
@src/validation/ashrae_140_validator.rs
@src/validation/statistical.rs
@src/validation/reporter.rs

# From Plans 19-01/19-02: StatisticalValidator, StatisticalMetrics, ValidationGroup implemented
# From Plan 19-03: Report generation with statistical sections completed
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --statistical flag to Validate subcommand</name>
  <files>src/bin/fluxion.rs</files>
  <behavior>
    - Test 1: Validate subcommand accepts --statistical flag
    - Test 2: Validate subcommand accepts --alpha flag for custom threshold
    - Test 3: Default behavior unchanged (no --statistical = tolerance only)
    - Test 4: CLI parsing fails on invalid alpha values (<0 or >1)
  </behavior>
  <action>In src/bin/fluxion.rs, extend Validate subcommand in Commands enum:
- Add #[arg(long)] statistical: bool to Validate struct
- Add #[arg(long, default_value = "0.05")] alpha: f64 to Validate struct
- Import StatisticalValidator from crate::validation::statistical
- In validate command handler, check statistical flag:
  - If true: create StatisticalValidator::with_alpha(args.alpha), call validate_with_statistics()
  - If false: use existing ASHRAE140Validator (backward compatibility)
- Add validation for alpha: assert!(0.0 <= alpha && alpha <= 1.0, "alpha must be in [0, 1]")
Add inline test parsing Validate command with --statistical and --alpha flags, verify field values.</action>
  <verify>cargo test --bin fluxion</verify>
  <done>--statistical flag added to Validate subcommand, alpha argument supported, default behavior unchanged</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire statistical validation to CLI handler</name>
  <files>src/bin/fluxion.rs</files>
  <behavior>
    - Test 1: validate command with --statistical generates StatisticalReport
    - Test 2: StatisticalReport converted to ValidationReportGenerator input
    - Test 3: CLI outputs statistical metrics summary to console
    - Test 4: CLI generates report with statistical section when --statistical set
  </behavior>
  <action>In src/bin/fluxion.rs validate command handler:
- When statistical=true:
  - Create statistical_validator = StatisticalValidator::with_alpha(args.alpha)
  - Run validation: let stat_report = statistical_validator.validate_with_statistics(cases)?
  - Create ValidationReportGenerator with stat_report.tolerance
  - Generate report with statistical sections: report_generator.generate_report_with_statistics(&stat_report)
  - Print statistical summary to console:
    ```
    === Statistical Validation Results ===
    NMBE: X.XX% [Y.YY%, Z.ZZ% 95% CI]
    CV(RMSE): X.XX% [Y.YY%, Z.ZZ% 95% CI]
    Effect Size (Cohen's d): X.XX (EffectDirection)
    ```
- When statistical=false: use existing ASHRAE140Validator workflow (no changes)
- Handle StatisticalReport conversion to BenchmarkReport for report generation
Add inline test mocking StatisticalValidator and verifying CLI handler behavior.</action>
  <verify>cargo test --bin fluxion</verify>
  <done>CLI handler wired to statistical validation, outputs summary, generates report with statistical sections</done>
</task>

</tasks>

<verification>
1. --statistical flag integrates with CLI Validate subcommand
2. StatisticalValidator workflow works end-to-end via CLI
3. CLI outputs statistical metrics summary to console
4. CLI generates report with statistical sections when --statistical set
5. CLI default behavior unchanged (tolerance-based validation without flag)
</verification>

<success_criteria>
- `fluxion validate --statistical` enables statistical validation
- CLI accepts `--alpha` argument for custom FDR threshold
- Default behavior unchanged (tolerance-based validation without flag)
- CLI outputs statistical metrics summary (NMBE, CV(RMSE), CI, Cohen's d)
- CLI generates report with statistical validation sections
- All existing CLI tests pass (backward compatibility preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistical-validation/19-04-SUMMARY.md`
</output>
