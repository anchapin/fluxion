---
phase: 19-statistical-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Cargo.toml, src/validation/mod.rs, src/validation/statistical.rs]
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04]
user_setup: []

must_haves:
  truths:
    - NMBE and CV(RMSE) can be calculated from validation results
    - Benjamini-Hochberg FDR correction produces boolean pass/fail per test
    - Statistical metrics follow ASHRAE Guideline 14 methodology
    - Zero/near-zero reference values are excluded from NMBE calculation
  artifacts:
    - path: src/validation/statistical.rs
      provides: StatisticalMetrics, BenjaminiHochberg, CohenD implementations
      min_lines: 200
    - path: Cargo.toml
      provides: statrs dependency for statistical computing
      contains: "statrs"
    - path: src/validation/mod.rs
      provides: Public statistical module exports
      contains: "pub mod statistical"
  key_links:
    - from: src/validation/statistical.rs
      to: statrs crate
      via: "use statrs::distribution::{StudentsT, ContinuousCDF}"
      pattern: "statrs::"
    - from: src/validation/statistical.rs
      to: src/validation/report.rs
      via: "BenchmarkReport and ValidationResult imports"
      pattern: "crate::validation::report::"
---

<objective>
Implement core statistical validation infrastructure: NMBE, CV(RMSE), confidence intervals, and Benjamini-Hochberg FDR correction.

Purpose: Provide foundational statistical computing capabilities for ASHRAE 140 Addendum B compliance, enabling per-case metric calculation and multiple testing correction.

Output: New `src/validation/statistical.rs` module with statistical functions, updated `Cargo.toml` with statrs dependency.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/validation/report.rs
@src/validation/ashrae_140_validator.rs

# External Library: statrs 0.18.0
# Provides: StudentsT distribution, ContinuousCDF trait, Statistics trait
# Key methods: StudentsT::new(location, scale, df), cdf(), inverse_cdf()
# Statistics trait: mean(), std_dev(), variance()
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add statrs dependency and create statistical module skeleton</name>
  <files>Cargo.toml, src/validation/mod.rs, src/validation/statistical.rs</files>
  <behavior>
    - Test 1: statrs dependency allows importing StudentsT distribution
    - Test 2: statistical module compiles with no exports yet
    - Test 3: mod.rs exports statistical module publicly
  </behavior>
  <action>Add statrs = "0.18.0" to Cargo.toml dependencies. Create src/validation/statistical.rs with empty mod structure and doc comments explaining ASHRAE 140 Addendum B compliance. Export statistical module in src/validation/mod.rs with "pub mod statistical". Run `cargo check` to verify compilation.</action>
  <verify>cargo check</verify>
  <done>statrs dependency added, statistical module compiles, exported in mod.rs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement NMBE and CV(RMSE) calculation functions</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: calculate_nmbe() returns signed percentage (positive = overprediction)
    - Test 2: calculate_nmbe() uses reference midpoint (ref_min + ref_max) / 2.0
    - Test 3: calculate_nmbe() excludes zero/near-zero references (|ref| < 1e-10)
    - Test 4: calculate_cv_rmse() returns percentage using RMSE / mean(reference) * 100
    - Test 5: calculate_cv_rmse() handles empty result arrays gracefully
  </behavior>
  <action>Implement pub fn calculate_nmbe(results: &[ValidationResult]) -> f64 and pub fn calculate_cv_rmse(results: &[ValidationResult]) -> f64 following CONTEXT.md locked decisions:
- NMBE formula: Σ((predicted - reference_midpoint) / reference_midpoint) / n * 100
- CV(RMSE) formula: (RMSE / mean(reference_midpoint)) * 100
- Reference midpoint: (ref_min + ref_max) / 2.0
- Exclude zero/near-zero references: check abs(ref_midpoint) < 1e-10 before including
- Return f64::NAN if all cases excluded
Import ValidationResult and MetricType from crate::validation::report. Add inline tests verifying formula correctness with sample data.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>NMBE and CV(RMSE) functions implemented with tests passing, zero-exclusion logic verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement 95% confidence interval calculation using t-distribution</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: calculate_ci_nmbe() uses StudentsT distribution for n < 30
    - Test 2: calculate_ci_nmbe() uses normal approximation for n >= 30
    - Test 3: calculate_ci_nmbe() returns (f64::NAN, f64::NAN) for n < 2 (insufficient data)
    - Test 4: CI width increases with smaller sample size (higher uncertainty)
  </behavior>
  <action>Implement pub fn calculate_ci_nmbe(nmbe: f64, std_error: f64, n: usize) -> (f64, f64) and similar for CV(RMSE):
- Use statrs::distribution::StudentsT::new(0.0, 1.0, df) where df = n - 1
- For n >= 30: use t_critical = 1.96 (normal approximation)
- For n < 30: use t_critical = StudentsT::cdf(1.96) (exact t-distribution)
- Return (lower, upper) as (nmbe - t_critical * se, nmbe + t_critical * se)
- Standard error calculation: se = std_dev / sqrt(n)
- Return (f64::NAN, f64::NAN) for n < 2 (not enough degrees of freedom)
Add helper function calculate_standard_error() using statrs::statistics::Statistics trait. Add inline tests validating CI behavior with known t-distribution critical values.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>Confidence interval functions implemented, t-distribution used for small samples, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement Benjamini-Hochberg FDR correction (STATS-04)</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: benjamini_hochberg() sorts p-values and applies threshold (k/m) * α where k is rank and m is total tests
    - Test 2: benjamini_hochberg() returns vector of booleans indicating BH pass
    - Test 3: benjamini_hochberg() stops at first failure (p-values sorted ascending)
    - Test 4: benjamini_hochberg() handles empty p-values vector gracefully
    - Test 5: benjamini_hochberg() uses alpha = 0.05 by default per CONTEXT.md decision
  </behavior>
  <action>Implement pub struct BenjaminiHochberg with pub fn apply(p_values: Vec<f64>, alpha: f64) -> Vec<bool> following standard BH procedure:
1. Create indexed pairs (original_index, p_value)
2. Sort by p_value ascending
3. For each rank k (1-indexed), compute threshold = (k / m) * alpha where m = total tests
4. Mark test as passing if p_k <= threshold
5. Stop at first failure (due to sorting)
6. Map corrected results back to original indices
7. Return Vec<bool> where true = passes BH correction
This addresses STATS-04 requirement for multiple testing corrections. Add inline tests with synthetic p-values [0.01, 0.04, 0.07, 0.10] and alpha=0.05, verify correct BH pass/fail pattern.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>Benjamini-Hochberg correction implemented (STATS-04), threshold formula (k/m)*α specified, handles edge cases, tests verify BH procedure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement Cohen's d effect size calculation</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: calculate_cohens_d() returns signed effect size (positive = underprediction)
    - Test 2: calculate_cohens_d() uses pooled standard deviation for two-sample comparison
    - Test 3: calculate_cohens_d() uses reference std dev for single-sample vs population
    - Test 4: Effect magnitude interpretation: small (0.2), medium (0.5), large (0.8) benchmarks
  </behavior>
  <action>Implement pub fn calculate_cohens_d(predicted: &[f64], reference: &[f64]) -> f64 following Cohen's d formula:
- Single-sample vs population: d = (mean(reference) - mean(predicted)) / std_dev(reference)
- Two-sample comparison: d = (mean1 - mean2) / pooled_std where pooled_std = sqrt(((n1-1)*s1^2 + (n2-1)*s2^2) / (n1+n2-2))
- Return signed value: positive = Fluxion underprediction, negative = Fluxion overprediction
- Use statrs::statistics::Statistics trait for mean() and std_dev()
- Add helper function calculate_pooled_std() for two-sample case
Define pub enum EffectDirection { Overprediction, Underprediction } derived from Cohen's d sign. Add inline tests validating effect size calculation with known data.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>Cohen's d implemented with pooled std dev, effect direction enum added, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Implement StatisticalMetrics struct aggregation</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: StatisticalMetrics::calculate() aggregates all metrics from BenchmarkReport
    - Test 2: StatisticalMetrics contains nmbe, cv_rmse, nmbe_ci, cv_rmse_ci, cohens_d, effect_direction
    - Test 3: StatisticalMetrics serializes to JSON for report export
    - Test 4: StatisticalMetrics excludes zero-reference cases from NMBE calculation
  </behavior>
  <action>Implement pub struct StatisticalMetrics with fields: pub nmbe: f64, pub cv_rmse: f64, pub nmbe_ci: (f64, f64), pub cv_rmse_ci: (f64, f64), pub cohens_d: f64, pub effect_direction: EffectDirection, pub excluded_cases: usize. Derive Debug, Clone, Serialize, Deserialize. Implement impl StatisticalMetrics with pub fn calculate(report: &BenchmarkReport) -> Self that:
1. Extract predicted values from report.results (fluxion_value)
2. Extract reference midpoints from (ref_min + ref_max) / 2.0
3. Filter out zero/near-zero references (abs(ref_midpoint) < 1e-10), count excluded
4. Calculate NMBE using calculate_nmbe() helper
5. Calculate CV(RMSE) using calculate_cv_rmse() helper
6. Calculate 95% CIs using calculate_ci_nmbe() and calculate_ci_cv_rmse()
7. Calculate Cohen's d using calculate_cohens_d()
8. Determine effect direction from Cohen's d sign
Add inline test with synthetic BenchmarkReport data verifying aggregation correctness.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>StatisticalMetrics struct aggregates all metrics, serializes, excludes zero-references, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Integrate with existing validation report structures</name>
  <files>src/validation/statistical.rs, src/validation/report.rs</files>
  <behavior>
    - Test 1: StatisticalMetrics imports from crate::validation::report compile successfully
    - Test 2: StatisticalMetrics can be added to BenchmarkReport without breaking existing code
    - Test 3: ValidationResult doesn't need modification (per-case stats handled separately)
  </behavior>
  <action>In src/validation/statistical.rs, add use crate::validation::report::{BenchmarkReport, ValidationResult, MetricType} imports. In src/validation/report.rs, ensure BenchmarkReport struct is compatible with StatisticalMetrics (no changes needed if using composition pattern). Add #[cfg(test)] module in statistical.rs with integration test creating BenchmarkReport and calling StatisticalMetrics::calculate(). Run `cargo test` to ensure no breaking changes to existing validation code.</action>
  <verify>cargo test</verify>
  <done>Statistical metrics integrate with existing report structures, no breaking changes, all tests pass</done>
</task>

</tasks>

<verification>
1. All statistical functions (NMBE, CV(RMSE), CI, BH, Cohen's d) pass unit tests
2. statrs dependency is correctly integrated
3. Zero-reference exclusion logic verified with test cases
4. Benjamini-Hochberg procedure correctly sorts p-values and applies threshold (k/m) * α
5. Confidence intervals use t-distribution for small samples (n < 30)
6. No breaking changes to existing ASHRAE140Validator or report structures
7. STATS-04 requirement addressed via BenjaminiHochberg::apply() implementation
</verification>

<success_criteria>
- Statistical module compiles with statrs dependency
- NMBE calculates signed percentage using reference midpoint
- CV(RMSE) calculates using RMSE / mean(reference) * 100
- 95% confidence intervals use t-distribution for small samples
- Benjamini-Hochberg correction (STATS-04) produces correct pass/fail per test with threshold formula (k/m) * α
- Cohen's d calculates effect size with pooled standard deviation
- All functions have comprehensive inline tests
- Integration with existing report structures verified
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistical-validation/19-01-SUMMARY.md`
</output>
