---
phase: 19-statistical-validation
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified: [src/validation/reporter.rs, src/validation/report.rs]
autonomous: true
requirements: [STATS-06]
user_setup: []

must_haves:
  truths:
    - Validation reports include new "Statistical Validation" section
    - Reports show NMBE, CV(RMSE), 95% CI for both metrics
    - Reports display BH-corrected p-values with annotations
    - Reports include Cohen's d and effect direction
    - Existing tolerance-based Pass/Warning/Fail system preserved
  artifacts:
    - path: src/validation/reporter.rs
      provides: Statistical report generation methods
      min_lines: 200
    - path: src/validation/report.rs
      provides: Extended report data structures
      min_lines: 50
    - path: docs/ASHRAE140_RESULTS.md
      provides: Example statistical validation section
      contains: "## Statistical Validation"
  key_links:
    - from: ValidationReportGenerator
      to: StatisticalMetrics
      via: "format statistical metrics in Markdown"
      pattern: "nmbe.*cv_rmse.*confidence_interval"
    - from: ValidationReportGenerator
      to: BenjaminiHochberg
      via: "display BH-corrected results"
      pattern: "benjamini.*hochberg.*corrected"
    - from: report generation
      to: existing tolerance system
      via: "preserve Pass/Warning/Fail status"
      pattern: "Pass.*Warning.*Fail"
---

<objective>
Extend validation report generation to include comprehensive statistical validation section with NMBE, CV(RMSE), confidence intervals, BH-corrected p-values, and effect sizes.

Purpose: Generate ASHRAE 140 Addendum B compliant reports with statistical metrics alongside existing tolerance-based validation, providing formal compliance evidence.

Output: Extended ValidationReportGenerator in src/validation/reporter.rs, updated report structures.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-statistical-validation/19-01-PLAN.md
@.planning/phases/19-statistical-validation/19-02-PLAN.md

@src/validation/reporter.rs
@src/validation/report.rs
@src/validation/statistical.rs

# From Plan 19-01: StatisticalMetrics, BenjaminiHochberg, CohenD implemented
# From Plan 19-02: StatisticalValidator, ValidationGroup, StatisticalReport implemented
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend report structures with statistical metrics fields</name>
  <files>src/validation/report.rs</files>
  <behavior>
    - Test 1: BenchmarkReport can hold optional StatisticalMetrics
    - Test 2: ValidationResult doesn't need modification (per-case stats separate)
    - Test 3: Report structures serialize to JSON/CSV with statistical fields
    - Test 4: Optional statistical fields preserve backward compatibility
  </behavior>
  <action>In src/validation/report.rs, add optional statistical fields to relevant structs:
- Add pub statistical_metrics: Option<StatisticalMetrics> to BenchmarkReport
- Ensure StatisticalMetrics is imported from crate::validation::statistical
- Add pub statistical_p_values: Option<Vec<f64>> and pub statistical_corrected: Option<Vec<bool>> to BenchmarkReport for per-case p-values and BH correction
- Add pub group_validation: Option<HashMap<ValidationGroup, bool>> to BenchmarkReport for group-level results
- Add #[serde(skip_serializing_if = "Option::is_none")] to all new optional fields
Add inline test creating BenchmarkReport with and without statistical fields, verify serialization works and backward compatibility preserved.</action>
  <verify>cargo test --lib validation::report</verify>
  <done>Report structures extended with statistical fields, optional fields preserve backward compatibility, serialization works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement statistical metrics formatting in ValidationReportGenerator</name>
  <files>src/validation/reporter.rs</files>
  <behavior>
    - Test 1: format_statistical_metrics() returns Markdown table with NMBE, CV(RMSE), CI
    - Test 2: format_statistical_metrics() includes Cohen's d and effect direction
    - Test 3: format_statistical_metrics() shows excluded cases count
    - Test 4: format_statistical_metrics() handles NaN values gracefully
  </behavior>
  <action>In src/validation/reporter.rs, add pub fn format_statistical_metrics(metrics: &StatisticalMetrics) -> String that generates Markdown table:
## Statistical Metrics
| Metric | Value | 95% CI Lower | 95% CI Upper |
|--------|-------|--------------|--------------|
| NMBE | X.XX% | Y.YY% | Z.ZZ% |
| CV(RMSE) | X.XX% | Y.YY% | Z.ZZ% |

**Effect Size (Cohen's d):** X.XX (EffectDirection)

**Excluded Cases:** N (zero/near-zero reference values)

Format CI as (lower, upper) with % sign. Handle NaN values by showing "N/A". Add inline test with sample StatisticalMetrics data verifying Markdown formatting matches expected structure.</action>
  <verify>cargo test --lib validation::reporter</verify>
  <done>Statistical metrics formatter implemented, generates Markdown table, handles NaN, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement BH correction formatting in reports</name>
  <files>src/validation/reporter.rs</files>
  <behavior>
    - Test 1: format_bh_correction() shows p-values and BH-corrected status
    - Test 2: format_bh_correction() annotates which tests pass BH correction
    - Test 3: format_bh_correction() groups by metric type
    - Test 4: format_bh_correction() uses per-group application per CONTEXT.md
  </behavior>
  <action>In src/validation/reporter.rs, add pub fn format_bh_correction(p_values: &[f64], corrected: &[bool], metric_types: &[MetricType]) -> String that generates Markdown section:
## Benjamini-Hochberg FDR Correction
| Metric | P-Value | BH Corrected |
|--------|----------|--------------|
| AnnualHeating | 0.0234 | ✅ |
| AnnualCooling | 0.089 | ❌ |

**Note:** Applied separately within each validation group (α = 0.05)

Use checkmark (✅) for corrected=true, cross (❌) for corrected=false. Group by metric type for clarity. Add inline test with synthetic p-values and corrected boolean array verifying Markdown formatting and BH annotation.</action>
  <verify>cargo test --lib validation::reporter</verify>
  <done>BH correction formatter implemented, shows p-values and status, grouped by metric, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement group validation formatting in reports</name>
  <files>src/validation/reporter.rs</files>
  <behavior>
    - Test 1: format_group_validation() shows PASS/FAIL status per group
    - Test 2: format_group_validation() shows passing rate for groups with ≥5 cases
    - Test 3: format_group_validation() shows case count for each group
    - Test 4: format_group_validation() handles empty groups gracefully
  </behavior>
  <action>In src/validation/reporter.rs, add pub fn format_group_validation(group_results: &HashMap<ValidationGroup, bool>, case_counts: &HashMap<ValidationGroup, usize>) -> String that generates Markdown section:
## Case Group Validation
| Group | Cases | Pass Rate | Status |
|-------|--------|-----------|--------|
| Baseline | 6 | 83.3% | PASS |
| HighMass | 7 | 57.1% | FAIL |

**Threshold:** 80% passing rate for groups with ≥5 cases, single-case validation for 1-4 cases

Use ✅ for PASS, ❌ for FAIL. Calculate pass rate as (passed / total) * 100. Handle case_counts for each group. Add inline test with synthetic group results verifying Markdown formatting and status calculation.</action>
  <verify>cargo test --lib validation::reporter</verify>
  <done>Group validation formatter implemented, shows PASS/FAIL status, pass rate, case count, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Integrate statistical sections into main report generation</name>
  <files>src/validation/reporter.rs</files>
  <behavior>
    - Test 1: generate_report() includes "## Statistical Validation" section when statistical_metrics present
    - Test 2: generate_report() preserves existing tolerance-based sections
    - Test 3: generate_report() maintains backward compatibility (no statistical metrics = no new section)
    - Test 4: Statistical section appears after tolerance results
  </behavior>
  <action>In src/validation/reporter.rs, extend impl ValidationReportGenerator with modifications to generate_report() or add new pub fn generate_report_with_statistics(&self, report: &BenchmarkReport) -> String:
1. Call existing generate_report() for tolerance-based sections (Pass/Warning/Fail)
2. Check if report.statistical_metrics.is_some()
3. If present, append new "## Statistical Validation" section with:
   - format_statistical_metrics(&metrics)
   - format_bh_correction(&p_values, &corrected, &metric_types)
   - format_group_validation(&group_validation, &case_counts)
4. Preserve all existing tolerance-based content (no deletions)
5. Use Option pattern to gracefully handle missing statistical data
Add inline test generating report with and without statistical fields, verify both formats work correctly.</action>
  <verify>cargo test --lib validation::reporter</verify>
  <done>Report generation integrated with statistical sections, backward compatibility preserved, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Export statistical metrics to CSV/JSON formats</name>
  <files>src/validation/reporter.rs</files>
  <behavior>
    - Test 1: export_statistical_csv() writes CSV with NMBE, CV(RMSE), CI columns
    - Test 2: export_statistical_json() writes JSON with statistical metrics
    - Test 3: Export formats align with ASHRAE Guideline 14 conventions
    - Test 4: Exports include metadata (alpha threshold, excluded cases count)
  </behavior>
  <action>In src/validation/reporter.rs, add export functions for statistical metrics:
- pub fn export_statistical_csv(report: &BenchmarkReport, path: &Path) -> Result<()> that writes CSV with columns: case_id, metric_type, predicted, reference_midpoint, nmbe, cv_rmse, ci_nmbe_lower, ci_nmbe_upper, ci_cvrmse_lower, ci_cvrmse_upper, p_value, bh_corrected, cohens_d
- pub fn export_statistical_json(report: &BenchmarkReport, path: &Path) -> Result<()> that writes JSON with nested structure: { statistical_metrics: {...}, group_validation: {...}, metadata: { alpha, excluded_cases } }
- Use existing CSV/JSON infrastructure from reporter.rs as template
- Add inline test writing to temporary file and verifying output format matches expected schema</action>
  <verify>cargo test --lib validation::reporter</verify>
  <done>Statistical metrics export to CSV/JSON implemented, formats follow conventions, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Generate example statistical validation report</name>
  <files>docs/ASHRAE140_RESULTS.md</files>
  <behavior>
    - Test 1: Example report shows "## Statistical Validation" section
    - Test 2: Example includes NMBE, CV(RMSE), CI tables
    - Test 3: Example shows BH correction and group validation
    - Test 4: Example demonstrates formatting with synthetic data
  </behavior>
  <action>In docs/ASHRAE140_RESULTS.md (or create new example file docs/ASHRAE140_STATISTICAL_EXAMPLE.md), add example statistical validation section:
## Statistical Validation Example

### Statistical Metrics
[Example table with NMBE = 2.3%, CV(RMSE) = 8.7%, 95% CIs]

### Benjamini-Hochberg FDR Correction
[Example table showing p-values and BH-corrected status]

### Case Group Validation
[Example table showing PASS/FAIL per group]

Use synthetic data demonstrating realistic validation scenario (e.g., Baseline group 5/6 passing, HighMass 4/7 failing). Ensure formatting matches actual report generation code. This provides reference for users and developers.</action>
  <verify>grep "## Statistical Validation" docs/ASHRAE140_RESULTS.md || grep "## Statistical Validation Example" docs/ASHRAE140_STATISTICAL_EXAMPLE.md</verify>
  <done>Example statistical report generated, demonstrates all sections, formatting matches production code</done>
</task>

</tasks>

<verification>
1. Report structures include optional statistical fields
2. Statistical metrics formatter generates correct Markdown tables
3. BH correction formatter shows p-values and corrected status
4. Group validation formatter displays PASS/FAIL per group
5. Main report generation integrates statistical sections
6. CSV/JSON export formats include statistical metrics
7. Example report demonstrates statistical validation section
8. Backward compatibility with tolerance-based reports preserved
</verification>

<success_criteria>
- Validation reports include "## Statistical Validation" section
- Reports display NMBE, CV(RMSE), 95% CIs
- Reports show BH-corrected p-values with annotations
- Reports include Cohen's d and effect direction
- Reports show case group validation results
- CSV/JSON exports include statistical metrics
- Example report demonstrates all statistical sections
- Existing tolerance-based Pass/Warning/Fail system unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistical-validation/19-03-SUMMARY.md`
</output>
