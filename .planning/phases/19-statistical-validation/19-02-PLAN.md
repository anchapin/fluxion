---
phase: 19-statistical-validation
plan: 02
type: execute
wave: 1
depends_on: [19-01]
files_modified: [src/validation/statistical.rs, src/validation/ashrae_140_validator.rs]
autonomous: true
requirements: [STATS-01, STATS-05]
user_setup: []

must_haves:
  truths:
    - StatisticalValidator wraps ASHRAE140Validator and provides statistical validation path
    - Case groups are defined (baseline, high-mass, diagnostics, equipment) per Claude's discretion
    - Group validation enforces 80% passing rate for groups with ≥5 cases
    - Single-case validation applies to groups with 1-4 cases
    - FDR correction applied separately within each validation group
  artifacts:
    - path: src/validation/statistical.rs
      provides: StatisticalValidator struct
      min_lines: 150
    - path: src/validation/ashrae_140_validator.rs
      provides: Statistical validation integration hooks
      min_lines: 50
    - path: src/validation/statistical.rs
      provides: ValidationGroup enum and group validation logic
      min_lines: 100
  key_links:
    - from: StatisticalValidator
      to: ASHRAE140Validator
      via: "base_validator field wrapping existing validator"
      pattern: "ASHRAE140Validator::with_full_diagnostics()"
    - from: StatisticalValidator::validate_groups()
      to: BenjaminiHochberg
      via: "apply FDR correction per group"
      pattern: "BenjaminiHochberg::apply()"
    - from: ValidationGroup::Baseline
      to: ASHRAE140Case
      via: "case_id mapping (600, 610, 620, 630, 640, 650)"
      pattern: "case_id.starts_with(\"6\")"
---

<objective>
Implement StatisticalValidator wrapper with case group validation and hybrid threshold enforcement (80% for ≥5 cases, single-case for <5).

Purpose: Provide parallel statistical validation path wrapping existing ASHRAE140Validator, enabling Addendum B compliance with group-level passing rates and per-group FDR correction.

Output: StatisticalValidator struct in src/validation/statistical.rs, ValidationGroup enum, group validation logic.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-statistical-validation/19-01-PLAN.md

@src/validation/statistical.rs
@src/validation/ashrae_140_validator.rs
@src/validation/report.rs

# External: statrs 0.18.0 (added in Plan 19-01)
# Provides: Statistical functions already integrated
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ValidationGroup enum with case membership rules</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: ValidationGroup::Baseline includes Cases 600, 610, 620, 630, 640, 650
    - Test 2: ValidationGroup::HighMass includes Cases 900, 910, 920, 930, 940, 950, 960
    - Test 3: ValidationGroup::FreeFloating includes Cases 600FF, 650FF, 900FF, 950FF
    - Test 4: ValidationGroup::Diagnostics includes Cases 195-470 (if implemented)
    - Test 5: ValidationGroup::Equipment includes Cases 800-810
  </behavior>
  <action>Define pub enum ValidationGroup with variants: Baseline, HighMass, FreeFloating, Diagnostics, Equipment. Implement impl ValidationGroup with pub fn from_case_id(case_id: &str) -> Option<Self> using string matching patterns:
- Baseline: case_id starts with "6" and doesn't contain "FF" (600, 610, 620, 630, 640, 650)
- HighMass: case_id starts with "9" and doesn't contain "FF" (900, 910, 920, 930, 940, 950, 960)
- FreeFloating: case_id contains "FF" (600FF, 650FF, 900FF, 950FF)
- Diagnostics: case_id is "195" or range 196-470 (check if numeric 196-470)
- Equipment: case_id starts with "8" (800, 801, 802, 803, 804, 805, 806, 807, 808, 809, 810)
Add inline tests verifying case_id mapping correctness. This implements Claude's discretion on grouping (case type/range approach).</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>ValidationGroup enum defined, case membership rules implemented, tests verify mapping</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hybrid threshold group validation logic</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: validate_group_80_percent() returns true if ≥80% of cases pass tolerance
    - Test 2: validate_group_80_percent() returns false if <80% of cases pass tolerance
    - Test 3: validate_group_single_case() returns true if all cases pass (1-4 cases)
    - Test 4: validate_group_single_case() returns false if any case fails (1-4 cases)
    - Test 5: validate_group_hybrid() applies 80% threshold for ≥5 cases, single-case for <5
  </behavior>
  <action>Implement helper functions in src/validation/statistical.rs:
- pub fn validate_group_80_percent(passed: usize, total: usize) -> bool: returns (passed as f64 / total as f64) >= 0.8
- pub fn validate_group_single_case(all_passed: bool) -> bool: returns all_passed (binary check)
- pub fn validate_group_hybrid(passed: usize, total: usize) -> bool:
  - If total >= 5: apply 80% threshold via validate_group_80_percent()
  - If 1 <= total < 5: apply single-case validation via validate_group_single_case() (all must pass)
  - If total == 0: return false (no cases to validate)
Add inline tests with various passed/total combinations verifying hybrid threshold behavior. This implements CONTEXT.md locked decision on hybrid threshold approach.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>Hybrid threshold functions implemented, 80% for ≥5 cases, single-case for <5, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement group-level validation with FDR correction per group</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: validate_groups() partitions BenchmarkReport by ValidationGroup
    - Test 2: validate_groups() applies Benjamini-Hochberg separately per group
    - Test 3: validate_groups() returns HashMap<ValidationGroup, bool> with PASS/FAIL results
    - Test 4: validate_groups() handles empty groups gracefully (returns false)
    - Test 5: validate_groups() groups cases correctly using ValidationGroup::from_case_id()
  </behavior>
  <action>Implement pub fn validate_groups(report: &BenchmarkReport, alpha: f64) -> HashMap<ValidationGroup, bool> in src/validation/statistical.rs:
1. Partition report.results by ValidationGroup using ValidationGroup::from_case_id(case_id)
2. For each group:
   - Calculate per-case p-values from tolerance deviation using one-sample t-test: For each ValidationResult, compute t = (fluxion_value - reference_midpoint) / (reference_std / sqrt(1)), then p = 2 * (1 - StudentsT::cdf(abs(t), df=reference_count-1))
   - Apply BenjaminiHochberg::apply(p_values, alpha) separately per group (CONTEXT.md decision)
   - Count cases passing BH correction
   - Apply hybrid threshold (80% for ≥5, single-case for <5)
   - Store result in HashMap
3. Return HashMap mapping ValidationGroup to boolean (true = PASS group validation)
P-value calculation rationale: One-sample t-test compares Fluxion prediction against reference midpoint distribution. Use statrs::distribution::StudentsT for CDF calculation. Add inline test with synthetic BenchmarkReport containing cases from multiple groups, verify partitioning and threshold application.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>Group validation implemented, FDR applied per group, hybrid threshold enforced, p-values calculated via one-sample t-test, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement StatisticalValidator struct wrapping ASHRAE140Validator</name>
  <files>src/validation/statistical.rs</files>
  <behavior>
    - Test 1: StatisticalValidator::new() creates validator with alpha=0.05
    - Test 2: StatisticalValidator::with_alpha() creates validator with custom alpha
    - Test 3: StatisticalValidator wraps ASHRAE140Validator::with_full_diagnostics()
    - Test 4: StatisticalValidator validates cases using base validator
  </behavior>
  <action>Implement pub struct StatisticalValidator in src/validation/statistical.rs with fields: pub base_validator: ASHRAE140Validator, pub alpha: f64. impl StatisticalValidator with:
- pub fn new() -> Self: creates base_validator via ASHRAE140Validator::with_full_diagnostics(), sets alpha=0.05 (CONTEXT.md decision)
- pub fn with_alpha(alpha: f64) -> Self: allows custom alpha threshold
- pub fn validate_case(&self, case_id: &str) -> Result<BenchmarkReport>: delegates to base_validator.validate_case()
- pub fn validate_all(&self, cases: &[&str]) -> Result<BenchmarkReport>: delegates to base_validator.validate_all_cases()
Add inline tests creating StatisticalValidator and calling validate_case() with known case IDs (e.g., "600"), verify report generation works.</action>
  <verify>cargo test --lib validation::statistical</verify>
  <done>StatisticalValidator struct implemented, wraps ASHRAE140Validator, delegates validation, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Integrate StatisticalValidator with existing validation workflow</name>
  <files>src/validation/statistical.rs, src/validation/ashrae_140_validator.rs</files>
  <behavior>
    - Test 1: StatisticalValidator can be imported and used alongside ASHRAE140Validator
    - Test 2: StatisticalValidator.validate_with_statistics() returns StatisticalReport
    - Test 3: StatisticalReport contains tolerance report, metrics, group validation
    - Test 4: Existing ASHRAE140Validator API unchanged (backward compatibility)
  </behavior>
  <action>In src/validation/statistical.rs, define pub struct StatisticalReport with fields: pub tolerance: BenchmarkReport, pub metrics: StatisticalMetrics, pub corrected_p_values: Vec<bool>, pub group_validation: HashMap<ValidationGroup, bool>. Add impl StatisticalValidator with pub fn validate_with_statistics(&self, cases: &[&str]) -> Result<StatisticalReport> that:
1. Calls self.validate_all(cases) to get tolerance report
2. Calculates StatisticalMetrics::calculate(&tolerance_report)
3. Applies BenjaminiHochberg::apply(p_values, self.alpha) per metric type
4. Calls validate_groups(&tolerance_report, self.alpha) for group validation
5. Returns StatisticalReport aggregating all results
In src/validation/ashrae_140_validator.rs, ensure no breaking changes to existing API (no modifications needed if using composition). Add inline test in statistical.rs creating StatisticalValidator, calling validate_with_statistics(["600", "900"]), verifying StatisticalReport structure.</action>
  <verify>cargo test</verify>
  <done>StatisticalValidator integrated, StatisticalReport struct defined, backward compatibility preserved, all tests pass</done>
</task>

</tasks>

<verification>
1. ValidationGroup enum correctly maps case IDs to groups
2. Hybrid threshold logic applies 80% for ≥5 cases, single-case for <5
3. Benjamini-Hochberg correction applied separately per validation group
4. StatisticalValidator wraps ASHRAE140Validator without breaking changes
5. validate_with_statistics() returns comprehensive StatisticalReport
6. All group validation logic tested with synthetic data
7. P-values calculated using one-sample t-test (no placeholder)
</verification>

<success_criteria>
- ValidationGroup enum implements case type/range grouping per Claude's discretion
- Group validation enforces hybrid threshold (80% for ≥5, single-case for <5)
- FDR correction applied independently per validation group
- StatisticalValidator provides parallel statistical validation path
- Backward compatibility with ASHRAE140Validator maintained
- All functions have comprehensive inline tests
- P-values calculated via one-sample t-test using statrs StudentsT distribution
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistical-validation/19-02-SUMMARY.md`
</output>
