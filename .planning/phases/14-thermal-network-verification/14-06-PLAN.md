---
phase: 14-thermal-network-verification
plan: 06
type: execute
wave: 1
depends_on: [14-01]
files_modified: [src/ai/surrogate.rs, tests/test_surrogate.rs]
autonomous: true
requirements: [PHYS-01]
gap_closure: true

must_haves:
  truths:
    - "All mock predictions in SurrogateManager are removed and replaced with proper error handling"
    - "SurrogateManager panics with descriptive messages when use_ai=true and no ONNX model loaded"
    - "Tests expect panic/error behavior instead of mock predictions"
  artifacts:
    - path: "src/ai/surrogate.rs"
      provides: "SurrogateManager with proper error handling, no mock predictions"
      contains: "panic!(\"SurrogateManager requires ONNX model to be loaded"
    - path: "tests/test_surrogate.rs"
      provides: "Updated tests expecting panic/error behavior"
      exports: ["test_predict_loads_no_model_panics", "test_predict_loads_batched_no_model_panics"]
  key_links:
    - from: "src/ai/surrogate.rs::predict_loads"
      to: "Panic with descriptive error"
      via: "model_loaded flag check"
      pattern: "if !self.model_loaded \\{ panic!"
    - from: "src/ai/surrogate.rs::predict_loads_batched"
      to: "Panic with descriptive error"
      via: "model_loaded flag check"
      pattern: "if !self.model_loaded || batch_temps.is_empty() \\{ panic!"
---

<objective>
Remove all remaining mock predictions from SurrogateManager to fully satisfy PHYS-01 requirement.

Purpose: PHYS-01 requires complete removal of mock predictions (vec![1.2; ...]) from SurrogateManager, not just analytical path fix. The 13 mock fallbacks in src/ai/surrogate.rs must be replaced with proper error handling (panic with descriptive messages) to prevent silent fallback behavior when use_ai=true and no ONNX model is loaded.

Output: SurrogateManager with proper error handling, updated tests expecting panic behavior, PHYS-01 fully satisfied.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-thermal-network-verification/14-CONTEXT.md
@.planning/phases/14-thermal-network-verification/14-RESEARCH.md
@.planning/phases/14-thermal-network-verification/14-VERIFICATION.md
@.planning/phases/14-thermal-network-verification/14-01-SUMMARY.md

# Existing Implementation Context

From 14-01-SUMMARY.md:
- Plan 14-01 implemented calculate_analytical_loads() and integrated it into solve_timesteps when use_ai=false
- Analytical physics path works correctly and does not use mock predictions
- However, Plan 14-01 did not address the fallback behavior in SurrogateManager when use_ai=true and no ONNX model loaded

From 14-CONTEXT.md (User Decisions):
**Locked Decision: Mock Data Replacement Strategy**
- Approach: Analytical physics calculations (not training ONNX models for this phase)
- Integration: Delegate to physics engine - ThermalModel::solve_timesteps() calculates loads directly when use_ai=false
- Remove SurrogateManager parameter from solve_timesteps() call when using analytical path
- Simpler call chain: ThermalModel handles its own physics without surrogate abstraction layer

**Key Decision for Gap Closure:**
The user decision states "remove all mock predictions" and PHYS-01 explicitly requires complete removal. The proper error handling strategy is to panic with descriptive messages when use_ai=true and no ONNX model is loaded, rather than silently returning mock values.

From src/ai/surrogate.rs (Mock Prediction Locations):
- Lines 780, 792, 811, 815, 821, 826, 863, 871, 887, 910, 914, 920, 925
- All used as fallback when ONNX inference fails or no model is loaded
- Pattern: return vec![1.2; current_temps.len()] or return vec![1.2; n_input]

From CLAUDE.md (SurrogateManager Design):
- Key Methods: predict_loads(current_temps), predict_loads_batched(batch_temps)
- Role: Replaces expensive CFD/ray-tracing with pre-trained neural networks
- Current behavior: new() returns mock predictions when no model loaded
- Batched Inference: SessionPool enables concurrent inference for maximum throughput
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove mock predictions from predict_loads method</name>
  <files>src/ai/surrogate.rs</files>
  <action>
    Replace all mock prediction fallbacks in SurrogateManager::predict_loads() method (lines 771-828) with panic!() calls that provide descriptive error messages.

    Specific changes:
    1. Line 780: Replace `return vec![1.2; current_temps.len()];` with `panic!("SurrogateManager requires ONNX model to be loaded. Call load_onnx() or with_gpu_backend() before calling predict_loads(). Current state: model_loaded = false");`
    2. Line 792: Replace `return vec![1.2; n_input];` with `panic!("Failed to create input tensor: {}. SurrogateManager requires ONNX model to be loaded and working. Error: {}", e, e);`
    3. Line 811: Replace `vec![1.2; current_temps.len()]` with `panic!("ONNX inference returned no outputs. SurrogateManager requires valid ONNX model output.");`
    4. Line 815: Replace `vec![1.2; current_temps.len()]` with `panic!("ONNX inference error: {}. SurrogateManager requires working ONNX model.", e);`
    5. Line 821: Replace `vec![1.2; current_temps.len()]` with `panic!("Could not acquire ORT session: {}. SurrogateManager requires valid SessionPool.", e);`
    6. Line 826: Replace `vec![1.2; current_temps.len()]` with `panic!("No session pool available. SurrogateManager requires valid SessionPool. Call load_onnx() or with_gpu_backend() first.");`

    Rationale:
    - PHYS-01 explicitly requires "remove all mock predictions" - not just fix analytical path
    - Silent fallback to mock values masks configuration errors and violates data integrity
    - Panic is appropriate for unrecoverable state (no model loaded, inference failures)
    - Descriptive messages guide users to proper API usage (load_onnx(), with_gpu_backend())

    Reference existing code pattern:
    - Line 778-781: Check model_loaded flag before attempting inference
    - Lines 782-823: Multiple fallback paths that return mock predictions
    - All mock returns must be replaced with panic!() calls
  </action>
  <verify>
    <automated>grep -n "vec!\[1\.2;" src/ai/surrogate.rs | head -20</automated>
  </verify>
  <done>All mock predictions (vec![1.2; ...]) removed from predict_loads method, replaced with panic!() calls with descriptive error messages. Grep confirms no remaining mock returns in predict_loads.</done>
</task>

<task type="auto">
  <name>Task 2: Remove mock predictions from predict_loads_batched method</name>
  <files>src/ai/surrogate.rs</files>
  <action>
    Replace all mock prediction fallbacks in SurrogateManager::predict_loads_batched() method (lines 852-927) with panic!() calls that provide descriptive error messages.

    Specific changes:
    1. Line 863: Replace `return batch_temps.iter().map(|t| vec![1.2; t.len()]).collect();` with `panic!("SurrogateManager requires ONNX model to be loaded for batched inference. Call load_onnx() or with_gpu_backend() before calling predict_loads_batched(). Current state: model_loaded = false");`
    2. Line 871: Replace `return batch_temps.iter().map(|t| vec![1.2; t.len()]).collect();` with `panic!("Inconsistent input sizes in batch: expected {} elements per config, found varying sizes. Batched inference requires consistent input dimensions.", input_size);`
    3. Line 887: Replace `return batch_temps.iter().map(|t| vec![1.2; t.len()]).collect();` with `panic!("Failed to create input tensor: {}. SurrogateManager requires valid ONNX model for batched inference. Error: {}", e, e);`
    4. Line 910: Replace `batch_temps.iter().map(|t| vec![1.2; t.len()]).collect()` with `panic!("ONNX inference returned no outputs for batch. SurrogateManager requires valid ONNX model output.");`
    5. Line 914: Replace `batch_temps.iter().map(|t| vec![1.2; t.len()]).collect()` with `panic!("ONNX inference error: {}. SurrogateManager requires working ONNX model for batched inference.", e);`
    6. Line 920: Replace `batch_temps.iter().map(|t| vec![1.2; t.len()]).collect()` with `panic!("Could not acquire ORT session: {}. SurrogateManager requires valid SessionPool for batched inference.", e);`
    7. Line 925: Replace `batch_temps.iter().map(|t| vec![1.2; t.len()]).collect()` with `panic!("No session pool available. SurrogateManager requires valid SessionPool for batched inference. Call load_onnx() or with_gpu_backend() first.");`

    Rationale:
    - Same approach as Task 1: PHYS-01 requires complete removal of mock predictions
    - Batched inference has same fallback behavior issues as single prediction
    - Panic is appropriate for unrecoverable state in batch operations
    - Descriptive messages guide users to proper API usage for batch operations

    Reference existing code pattern:
    - Line 862: Check model_loaded flag before attempting batched inference
    - Lines 865-926: Multiple fallback paths that return mock predictions
    - All mock returns must be replaced with panic!() calls
  </action>
  <verify>
    <automated>grep -n "vec!\[1\.2;" src/ai/surrogate.rs</automated>
  </verify>
  <done>All mock predictions (vec![1.2; ...]) removed from predict_loads_batched method, replaced with panic!() calls with descriptive error messages. Grep confirms no remaining mock returns in entire surrogate.rs file.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests to expect panic behavior</name>
  <files>tests/test_surrogate.rs</files>
  <action>
    Update SurrogateManager tests to expect panic!() behavior instead of mock predictions when no ONNX model is loaded.

    Analysis: Identify tests that call SurrogateManager::new() without loading a model and then call predict_loads() or predict_loads_batched(). These tests will now panic and must be updated.

    Required changes:
    1. Add #[should_panic] attribute to tests that intentionally test no-model behavior:
       - Find tests using SurrogateManager::new() without load_onnx()
       - Add #[should_panic(expected = "requires ONNX model")] to expect panic message

    2. Create new explicit panic tests (if not already present):
       - test_predict_loads_no_model_panics: Create SurrogateManager without model, call predict_loads(), expect panic with "requires ONNX model"
       - test_predict_loads_batched_no_model_panics: Create SurrogateManager without model, call predict_loads_batched(), expect panic with "requires ONNX model"

    3. Update docstring examples (if any) that show mock behavior:
       - Document that SurrogateManager requires load_onnx() or with_gpu_backend() before inference
       - Remove examples showing mock predictions returned when no model loaded

    Reference existing test patterns from CLAUDE.md:
    - "Mock tests (no model): Use SurrogateManager::new() which returns vec![1.2; ...]" - this pattern must change
    - Tests should now expect panic instead of mock returns
    - Use #[should_panic] attribute for intentional panic tests

    Safety check: Run cargo test --test test_surrogate to identify failing tests after Tasks 1-2, then update accordingly.
  </action>
  <verify>
    <automated>cargo test --test test_surrogate -- --nocapture</automated>
  </verify>
  <done>All SurrogateManager tests updated to expect panic!() behavior when no ONNX model is loaded. Tests pass with #[should_panic] attributes or skip no-model scenarios. Documentation updated to reflect error handling strategy.</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. **Mock predictions removed:**
   ```bash
   grep -n "vec!\[1\.2;" src/ai/surrogate.rs
   ```
   Expected: No output (0 matches)

2. **Panic behavior confirmed:**
   ```bash
   cargo test --test test_surrogate
   ```
   Expected: All tests pass, panic tests have #[should_panic] attributes

3. **Compilation check:**
   ```bash
   cargo check
   ```
   Expected: No errors, no warnings

4. **Analytical path still works:**
   ```bash
   cargo test test_energy_conservation --lib
   ```
   Expected: Passes (use_ai=false path unaffected)

5. **PHYS-01 satisfaction:**
   Verify all 13 mock prediction locations (lines 780, 792, 811, 815, 821, 826, 863, 871, 887, 910, 914, 920, 925) are replaced with panic!() calls.
</verification>

<success_criteria>
- All 13 instances of `vec![1.2; ...]` mock predictions removed from src/ai/surrogate.rs
- All fallback paths in predict_loads() and predict_loads_batched() replaced with panic!() calls
- Descriptive error messages guide users to proper API usage (load_onnx(), with_gpu_backend())
- All SurrogateManager tests updated to expect panic behavior
- Analytical physics path (use_ai=false) still works correctly
- PHYS-01 requirement fully satisfied (complete removal of mock predictions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-thermal-network-verification/14-06-SUMMARY.md`

Summary should include:
- Tasks completed (3 tasks)
- Files modified (src/ai/surrogate.rs, tests/test_surrogate.rs)
- Mock predictions removed (13 instances → 0 instances)
- Test updates (#[should_panic] attributes, new panic tests)
- PHYS-01 verification (grep confirms no vec![1.2; ...] remains)
</output>
