---
phase: 13-Documentation-and-Tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/tutorials/extending_fluxion.md
  - examples/tutorial_custom_model.rs
autonomous: true
requirements:
  - DOC-03
  - DOC-04
must_haves:
  truths:
    - Tutorial "Extending Fluxion with custom thermal models" published
    - Parameter vector semantics and design variable bounds documented
  artifacts:
    - path: docs/tutorials/extending_fluxion.md
      provides: Start-to-finish tutorial
      contains: Thermal model structure, extension points, integration
    - path: examples/tutorial_custom_model.rs
      provides: Complete working example
      contains: Custom thermal model implementation
  key_links:
    - from: docs/tutorials/extending_fluxion.md
      to: docs/API_REFERENCE.md
      via: API reference
      pattern: "See API_REFERENCE.md"
    - from: examples/tutorial_custom_model.rs
      to: src/sim/engine.rs
      via: ThermalModel extension
      pattern: "ThermalModel.*trait"
---

<objective>
Create tutorial "Extending Fluxion with custom thermal models" and document parameter vector semantics.

Purpose: Provide complete guide for developers extending Fluxion with custom thermal models.
Output: Tutorial markdown file and working example code
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-Documentation-and-Tools/13-CONTEXT.md

@docs/ARCHITECTURE.md
@examples/validate_6r2c.rs
@examples/construction_example.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tutorial "Extending Fluxion with custom thermal models"</name>
  <files>docs/tutorials/extending_fluxion.md</files>
  <action>
Create docs/tutorials/extending_fluxion.md with start-to-finish walkthrough:

1. **Tutorial structure**:
   - Introduction (BatchOracle vs Model, when to use each)
   - Thermal model overview (5R1C network, zones, conductances)
   - Implementing custom thermal model (step-by-step)
   - Integration with SimulationDiagnostics
   - Complete working example

2. **Section 1: Project overview**:
   - BatchOracle: High-throughput parallel evaluation for optimization
   - Model: Single-building detailed analysis for validation
   - When to use BatchOracle (population evaluation, quantum/GA workflows)
   - When to use Model (ASHRAE validation, hourly traces, debugging)

3. **Section 2: Thermal model structure**:
   - Zones and zone count (num_zones)
   - 5R1C thermal network components:
     * Temperature nodes (temperatures: VectorField)
     * Mass temperatures (mass_temperatures: VectorField)
     * Thermal loads (loads: VectorField)
   - Conductances (h_tr_em, h_tr_ms, h_tr_is, h_tr_w, h_ve)
   - Design variables (window_u_value, hvac_setpoint)

4. **Section 3: Implementing heat_transfer() method**:
   - Method signature: `fn heat_transfer(&self) -> VectorField`
   - Solar gain integration
   - Inter-zone heat transfer (for multi-zone models)
   - Ventilation heat transfer
   - Return: Total heat transfer per zone (Watts)

5. **Section 4: Integration with SimulationDiagnostics**:
   - Diagnostic data collection (hourly temperatures, loads, HVAC demand)
   - CSV export for analysis
   - Peak load tracking
   - Free-floating temperature calculation

6. **Section 5: Complete working example**:
   - Full code example: Custom building simulation
   - Compare against Case 600 baseline
   - Error handling (validate_parameters, Result types)
   - Testing with BatchOracle and Model

7. **Requirements satisfied**:
   - DOC-03: Tutorial "Extending Fluxion with custom thermal models" published

Follow CONTRIBUTING.md patterns (clear sections, code blocks with syntax highlighting, cross-references).
  </action>
  <verify>
    <automated>grep -q "BatchOracle vs Model" docs/tutorials/extending_fluxion.md && grep -q "heat_transfer" docs/tutorials/extending_fluxion.md && grep -q "SimulationDiagnostics" docs/tutorials/extending_fluxion.md</automated>
  </verify>
  <done>Tutorial markdown file created with complete walkthrough and code examples</done>
</task>

<task type="auto">
  <name>Task 2: Create complete working example tutorial_custom_model.rs</name>
  <files>examples/tutorial_custom_model.rs</files>
  <action>
Create examples/tutorial_custom_model.rs with complete working thermal model extension example:

1. **Example structure**:
   - Main function with error handling
   - Custom thermal model implementation
   - Simulation with BatchOracle and Model
   - Comparison against Case 600 baseline

2. **Custom thermal model**:
   - struct CustomThermalModel
   - Implement heat_transfer() method
   - Solar gain integration
   - Ventilation calculation
   - HVAC demand calculation

3. **BatchOracle usage**:
   - Define population (parameter vectors)
   - Evaluate with evaluate_population()
   - Collect results and analyze

4. **Model usage**:
   - Single configuration simulation
   - Detailed hourly traces
   - Peak load tracking
   - Energy consumption breakdown

5. **Error handling**:
   - validate_parameters() for bounds checking
   - NaN/Inf detection
   - Result type propagation

6. **Requirements satisfied**:
   - DOC-03: Tutorial includes complete working example

Follow examples/validate_6r2c.rs pattern (modular code, clear comments, testable).
  </action>
  <verify>
    <automated>grep -q "fn main" examples/tutorial_custom_model.rs && grep -q "BatchOracle" examples/tutorial_custom_model.rs && grep -q "Model" examples/tutorial_custom_model.rs</automated>
  </verify>
  <done>Complete working example created and compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Document parameter vector semantics and design variable bounds</name>
  <files>docs/CONTRIBUTING.md</files>
  <action>
Add parameter vector semantics documentation to docs/CONTRIBUTING.md:

1. **New section: Parameter Vector Semantics**:
   - Population format (critical for external APIs)
   - Element mapping:
     * Element 0: Window U-value (range: 0.1–5.0 W/m²K)
     * Element 1: HVAC setpoint (range: 15–30°C)
     * Element 2+: Future design variables (thermal mass, infiltration rates, etc.)
   - Design variable bounds:
     * MIN_U_VALUE constant (0.1 W/m²K)
     * MAX_U_VALUE constant (5.0 W/m²K)
     * MIN_SETPOINT constant (15°C)
     * MAX_SETPOINT constant (30°C)
   - validate_parameters() enforcement (returns NaN for invalid configs)

2. **Design variable documentation**:
   - Window U-value: thermal transmittance of glazing
   - HVAC setpoint: target indoor temperature
   - Thermal mass: thermal capacitance of building materials (future)
   - Infiltration rate: air exchange rate with outdoors (future)

3. **API integration**:
   - get_parameter_bounds() method for programmatic access
   - ParameterBounds struct with get_bounds() static method
   - validate_parameters() Python method

4. **Requirements satisfied**:
   - DOC-04: Parameter vector semantics and design variable bounds documented

Cross-reference to CLAUDE.md (Parameter Vector Semantics section).
  </action>
  <verify>
    <automated>grep -q "Parameter Vector Semantics" docs/CONTRIBUTING.md && grep -q "Element 0: Window U-value" docs/CONTRIBUTING.md && grep -q "validate_parameters" docs/CONTRIBUTING.md</automated>
  </verify>
  <done>Parameter vector semantics and design variable bounds documented in CONTRIBUTING.md</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Tutorial markdown file exists with complete walkthrough
2. Working example compiles and runs successfully
3. Parameter vector semantics documented in CONTRIBUTING.md
4. Cross-references between tutorial and API reference
5. Code examples follow established patterns
</verification>

<success_criteria>
- DOC-03: Tutorial "Extending Fluxion with custom thermal models" published ✅
- DOC-04: Parameter vector semantics and design variable bounds documented ✅
</success_criteria>

<output>
After completion, create `.planning/phases/13-Documentation-and-Tools/13-02-SUMMARY.md`
</output>
