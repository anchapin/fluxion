---
phase: 40-case-expansion-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validation/reference/mod.rs
  - src/validation/reference/series_800.csv
  - src/validation/reference/series_195.csv
  - data/reference/ashrae140/
autonomous: true
requirements:
  - CASE-03

must_haves:
  truths:
    - "User can access extended reference database for new cases"
  artifacts:
    - path: "src/validation/reference/mod.rs"
      provides: "Reference data loading for expanded cases"
      min_lines: 80
      contains: "load_series_800_reference, load_series_195_reference"
    - path: "src/validation/reference/series_800.csv"
      provides: "Reference data for Cases 800-810"
      min_lines: 100
      contains: "Case800,Case801,Case802,Case803,Case804,Case805,Case806,Case807,Case808,Case809,Case810"
    - path: "src/validation/reference/series_195.csv"
      provides: "Reference data for Cases 195-470"
      min_lines: 500
      contains: "Case195,Case196,Case197,Case198,Case199,Case200"
    - path: "data/reference/ashrae140/"
      provides: "Organized reference data directory"
      is_directory: true
      contains: "series_800.csv, series_195.csv"
  key_links:
    - from: "src/validation/reference/mod.rs"
      to: "src/validation/reference/series_800.csv"
      via: "CSV parsing and loading"
      pattern: "csv::Reader::from_path.*series_800.csv"
    - from: "src/validation/reference/mod.rs"
      to: "src/validation/reference/series_195.csv"
      via: "CSV parsing and loading"
      pattern: "csv::Reader::from_path.*series_195.csv"
    - from: "src/validation/ashrae140/cases/mod.rs"
      to: "src/validation/reference/mod.rs"
      via: "Reference data lookup"
      pattern: "reference::load_reference_data"
---

<objective>
Implement Extended Reference Database for New ASHRAE 140 Cases

Purpose: Create and integrate reference data for Cases 800-810 and 195-470 to enable validation against expected results. This provides the baseline for determining validation success/failure.

Output:
- Reference data loading module with CSV parsing
- CSV files containing reference values for all new cases
- Organized data directory structure
- Integration with validation framework
</objective>

<execution_context>
@/home/alex/.agents/get-shit-done/workflows/execute-plan.md
@/home/alex/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP_v1.1.md
@.planning/STATE.md
@.planning/phases/40-case-expansion-foundation/40-CONTEXT.md
@.planning/phases/40-case-expansion-foundation/40-RESEARCH.md

# Reference existing reference data structure
@src/validation/reference/mod.rs
@data/reference/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Reference Data Directory Structure</name>
  <files>data/reference/ashrae140/</files>
  <read_first>
    - data/reference/ (existing structure)
    - src/validation/reference/mod.rs (current reference loading)
  </read_first>
  <action>
    Create organized directory structure for ASHRAE 140 reference data:

    ```bash
    mkdir -p data/reference/ashrae140
    ```

    Ensure directory follows existing Fluxion conventions:
    - Lowercase names with underscores
    - Clear separation by case series
    - Proper file permissions (readable by build system)

    Create README.md in data/reference/ashrae140/ documenting:
    - Data format specification
    - Source of reference data (ASHRAE 140-2017)
    - Column definitions
    - Usage instructions
  </action>
  <verify>
    <automated>test -d data/reference/ashrae140</automated>
    <automated>test -f data/reference/ashrae140/README.md</automated>
    <automated>ls -la data/reference/ashrae140/ | grep -c "README.md"</automated>
  </verify>
  <done>
    - data/reference/ashrae140/ directory created
    - README.md file created with format documentation
    - Directory structure follows Fluxion conventions
    - Proper permissions set
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Reference Data CSV Files</name>
  <files>
    data/reference/ashrae140/series_800.csv
    data/reference/ashrae140/series_195.csv
  </files>
  <read_first>
    - ASHRAE Standard 140-2017 specification (reference values)
    - data/reference/README.md (existing data format)
    - src/validation/reference/mod.rs (expected data structure)
  </read_first>
  <action>
    Create CSV files containing reference data for new ASHRAE 140 cases:

    **series_800.csv:** Reference data for Cases 800-810 (HVAC equipment validation)
    ```csv
    case,hour,zone1_temp,zone1_heating,zone1_cooling,zone2_temp,zone2_heating,zone2_cooling,total_energy
    800,1,21.1,0.0,1250.0,20.8,0.0,1320.0,2570.0
    800,2,21.0,0.0,1180.0,20.7,0.0,1280.0,2460.0
    # ... all 8760 hours for Case 800
    800,8760,21.2,520.0,0.0,20.9,610.0,0.0,1130.0
    801,1,22.0,0.0,980.0,21.8,0.0,1050.0,2030.0
    # ... all cases through 810
    ```

    **series_195.csv:** Reference data for Cases 195-470 (diagnostic validation)
    ```csv
    case,hour,zone1_temp,zone1_heating,zone1_cooling,total_energy,peak_load
    195,1,23.5,1200.0,0.0,1200.0,1200.0
    195,2,23.4,1180.0,0.0,1180.0,1180.0
    # ... all 8760 hours for Case 195
    195,8760,23.6,1450.0,0.0,1450.0,3200.0
    196,1,24.1,890.0,0.0,890.0,890.0
    # ... all cases through 470
    ```

    Use ASHRAE 140-2017 reference values or generate synthetic data following specification:
    - Hourly values for all 8760 hours per case
    - Temperature (°C), energy (W), and load (W) values
    - Consistent CSV format with header row
    - Comma-delimited, no quotes unless necessary

    For cases without published reference data, generate synthetic data using:
    - Base values from similar validated cases
    - Adjustments based on case parameters (thermal mass, window area, etc.)
    - Clear documentation of synthetic data generation method
  </action>
  <verify>
    <automated>test -f data/reference/ashrae140/series_800.csv</automated>
    <automated>test -f data/reference/ashrae140/series_195.csv</automated>
    <automated>head -1 data/reference/ashrae140/series_800.csv | grep -c "case,hour,zone1_temp"</automated>
    <automated>wc -l data/reference/ashrae140/series_800.csv | awk '{print $1}' | grep -E "^876[0-9]{1,2}$"</automated>
  </verify>
  <done>
    - series_800.csv created with 11 cases × 8760 hours = 96,360 data rows
    - series_195.csv created with 276 cases × 8760 hours = 2,418,560 data rows
    - Both files have proper CSV format with headers
    - Reference values follow ASHRAE 140-2017 specification
    - Synthetic data generation documented
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Reference Data Loading Module</name>
  <files>src/validation/reference/mod.rs</files>
  <read_first>
    - src/validation/reference/mod.rs (current implementation)
    - src/validation/ashrae140/cases/mod.rs (case enum)
    - data/reference/ashrae140/series_800.csv (data format)
    - data/reference/ashrae140/series_195.csv (data format)
  </read_first>
  <action>
    Extend the reference data loading module to support new ASHRAE 140 cases:

    ```rust
    use std::path::Path;
    use csv::Reader;
    use serde::Deserialize;
    use crate::validation::ashrae140::ASHRAE140Case;

    #[derive(Debug, Deserialize)]
    struct ReferenceDataRow {
        case: u32,
        hour: u32,
        zone1_temp: f64,
        zone1_heating: f64,
        zone1_cooling: f64,
        zone2_temp: Option<f64>,
        zone2_heating: Option<f64>,
        zone2_cooling: Option<f64>,
        total_energy: f64,
        peak_load: Option<f64>,
    }

    pub fn load_reference_data(case: ASHRAE140Case) -> Result<ReferenceDataset> {
        match case {
            ASHRAE140Case::Case800..=ASHRAE140Case::Case810 => {
                load_series_800_reference(case)
            }
            ASHRAE140Case::Case195..=ASHRAE140Case::Case470 => {
                load_series_195_reference(case)
            }
            _ => load_legacy_reference(case),
        }
    }

    fn load_series_800_reference(case: ASHRAE140Case) -> Result<ReferenceDataset> {
        let case_num = match case {
            ASHRAE140Case::Case800 => 800,
            ASHRAE140Case::Case801 => 801,
            // ... map all cases
            _ => unreachable!("Invalid case for series 800"),
        };

        let path = Path::new("data/reference/ashrae140/series_800.csv");
        let mut reader = Reader::from_path(path)?;

        let mut dataset = ReferenceDataset::new(case);

        for result in reader.deserialize() {
            let record: ReferenceDataRow = result?;
            if record.case == case_num {
                dataset.add_hourly_data(record.hour, record.zone1_temp,
                                      record.zone1_heating, record.zone1_cooling,
                                      record.total_energy);
            }
        }

        Ok(dataset)
    }

    fn load_series_195_reference(case: ASHRAE140Case) -> Result<ReferenceDataset> {
        // Similar implementation for diagnostic cases
        // Handle optional fields appropriately
    }
    ```

    Implement proper error handling:
    - File not found errors with helpful messages
    - CSV parsing errors with line numbers
    - Case number mismatches
    - Missing data validation

    Add caching mechanism to avoid re-reading files:
    ```rust
    use std::collections::HashMap;
    use once_cell::sync::Lazy;

    static REFERENCE_CACHE: Lazy<Mutex<HashMap<ASHRAE140Case, ReferenceDataset>>> =
        Lazy::new(|| Mutex::new(HashMap::new()));
    ```
  </action>
  <verify>
    <automated>grep -c "load_series_800_reference\|load_series_195_reference" src/validation/reference/mod.rs</automated>
    <automated>cargo check --lib</automated>
    <automated>cargo test --lib reference::test_loading -- --nocapture</automated>
  </verify>
  <done>
    - Reference data loading functions implemented for both series
    - CSV parsing with proper error handling
    - Caching mechanism to improve performance
    - Module compiles and passes basic tests
    - Integration with existing validation framework
  </done>
</task>

</tasks>

<verification>
1. **Directory Structure:** data/reference/ashrae140/ directory created with proper organization
2. **Reference Data Files:** Both CSV files created with proper format and data
3. **Data Completeness:** Files contain expected number of rows (8760 hours per case)
4. **Loading Module:** Reference data loading functions compile and pass tests
5. **Integration:** Module integrates with validation framework without breaking existing functionality
</verification>

<success_criteria>
- User can load reference data for any new ASHRAE 140 case
- Reference data follows ASHRAE 140-2017 specification where available
- Loading performance is optimized with caching
- Error handling provides clear messages for debugging
- Data format is well-documented and consistent
</success_criteria>

<output>
After completion, create `.planning/phases/40-case-expansion-foundation/40-02-SUMMARY.md` with:
- Summary of reference data structure and format
- Documentation of data sources and generation methods
- Performance characteristics of loading functions
- Any limitations or known issues with reference data
- Next steps for cross-validation framework integration
</output>
