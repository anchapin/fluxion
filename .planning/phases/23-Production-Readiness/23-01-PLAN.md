---
phase: 23-Production-Readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["docs/API_REFERENCE.md", "src/lib.rs"]
autonomous: true
requirements: ["PROD-01", "PROD-02"]

# Goal-backward verification
must_haves:
  truths:
    - "User can find documentation for all PyO3 public functions"
    - "User can copy-paste usage examples and they work"
  artifacts:
    - path: "docs/API_REFERENCE.md"
      provides: "Complete API documentation"
      min_lines: 400
      exports: ["BatchOracle", "Model", "ParameterBounds", "ValidationError", "SurrogateError"]
    - path: "src/lib.rs"
      provides: "PyO3 module definition"
      contains: "#\\[pymethods\\]"
  key_links: []
---

<objective>
Complete API documentation covering all PyO3 public functions with usage examples.

Purpose: Users can understand and use all public APIs without guessing.
Output: Updated docs/API_REFERENCE.md with complete function coverage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

[Relevant source files:]
@src/lib.rs
@src/api/mod.rs
@src/api/parameters.rs
@src/api/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract all PyO3 public functions from lib.rs</name>
  <files>src/lib.rs</files>
  <action>
Extract all #[pymethods] functions from src/lib.rs. Document each with:
- Function signature
- Parameters with types and defaults
- Return types
- Error conditions
- Cross-reference to Rust doc comments if present

Use grep to find: grep -n '#\[pymethods\]' src/lib.rs
Use grep to find methods: grep -n 'fn ' src/lib.rs | grep -v '//'
  </action>
  <verify>All #[pymethods] blocks have corresponding documentation</verify>
  <done>List of all PyO3 functions with signatures</done>
</task>

<task type="auto">
  <name>Task 2: Update API_REFERENCE.md with complete function list</name>
  <files>docs/API_REFERENCE.md</files>
  <action>
Update docs/API_REFERENCE.md to include all PyO3 functions found:

1. For each struct/class (BatchOracle, Model, etc.):
   - Add constructor documentation
   - Add all public methods with signatures
   - Add parameter descriptions
   - Add return value descriptions
   - Add error conditions

2. Ensure each function has:
   - Function signature in Python style
   - Description paragraph
   - Parameters table
   - Returns section
   - Raises section (if applicable)
   - Example (if not already present)

Avoid: Repeating information already in docs - focus on completeness.
  </action>
  <verify>All PyO3 #[pymethods] have docs in API_REFERENCE.md</verify>
  <done>Complete API reference with all functions documented</done>
</task>

<task type="auto">
  <name>Task 3: Add missing usage examples for each public function</name>
  <files>docs/API_REFERENCE.md</files>
  <action>
For each public function in BatchOracle and Model:
1. If no example exists, add a minimal working example
2. Examples should be copy-pasteable and work

Functions needing examples:
- Check existing examples in API_REFERENCE.md
- Add examples for any methods missing them

Use realistic parameter values in examples.
  </action>
  <verify>All public methods have at least one usage example</verify>
  <done>Every function has working example</done>
</task>

</tasks>

<verification>
- [ ] All #[pymethods] in lib.rs have corresponding documentation
- [ ] API_REFERENCE.md has examples for all public functions
- [ ] Build succeeds (cargo build --release)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- API reference is complete and usable
</success_criteria>

<output>
After completion, create `.planning/phases/23-Production-Readiness/23-01-SUMMARY.md`
</output>
