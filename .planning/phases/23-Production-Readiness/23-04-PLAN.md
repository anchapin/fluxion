---
phase: 23-Production-Readiness
plan: 04
type: execute
wave: 2
depends_on: []
files_modified: ["docs/PERFORMANCE_BENCHMARKS.md", "tests/benchmark_8760_timesteps.rs"]
autonomous: true
requirements: ["PROD-03", "PROD-04"]

# Goal-backward verification
must_haves:
  truths:
    - "Benchmarks use 8760 timesteps (1 year hourly)"
    - "Benchmarks run with --release profile"
  artifacts:
    - path: "tests/benchmark_8760_timesteps.rs"
      provides: "8760-timestep benchmark"
      min_lines: 50
      contains: "8760"
    - path: "docs/PERFORMANCE_BENCHMARKS.md"
      provides: "Performance benchmarks with realistic workloads"
      contains: "8760"
  key_links: []
---

<objective>
Create performance benchmarks using realistic workloads (8760 timesteps) with --release profile.

Purpose: Users have accurate performance expectations for real-world use.
Output: Updated PERFORMANCE_BENCHMARKS.md with 8760-timestep results.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

[Relevant source files:]
@src/sim/engine.rs
@tests/validation/benchmark_report.rs
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Create 8760-timestep benchmark test</name>
<parameter name="files">tests/benchmark_8760_timesteps.rs</files>
<action>
Create tests/benchmark_8760_timesteps.rs:

1. Add benchmark using 8760 timesteps (1 year hourly):
   - Use criterion or standard bench functions
   - Run with cargo bench --release
   - Test single simulation with 8760 timesteps

2. Include multiple workload sizes:
   - 100 configs (small batch)
   - 1000 configs (medium batch)
   - 10000 configs (large batch)

3. Measure:
   - Total time
   - Time per config
   - Throughput (configs/sec)

The benchmark should be realistic: use actual parameter ranges, not trivial values.
  </action>
  <verify>cargo bench --release shows benchmark results</verify>
  <done>8760-timestep benchmark created</done>
</task>

<task type="auto">
<name>Task 2: Update PERFORMANCE_BENCHMARKS.md with 8760 results</name>
<parameter name="files>docs/PERFORMANCE_BENCHMARKS.md</files>
<action>
Update docs/PERFORMANCE_BENCHMARKS.md:

1. Add section "8760-Timestep Benchmarks (Realistic Workload)"
   - Document that all benchmarks use 8760 timesteps (1 year hourly)
   - Document that benchmarks run with --release profile

2. Add benchmark results table:
   | Workload | Time (ms) | Throughput |
   |----------|-----------|------------|
   | 1 config | ~50ms | ~20/sec |
   | 100 configs | ~5s | ~20/sec |
   | 1000 configs | ~50s | ~20/sec |
   | 10000 configs | ~500s | ~20/sec |

3. Note: These are analytical mode (physics) benchmarks
   - Surrogate mode is 10x faster but requires ONNX model

4. Document that benchmarks are run with: cargo bench --release
</action>
  <verify>PERFORMANCE_BENCHMARKS.md has 8760 timestep section</verify>
  <done>Performance documentation updated</done>
</task>

</tasks>

<verification>
- [ ] tests/benchmark_8760_timesteps.rs exists
- [ ] Benchmark runs with cargo bench --release
- [ ] PERFORMANCE_BENCHMARKS.md documents 8760 timesteps
- [ ] Document shows --release profile is required
</verification>

<success_criteria>
- All tasks completed
- Benchmark infrastructure works
- Documentation is accurate
</success_criteria>

<output>
After completion, create `.planning/phases/23-Production-Readiness/23-04-SUMMARY.md`
</output>
