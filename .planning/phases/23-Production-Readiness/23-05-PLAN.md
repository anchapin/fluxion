---
phase: 23-Production-Readiness
plan: 05
type: execute
wave: 2
depends_on: []
files_modified: [".githooks/perf-baseline.py", "tests/performance_regression_test.rs", "docs/PERFORMANCE_BENCHMARKS.md"]
autonomous: true
requirements: ["PROD-05", "PROD-06", "PROD-07"]

# Goal-backward verification
must_haves:
  truths:
    - "Regression detection fails PR if >10% slowdown"
    - "Latency and throughput are measured"
    - "FFI boundary overhead is measured"
  artifacts:
    - path: "tests/performance_regression_test.rs"
      provides: "Performance regression test"
      contains: "10%"
    - path: ".githooks/perf-baseline.py"
      provides: "Baseline comparison script"
      contains: "baseline"
    - path: "docs/PERFORMANCE_BENCHMARKS.md"
      contains: "latency"
      contains: "throughput"
      contains: "FFI"
  key_links: []
---

<objective>
Implement performance regression detection, latency/throughput measurement, and FFI boundary overhead measurement.

Purpose: Users can detect performance regressions before deployment.
Output: Regression test infrastructure and FFI measurement.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

[Relevant source files:]
@.githooks/perf-baseline.py
@tests/validation/benchmark_report.rs
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Implement performance regression test</name>
<parameter name="files">tests/performance_regression_test.rs</files>
<action>
Create tests/performance_regression_test.rs:

1. Add integration test that measures performance:
   - Run 1000 config evaluations
   - Measure elapsed time
   - Compare against baseline (stored in file)

2. Implement 10% regression threshold:
   - If current_time > baseline_time * 1.10, FAIL
   - Print warning if within 10% of baseline

3. Store baseline in tests/perf_baseline.json:
   - Format: {"timestamp": "...", "throughput": 2000.0, "latency_ms": 0.5}

4. Add test that can be run in CI to catch regressions
</action>
  <verify>Test runs and detects regressions</verify>
  <done>Regression test created</done>
</task>

<task type="auto">
<name>Task 2: Enhance perf-baseline.py for PR regression detection</name>
<parameter name="files">.githooks/perf-baseline.py</files>
<action>
Update .githooks/perf-baseline.py:

1. Add function to compare current performance vs baseline:
   - Load baseline from JSON
   - Run benchmark
   - Calculate percent change
   - Fail if >10% slowdown

2. Add JSON output for CI integration:
   - Write results to perf_results.json
   - Include timestamp, throughput, latency, percent_change

3. Add GitHub Actions integration:
   - Print check-friendly output
   - Exit with error code if regression detected
</action>
  <verify>Script detects >10% regression</verify>
  <done>Baseline comparison enhanced</done>
</task>

<task type="auto">
<name>Task 3: Document latency and throughput metrics</name>
<parameter name="files>docs/PERFORMANCE_BENCHMARKS.md</files>
  <action>
Add latency and throughput section to PERFORMANCE_BENCHMARKS.md:

1. Add latency metrics:
   - Per-config latency: ~0.5ms (analytical), ~0.1ms (surrogate)
   - Per-timestep latency: ~0.057µs per timestep

2. Add throughput metrics:
   - Analytical mode: ~2,000 configs/sec (8-core CPU)
   - Surrogate mode: ~10,000+ configs/sec

3. Add measurement methodology:
   - How latency is measured
   - How throughput is measured
   - What variables affect results
</action>
  <verify>Latency and throughput documented</verify>
  <done>Performance metrics documented</done>
</task>

<task type="auto">
<name>Task 4: Measure and document FFI boundary overhead</name>
<parameter name="files>docs/PERFORMANCE_BENCHMARKS.md</files>
  <action>
Add FFI boundary overhead section:

1. Document FFI (Foreign Function Interface) overhead:
   - Python to Rust call overhead
   - Array conversion overhead
   - Return value conversion overhead

2. Measure FFI overhead:
   - Compare pure Rust vs Python-wrapped execution
   - Estimate overhead per call

3. Document optimization strategies:
   - Batch operations to reduce FFI calls
   - Use numpy arrays directly
   - Avoid per-config calls in hot loops
</action>
  <verify>FFI overhead documented</verify>
  <done>FFI documentation complete</done>
</task>

</tasks>

<verification>
- [ ] Performance regression test exists and runs
- [ ] Baseline comparison script detects >10% slowdown
- [ ] Latency metrics documented
- [ ] Throughput metrics documented
- [ ] FFI overhead measured and documented
</verification>

<success_criteria>
- All tasks completed
- Regression detection works
- All metrics documented
</success_criteria>

<output>
After completion, create `.planning/phases/23-Production-Readiness/23-05-SUMMARY.md`
</output>
