---
phase: 07-advanced-analysis-visualization
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ashrae_140_references.json
  - src/validation/multi_reference.rs
  - src/validation/mod.rs
  - src/validation/ashrae_140_validator.rs
  - src/validation/reporter.rs
autonomous: true
requirements:
  - MREF-01
  - MREF-02
  - MREF-03
must_haves:
  truths:
    - "Versioned multi-program reference data is loaded from JSON"
    - "Per-program validation status (PASS/WARN/FAIL) is determined"
    - "Validation report includes per-program comparison tables"
    - "fluxion references update command validates and installs new reference data from ASHRAE publications"
  artifacts:
    - path: "docs/ashrae_140_references.json"
      provides: "Versioned reference ranges per case per program (EnergyPlus, ESP-r, TRNSYS)"
    - path: "src/validation/multi_reference.rs"
      provides: "MultiReferenceDB loader and query interface"
      min_lines: 100
    - path: "src/validation/mod.rs"
      provides: "Module declaration for multi_reference"
      contains: "pub mod multi_reference;"
    - path: "src/validation/reporter.rs"
      provides: "Integration of multi-program results into Markdown report"
      contains: "add_multireference_table"
  key_links:
    - from: "ASHRAE140Validator"
      to: "MultiReferenceDB"
      via: "get_program_ranges"
    - from: "ValidationReport"
      to: "CLI output"
      via: "per-program columns and overall status"
---

<objective>
Extend the validation framework to support multi-reference comparison against EnergyPlus, ESP-r, and TRNSYS. Introduce versioned reference data, compute per-program pass/fail, and enhance the Markdown report to show side-by-side results. Add a CLI command to update the reference data.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-advanced-analysis-visualization/07-CONTEXT.md
# Multi-reference: EnergyPlus primary, per-program reporting, fallback logic (PASS if EnergyPlus PASS, else WARN if any pass, FAIL if all fail).
# Reference versioning stored in docs/ashrae_140_references.json.
# CLI command: fluxion references update.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create versioned reference data file and loader</name>
  <files>
    - docs/ashrae_140_references.json
    - src/validation/multi_reference.rs
  </files>
  <action>
    1. Create docs/ashrae_140_references.json with example structure:
       ```json
       {
         "version": "2024-01",
         "source": "ASHRAE 140-2024 Standard, Tables X1-X5",
         "cases": {
           "600": {
             "annual_heating": { "EnergyPlus": { "min": 3.10, "max": 3.80 }, "ESP-r": { "min": 3.20, "max": 3.90 }, "TRNSYS": { "min": 3.05, "max": 3.85 } },
             "annual_cooling": { ... },
             "peak_heating": { ... },
             "peak_cooling": { ... }
           },
           "650": { ... }
         }
       }
       ```
       Populate with realistic ranges for a few cases (600, 900) based on ASHRAE 140 reference values; for others, placeholder values.
    2. Create src/validation/multi_reference.rs:
       - Define structs: `#[derive(Deserialize)] struct ProgramRange { min: f64, max: f64 }`, `#[derive(Deserialize)] struct CaseRefs { annual_heating: HashMap<String, ProgramRange>, annual_cooling: HashMap<String, ProgramRange>, peak_heating: HashMap<String, ProgramRange>, peak_cooling: HashMap<String, ProgramRange> }`, and `pub struct MultiReferenceDB { version: String, cases: HashMap<String, CaseRefs> }`.
       - Implement `impl MultiReferenceDB { pub fn from_file(path: &Path) -> Result<Self, Box<dyn std::error::Error>>` using serde_json; include error handling for missing fields.
       - Provide methods: `pub fn get_ranges(&self, case_id: &str, metric: &str) -> Option<&HashMap<String, ProgramRange>>`.
    3. Write unit test `test_multireference_loading`: load the JSON from repo and verify case 600's annual_heating contains keys "EnergyPlus", "ESP-r", etc., and that min < max.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output multi_reference::load
  </verify>
  <done>Reference data JSON created and loader implemented; test validates structure and contents.</done>
</task>

<task type="auto">
  <name>Task 2: Per-program status logic and report integration</name>
  <files>
    - src/validation/ashrae_140_validator.rs
    - src/validation/reporter.rs
    - src/validation/mod.rs
  </files>
  <action>
    1. In `src/validation/ashrae_140_validator.rs`:
       - Add field `multi_ref: Option<MultiReferenceDB>` to ASHRAE140Validator struct; provide method `with_multi_reference(mut self, path: &Path) -> Self` that loads from file and sets.
       - In the validation loop after computing case results, if self.multi_ref is Some, retrieve reference ranges for the case and each metric (annual_heating, annual_cooling, peak_heating, peak_cooling). For each program in the range, compare simulated value to min/max and assign ValidationStatus::Pass/Fail.
       - Store per-program statuses in a new field in the case result: `per_program: HashMap<String, ValidationStatus>`.
       - Compute overall status: PASS if EnergyPlus passes; else WARN if any program passes; else FAIL.
    2. In `src/validation/reporter.rs`:
       - Add method `add_multireference_table` that generates a markdown table with columns: Case, Metric, EnergyPlus, ESP-r, TRNSYS, Overall. For each case and metric, display value and status (in parentheses, e.g., "5.2 MWh (PASS)"). If a program has no reference data, show "-".
       - Call this method when generating the full report after the main summary.
    3. Add `pub mod multi_reference;` to src/validation/mod.rs (if not already present).
    4. Tests:
       - `validator::test_multireference_status`: construct validator with multi-ref DB, run on a specific case (maybe mock results), check per-program statuses and overall.
       - `reporter::test_multireference_table`: generate report and verify header and sample row.
    5. Stub `fluxion references update`: In a new file `src/validation/commands.rs` (or in mod.rs) define `pub fn update_references(url: Option<&str>) -> Result<()>` that if Some(url), downloads and validates; else checks local file. For now, just validate docs/ashrae_140_references.json is valid JSON and print success. CLI wiring in Plan 07-06.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output validator::multireference
    cargo test -- --test-threads=1 --show-output reporter::multireference
  </verify>
  <done>Per-program validation and reporting implemented; tests confirm logic and table format; references update function available.</done>
</task>

</tasks>

<verification>
- Build succeeds; all new modules compile.
- Multi-reference loader reads docs/ashrae_140_references.json without errors.
- Validation tests with multi-ref show correct overall status.
- Generated Markdown report contains the multi-program table with expected columns.
- All tests pass on CI.
</verification>

<success_criteria>
The validation framework now supports comparison against EnergyPlus, ESP-r, and TRNSYS simultaneously, with per-program reporting and versioned reference data. The system is ready for CLI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-analysis-visualization/07-05-SUMMARY.md` capturing:
- MultiReferenceDB design and JSON schema
- Per-program status algorithm (PASS/WARN/FAIL)
- Changes to BenchmarkReport to store per-program results
- Reporter extension for Markdown table
- Stub for `references update` command
</output>
