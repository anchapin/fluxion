---
phase: 07-advanced-analysis-visualization
task: 1.2 (07-01 in progress, 07-05 complete)
total_tasks: 7
status: in_progress
last_updated: '$timestamp'
---

<current_state>
Phase 7 Wave 1 execution in progress. Plan 07-05 (Multi-Reference Validation) is complete: SUMMARY.md written, all tasks committed. Plan 07-01 (Sensitivity Analysis) is executing but encountering test failures; agent is debugging CSV export and metric computation tests. The library builds (`cargo check` passes) but `cargo test` fails due to missing `per_program`/timing fields in some test scaffolding and a header assertion mismatch in sensitivity CSV test.
</current_state>

<completed_work>

- 07-05 complete: multi-reference DB, validator integration, reporter table, CLI commands stub.
- 07-01: analysis module scaffolding created; sensitivity sampling (OAT, Sobol) and metrics (normalized coeff, CVRMSE, NMBE, slope) implemented; manual linear regression; CSV export function exists.
- Consolidated ValidationResult type (removed duplicate in validator.rs, now using report::ValidationResult with optional `per_program`).
- Added `src/validation/multi_reference.rs`, `src/validation/commands.rs`, `docs/ashrae_140_references.json`.
- Updated `src/validation/mod.rs` to declare `pub mod multi_reference` and `pub mod commands`.
- Fixed commands import issue; module declarations correct.

</completed_work>

<remaining_work>

- 07-01:
  - Fix CSV export test (headers comparison: collect to Vec<&str>).
  - Ensure unit tests for sampling and metrics pass.
  - Commit final changes, write 07-01-SUMMARY.md, update STATE.md and ROADMAP.md via `roadmap update-plan-progress`.
- Wave 2 (after 07-01 finalizes):
  - 07-02: Delta testing framework
  - 07-03: Component breakdown and swing analysis
  - 07-04: Interactive Plotly visualization
  - 07-07: Extended CaseBuilder API
- Wave 3:
  - 07-06: CLI integration and integration tests

</remaining_work>

<decisions_made>

- Multi-reference overall status: PASS if EnergyPlus passes; else WARN if any program passes; else FAIL.
- ValidationResult.per_program: Option<HashMap<String, ValidationStatus>>, serialized only when Some.
- Added `create_result_with_multi()` helper in ASHRAE140Validator to keep call sites clean.
- CLI `references update` command validates local JSON; remote fetch TBD.
- Sensitivity manual regression avoids linregress crate API mismatch.
- Test scaffolding updates needed: all direct BenchmarkReport constructions must include `start_time: None, end_time: None`; all ValidationResult constructions must include `per_program: None`.

</decisions_made>

<blockers>

- Test compilation: several integration/unit tests construct ValidationResult/BenchmarkReport without new fields → missing `per_program` and `start_time/end_time` errors.
- CSV test assertion type mismatch in sensitivity.rs (StringRecord vs [&str; 6]).
- Potential remaining import issues (PathBuf in some test modules).
- 07-01 agent may need to patch test files (guardrails, result_aggregation) to include new fields.

</blockers>

<context>
Phase 7 structure: 7 plans, 3 waves. Wave 1 core infrastructure (analysis engine, validation extension); Wave 2 feature implementations (delta, diagnostics, viz, CaseBuilder); Wave 3 integration. Agents operate autonomously per wave; orchestrator monitors and advances waves upon completion. Build currently passes library check but tests fail due to struct evolution. Standard procedure: update all test constructions to match new struct definitions. 07-05 agent already accounted for this in its test additions (used per_program: None). 07-01 agent needs to apply similar fixes to existing tests that fail compilation (e.g., test_guardrail_exit_codes.rs, test_result_aggregation.rs). Also fix sensitivity CSV header assert to compare Vec<&str>.
</context>

<next_action>
Resume work: use `/gsd:resume-work` to continue Phase 7. The workflow will detect the active phase, read this handoff, and determine next steps. Likely next action: assist 07-01 agent in fixing remaining test scaffolding (add per_program: None and timing fields) and CSV header assertion, then allow tests to compile and run. After 07-01 commits SUMMARY, proceed to Wave 2.
</next_action>
