---
phase: 07-advanced-analysis-visualization
plan: 07
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
  - "04"
  - "05"
  - "06"
files_modified:
  - src/bin/fluxion.rs
  - tests/cli_integration.rs
autonomous: true
requirements: []  # This plan wires existing features; no new requirements
must_haves:
  truths:
    - "All Phase 7 subcommands are registered and accessible via fluxion CLI"
    - "End-to-end CLI tests verify each subcommand produces expected outputs"
  artifacts:
    - path: "src/bin/fluxion.rs"
      provides: "CLI subcommand definitions and handlers for Phase 7 features"
      contains: "Sensitivity", "Delta", "Components", "Swing", "Visualize", "Animate", "ReferencesUpdate"
    - path: "tests/cli_integration.rs"
      provides: "Comprehensive integration test suite"
      min_lines: 200
  key_links:
    - from: "fluxion CLI"
      to: "analysis::sensitivity"
      via: "sensitivity subcommand handler"
    - from: "fluxion CLI"
      to: "analysis::delta"
      via: "delta subcommand handler"
    - from: "fluxion CLI"
      to: "visualization"
      via: "visualize / animate subcommands"
    - from: "fluxion CLI"
      to: "validation::multi_reference::update_references"
      via: "references update subcommand"
---

<objective>
Wire all Phase 7 features into the main CLI, expose subcommands for sensitivity, delta, components, swing, visualization, animation, and references update. Add end-to-end integration tests to ensure correctness.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-advanced-analysis-visualization/07-CONTEXT.md
# CLI integration: all features become subcommands under fluxion.
# Existing CLI structure uses clap with Parser and Subcommand.
# New modules are ready in src/analysis, src/visualization, etc.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register all Phase 7 subcommands in CLI</name>
  <files>
    - src/bin/fluxion.rs
  </files>
  <action>
    In src/bin/fluxion.rs:
    1. Extend the `Commands` enum with new variants:
       - `Sensitivity { config: PathBuf, method: String, levels: Option<usize> }`
       - `Delta { config: PathBuf }`
       - `Components { case: String, output: Option<PathBuf> }`
       - `Swing { case: String }`
       - `Visualize { input: PathBuf, output: PathBuf }`
       - `Animate { input: PathBuf, output: PathBuf }`
       - `ReferencesUpdate { url: Option<String> }`
    2. In the main match for `Commands`, add handler arms:
       - For Sensitivity: parse config (CaseSpec), choose method, call `analysis::sensitivity::run_and_report` (to be implemented in Plan 01) and write CSV results.
       - For Delta: call `analysis::delta::run_and_report` (Plan 02).
       - For Components: run validation for given case ID (or use precomputed results), then call `analysis::components::aggregate_components` and export CSV.
       - For Swing: derive case and compute `analysis::swing::calculate_swing_metrics`, print to stdout.
       - For Visualize: read diagnostics CSV (from simulate --diagnostics), call `visualization::generate_html`.
       - For Animate: similar, call `visualization::generate_animation`.
       - For ReferencesUpdate: invoke `multi_reference::update_references` (Plan 06) to validate/install new JSON.
    3. For each subcommand, provide helpful descriptions and argument docs.
    4. Run `cargo check` to ensure all imports resolve (add any needed `use` statements for new modules: e.g., `use fluxion::analysis::sensitivity;`, `use fluxion::visualization;`, etc.).
  </action>
  <verify>
    fluxion --help | grep -q Sensitivity && fluxion --help | grep -q Delta && fluxion --help | grep -q Visualize
  </verify>
  <done>All Phase 7 subcommands appear in fluxion --help and handlers are stubbed with calls to the respective modules.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration tests</name>
  <files>
    - tests/cli_integration.rs
  </files>
  <action>
    Create tests/cli_integration.rs:
    1. For each Phase 7 subcommand (except ReferencesUpdate, which is optional), write a test function `#[test]` that:
       - Spawns a child process: `Command::new("target/debug/fluxion")` (or use `cargo run --quiet --` with args).
       - Provides necessary arguments: e.g., for Sensitivity, a sample config file (create a temporary minimal CaseSpec YAML in test). For Delta, a temporary YAML with base and variant. For Components and Swing, a simple case (600) can be used.
       - Captures stdout/stderr, checks exit status 0.
       - Verifies expected output files exist (e.g., sensitivity results CSV, delta markdown report, visualization HTML).
       - For Swing, check stdout contains metric labels like "min_temp", "max_temp".
    2. Use temp directories (std::env::temp_dir) to avoid polluting repo.
    3. Keep tests fast: use very small CaseSpecs (one zone, 1-year simulation might be slow; could use a smaller timestep like 24 hours for smoke test? The simulation expects 8760; but we can mock? Better: use real but quick simulations may take seconds. Acceptable for integration tests; mark as slow if needed.
    4. Ensure tests can find the fluxion binary: use `cargo` to run `fluxion` within test via `Command::new("cargo").arg("run").arg("--quiet").arg("--").arg(...`).
    5. Write assertion helpers.
    6. Run `cargo test cli_integration -- --test-threads=1` to verify.
  </action>
  <verify>
    cargo test cli_integration -- --test-threads=1 --show-output
  </verify>
  <done>Comprehensive CLI integration tests created and passing; end-to-end workflows validated.</done>
</task>

</tasks>

<verification>
- `cargo check` passes after CLI registrations.
- `fluxion --help` lists all Phase 7 subcommands.
- Integration tests exercise each subcommand with minimal data and confirm expected outputs (exit 0, files created, content non-empty).
- All tests (unit + integration) pass locally.
</verification>

<success_criteria>
All Phase 7 features are accessible through the fluxion CLI and verified with automated integration tests. The phase is complete and ready for release.
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-analysis-visualization/07-07-SUMMARY.md` summarizing:
- New CLI subcommands and their arguments
- Integration test coverage (each subcommand)
- Notes on performance of benchmarks (e.g., sensitivity may be compute-intensive)
</output>
