---
phase: 07-advanced-analysis-visualization
plan: 06
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
  - "04"
  - "05"
files_modified:
  - src/bin/fluxion.rs
  - tests/cli_integration.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "All Phase 7 subcommands (sensitivity, delta, components, swing, visualize, animate, references update) are registered and accessible via fluxion CLI"
    - "End-to-end integration tests exercise each subcommand and verify expected outputs"
  artifacts:
    - path: "src/bin/fluxion.rs"
      provides: "CLI subcommand definitions and handlers for all Phase 7 features"
      contains: "Sensitivity", "Delta", "Components", "Swing", "Visualize", "Animate", "ReferencesUpdate"
    - path: "tests/cli_integration.rs"
      provides: "Comprehensive integration test suite covering all subcommands"
      min_lines: 200
  key_links:
    - from: "fluxion CLI"
      to: "All analysis modules"
      via: "subcommand dispatch"
      pattern: "clap::App::subcommand"
---

<objective>
Wire all analysis and visualization features into the main CLI, and add basic integration tests to ensure end-to-end functionality. This plan connects the modules created in Wave 1 to the user-facing `fluxion` command.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-advanced-analysis-visualization/07-CONTEXT.md
# CLI subcommands: fluxion sensitivity, fluxion delta, fluxion visualize, fluxion animate, fluxion references update.
# Integration tests: invoke CLI with sample arguments and verify exit code and output.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register CLI subcommands in fluxion.rs</name>
  <files>
    - src/bin/fluxion.rs
  </files>
  <action>
    1. In src/bin/fluxion.rs, add subcommands using clap:
       - `sensitivity`: accepts config file, runs sensitivity analysis, outputs CSV and Markdown.
       - `delta`: accepts YAML config, runs comparison, writes report and hourly CSV.
       - `components`: accepts case ID, generates component breakdown CSV.
       - `swing`: accepts case ID, prints swing metrics to stdout.
       - `visualize`: accepts input diagnostics CSV, generates interactive HTML chart.
       - `animate`: accepts diagnostics CSV, generates animated HTML with controls.
       - `references update`: validates reference data; call `validation::update_references`.
    2. For each subcommand, parse arguments, call into the respective module (e.g., `analysis::sensitivity::run_and_report`, `analysis::delta::run_and_report`, `analysis::components::aggregate_and_export`, `analysis::swing::calculate_swing_metrics`, `visualization::generate_html`, `visualization::generate_animation`, `validation::update_references`), and print success message with output file path.
    3. Ensure error handling: propagate errors with `?` and display user-friendly messages.
    4. Update Cargo.toml if needed for additional CLI deps (clap already used).
  </action>
  <verify>
    cargo build --bin fluxion --release
    fluxion --help | grep -q sensitivity
    fluxion --help | grep -q delta
    fluxion --help | grep -q visualize
    fluxion --help | grep -q animate
    fluxion --help | grep -q references
  </verify>
  <done>All subcommands registered and visible in help; basic invocation works.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end CLI integration tests</name>
  <files>
    - tests/cli_integration.rs
  </files>
  <action>
    1. Create tests/cli_integration.rs:
       - Use `std::process::Command` to spawn `fluxion` with arguments.
       - For each subcommand (sensitivity, delta, components, swing, visualize, animate, references update), prepare minimal input (e.g., sensitivity params YAML, delta config YAML, case ID for components/swing, diagnostics CSV for visualize/animate, temp references.json for update).
       - Run command, assert exit status 0, check that expected output files exist (CSV, HTML, etc.), and that stdout contains success message.
       - Include a test for `fluxion references update` that creates a temporary references.json, runs update, and confirms validation message.
    2. Use `#[test]` for each subcommand; mark as integration test.
    3. Add any needed test fixtures under `tests/fixtures/` (or generate programmatically).
  </action>
  <verify>
    cargo test --test cli_integration -- --test-threads=1 --show-output
  </verify>
  <done>CLI integration tests cover all subcommands and pass.</done>
</task>

</tasks>

<verification>
- Build succeeds and binary includes all subcommands.
- Running `fluxion --help` shows new commands.
- Integration tests pass end-to-end for each subcommand.
- No panics or unwinds in normal operation.
</verification>

<success_criteria>
The fluxion CLI is fully extended with advanced analysis and visualization capabilities. Users can run sensitivity, delta, visualize, animate, and references update directly from the command line. Integration tests ensure correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-analysis-visualization/07-06-SUMMARY.md` summarizing:
- Subcommand registration details (argument parsing, module calls)
- Test coverage for each CLI entry point
- Any glue code added to pass data between CLI args and library APIs
</output>
