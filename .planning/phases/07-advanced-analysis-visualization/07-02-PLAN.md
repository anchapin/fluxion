---
phase: 07-advanced-analysis-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/analysis/delta.rs
autonomous: true
requirements:
  - DELTA-01
  - DELTA-02
  - DELTA-03
must_haves:
  truths:
    - "Delta test configurations in YAML are parsed correctly"
    - "Variant cases are expanded from patches and sweeps"
    - "Comparison metrics (absolute and percent differences) are computed accurately"
    - "Markdown delta reports and hourly CSV differences are generated"
  artifacts:
    - path: "src/analysis/delta.rs"
      provides: "Delta testing engine: config parser, case expansion, comparison logic, report generation"
      min_lines: 300
  key_links:
    - from: "src/analysis/delta.rs"
      to: "Model::simulate"
      via: "simulate each variant case"
      pattern: "Model::simulate"
    - from: "DeltaReport"
      to: "CSV files"
      via: "export_hourly_deltas_csv"
---

<objective>
Implement the delta testing framework: parse YAML configurations, expand variant cases (patches and sweeps), run simulations, and generate comparison reports with Markdown tables and detailed hourly differences in CSV format.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-advanced-analysis-visualization/07-CONTEXT.md
# Key Decisions:
# - YAML configuration with `base` and `variants` (patch/sweep)
# - Report includes absolute and percent differences; hourly differences in long-format CSV
# - Simple statistics for sweeps (mean, std dev)
</context>

<tasks>

<task type="auto">
  <name>Task 1: YAML configuration parser and variant builder</name>
  <files>
    - src/analysis/delta.rs
  </files>
  <action>
    In src/analysis/delta.rs (replacing stub):
    1. Define structs:
       - `pub struct DeltaConfig { pub base: CaseSpec, pub variants: Vec<Variant> }`
       - `pub struct Variant { pub name: String, pub patch: Option<HashMap<String, serde_yaml::Value>>, pub sweep: Option<HashMap<String, Vec<f64>>> }`
       Derive Deserialize for all.
    2. Implement `pub fn parse_config(path: &Path) -> Result<DeltaConfig, Box<dyn std::error::Error>>` using `serde_yaml::from_reader`.
    3. Implement `pub fn expand_variants(config: &DeltaConfig) -> Vec<(String, CaseSpec)>`:
       - For each variant: if `patch` is Some, apply patch fields to base CaseSpec (deep merge, handling numeric and string fields). If `sweep` is Some, produce one CaseSpec for each combination of sweep parameter values (cartesian product; multiple parameters in sweep can create multiple variants). Combine patch and sweep: patch is applied first, then sweep varies specified parameters.
       - Return vector of (variant_name, CaseSpec) tuples; for sweeps, append parameter values to name for identification (e.g., "Name: htr=5.0").
    4. Unit tests:
       - `test_config_parsing`: create sample YAML with both patch and sweep, parse and verify the CaseSpec count and field values.
       - `test_patch_application`: apply patch to base spec, ensure changed fields updated.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output delta::config_parsing
  </verify>
  <done>YAML parser and variant expansion work correctly; test coverage includes patch and sweep scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Comparison engine (metrics and hourly)</name>
  <files>
    - src/analysis/delta.rs
  </files>
  <action>
    Extend delta.rs:
    1. Define `pub struct DeltaReport { pub base_name: String, pub variants: Vec<VariantResult> }` and `pub struct VariantResult { pub name: String, pub annual_heating_mwh: f64, pub annual_cooling_mwh: f64, pub peak_heating_kw: f64, pub peak_cooling_kw: f64, pub hourly_differences: Option<Vec<HourlyDelta>> }`. `HourlyDelta` may contain fields: hour, zone, component, base_value, variant_value, difference.
    2. Implement `pub fn run_comparison(base: &CaseSpec, variants: &[(String, CaseSpec)], include_hourly: bool) -> Result<DeltaReport, Box<dyn std::error::Error>>`:
       - Run base case simulation: create model from base, run with diagnostics enabled (use `simulate` method with diagnostics?). Collect annual metrics (use EnergyBreakdown from validator) and hourly diagnostics (DiagnosticCollector).
       - For each variant: run simulation similarly, then compute differences: for each annual metric, diff = variant - base; percent = diff / base * 100. For hourly, if include_hourly, compute differences per hour per component (e.g., solar, internal, hvac, infiltration) from the HourlyData, and store in long-format.
       - For sweep variants: compute mean and standard deviation across the sweep dimension for each metric; these can be stored in VariantResult (fields: mean, std_dev). This may require grouping by sweep parameter values; simplify: treat each sweep point as separate entry; the report generator can summarize.
    3. Write unit test `test_comparison`: use two simple specs (e.g., different window U-values), verify that differences are computed and annual numbers are plausible.
    4. Ensure diagnostics are enabled during simulation to collect hourly data; reuse Phase 5 DiagnosticCollector by attaching to model.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output delta::comparison
  </verify>
  <done>Comparison engine correctly evaluates cases and computes differences; test passes with simple specs.</done>
</task>

<task type="auto">
  <name>Task 3: Markdown and CSV output generation</name>
  <files>
    - src/analysis/delta.rs
  </files>
  <action>
    Extend delta.rs:
    1. Implement `pub fn generate_markdown_report(report: &DeltaReport) -> String`:
       - Create markdown with header, summary table (rows: base, each variant) showing annual heating/cooling (MWh) and peaks (kW) with absolute and percent differences.
       - For sweeps, include additional columns for mean and std dev across swept parameter.
       - Use `tabel` format with alignment.
    2. Implement `pub fn export_hourly_deltas_csv(report: &DeltaReport, path: &Path)`:
       - Write CSV with columns: Hour, Zone, Component, Base_Value, Variant_Value, Difference. For multiple variants, write all rows sequentially (user can filter). For sweep variants, include all hourly points; statistical summarization left to external tools.
    3. Write unit tests:
       - `test_markdown_generation`: given a simple report, verify the markdown contains expected strings (e.g., "## Delta Test Report", "| Metric | Base | Variant |").
       - `test_csv_export`: create report, call export, read file and verify header and row count > 0.
    4. Add high-level convenience function `pub fn run_and_report(config: DeltaConfig, output_dir: &Path, include_hourly: bool) -> Result<(), Box<dyn std::error::Error>>` that orchestrates everything: parse, expand, compare, write markdown file and CSV if requested.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output delta::report
  </verify>
  <done>Markdown report and hourly differences CSV generation work; tests validate output structure.</done>
</task>

</tasks>

<verification>
- After all tasks: `cargo check --message-format=short` passes.
- All unit tests for delta module pass.
- No clippy warnings in modified files.
</verification>

<success_criteria>
Delta testing engine is fully implemented: YAML parsing, variant expansion, comparison, and report generation. The module is ready for CLI integration in Plan 07-06.
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-analysis-visualization/07-02-SUMMARY.md` documenting:
- DeltaConfig and Variant structures
- Expansion logic (patch + sweep)
- Comparison pipeline
- Report formats (Markdown and long-format CSV)
</output>
