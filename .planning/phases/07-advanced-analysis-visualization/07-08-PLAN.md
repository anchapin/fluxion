---
phase: 07-advanced-analysis-visualization
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validation/ashrae_140_cases.rs
  - src/validation/assembly_library.rs
  - src/validation/mod.rs
  - config/assemblies.yaml
  - docs/cases/quickstart.md
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
must_haves:
  truths:
    - "Simplified geometry builder methods (rectangular_zone, add_common_wall) enable easy custom case creation"
    - "AssemblyLibrary loads named construction assemblies from YAML configuration"
    - "Custom EPW weather files can be specified via CaseBuilder"
    - "Comprehensive documentation and examples demonstrate extensibility features"
  artifacts:
    - path: "src/validation/ashrae_140_cases.rs"
      provides: "Extended CaseBuilder with simplified geometry and custom weather support"
      contains: ["rectangular_zone", "add_common_wall", "with_weather_epw"]
    - path: "src/validation/assembly_library.rs"
      provides: "AssemblyLibrary struct for loading named constructions from YAML"
      min_lines: 80
    - path: "src/validation/mod.rs"
      provides: "Module declaration for assembly_library"
      contains: "pub mod assembly_library;"
    - path: "config/assemblies.yaml"
      provides: "Default assembly library with common constructions (wood_frame, CMU, etc.)"
    - path: "docs/cases/quickstart.md"
      provides: "Extensibility quickstart guide with examples"
      min_lines: 200
  key_links:
    - from: "CaseBuilder::rectangular_zone"
      to: "GeometrySpec"
      via: "adds a simple rectangular zone definition"
    - from: "CaseBuilder::add_common_wall"
      to: "CommonWall"
      via: "creates interior wall connection between zones"
    - from: "AssemblyLibrary"
      to: "config/assemblies.yaml"
      via: "loads and caches named assembly definitions"
    - from: "CaseBuilder::with_weather_epw"
      to: "WeatherSource::Epw"
      via: "assigns custom EPW file to CaseSpec"
---

<objective>
Extend the CaseBuilder API to support simplified geometry creation, custom weather (EPW), and a reusable assembly library. Provide comprehensive documentation to enable users to create custom cases easily.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-advanced-analysis-visualization/07-CONTEXT.md
# Extensible Case Framework: simplified geometry (rectangular_zone, add_common_wall), assembly library (YAML), custom EPW support, documentation-driven extensibility.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add simplified geometry methods to CaseBuilder</name>
  <files>
    - src/validation/ashrae_140_cases.rs
  </files>
  <action>
    In src/validation/ashrae_140_cases.rs, extend CaseBuilder impl block:
    1. Add method `pub fn rectangular_zone(&mut self, length: f64, width: f64, height: f64, name: Option<&str>) -> &mut Self`:
       - Creates a GeometrySpec::Zone with dimensions (length, width, height calculates floor area and volume, orientation defaults to 0° (North?)).
       - The zone gets an auto-generated ID if not named.
       - Push to self.geometry.
       - Return &mut Self for chaining.
    2. Add method `pub fn add_common_wall(&mut self, zone1_id: &str, zone2_id: &str, area: f64, r_value: f64) -> &mut Self`:
       - Creates a CommonWall specification with the given parameters and adds to self.common_walls.
       - Validate that zone1_id and zone2_id exist in current geometry (simple check: if any GeometrySpec::Zone has matching id). If not, return error? For now, panic in debug, warning in release; better: store and validate later in build().
       - Return &mut Self.
    3. Write unit tests:
       - `test_rectangular_zone_builder`: create builder, call rectangular_zone, verify geometry contains a zone with correct dimensions.
       - `test_add_common_wall_builder`: create builder with two zones, call add_common_wall, verify common_walls contains entry with correct fields.
    4. Ensure these methods are intuitive and documented.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output ashrae_140_cases::builder
  </verify>
  <done>Simplified geometry methods implemented and tested; CaseBuilder API more accessible for custom case creation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AssemblyLibrary and YAML loading</name>
  <files>
    - src/validation/assembly_library.rs
    - src/validation/mod.rs
    - config/assemblies.yaml
  </files>
  <action>
    1. Create src/validation/assembly_library.rs:
       - Define `pub struct AssemblyLibrary { assemblies: HashMap<String, ConstructionSpec> }`.
       - Implement `impl AssemblyLibrary`:
         * `pub fn from_file(path: &Path) -> Result<Self, Box<dyn std::error::Error>>`: read YAML file, deserialize into a HashMap where keys are assembly names and values are ConstructionSpec structures. Use serde_yaml.
         * `pub fn get(&self, name: &str) -> Option<&ConstructionSpec>`: lookup.
         * `pub fn list(&self) -> Vec<&str>`: all names.
       - Define YAML schema: top-level mapping: `AssemblyName: { construction_type: ..., layers: [{material: "...", thickness: f64, density: f64, specific_heat: f64, ...}] }`. The ConstructionSpec should match what's used in CaseBuilder (from existing code). Reuse existing structs from crate::sim::construction (like Construction, Layer, Material) if they exist, or define simplified equivalents if needed.
    2. Add `pub mod assembly_library;` to src/validation/mod.rs.
    3. Create config/assemblies.yaml with several example assemblies:
       - wood_frame_R20: wood stud wall with fiberglass insulation (R-20)
       - cmu_block_R10: concrete masonry unit wall
       - insulated_concrete_form_R30: ICF wall
       - double_pane_window: window with U=2.0, SHGC=0.6
       Use realistic layer definitions; follow existing ConstructionSpec format (use the same field names as in ASHRAE cases).
    4. Write unit tests:
       - `test_assembly_library_load`: load from config/assemblies.yaml and verify count > 0, get known assembly returns Some.
       - `test_assembly_schema_valid`: deserialize sample YAML ensures required fields present.
    5. Document that users can provide custom assembly files via environment variable or config path.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output assembly_library::load
  </verify>
  <done>AssemblyLibrary implemented, default assemblies provided, tests validate loading and lookup.</done>
</task>

<task type="auto">
  <name>Task 3: Add custom EPW weather support to CaseBuilder</name>
  <files>
    - src/validation/ashrae_140_cases.rs
  </files>
  <action>
    In src/validation/ashrae_140_cases.rs:
    1. Check if CaseSpec already has a weather field of type WeatherSource (from crate::weather). If not, add: `pub weather: WeatherSource` (default = WeatherSource::DefaultDenver?).
    2. In CaseBuilder, add method `pub fn with_weather_epw(&mut self, path: &str) -> &mut Self` that sets the CaseSpec's weather to `WeatherSource::Epw(PathBuf::from(path))` (assuming WeatherSource enum has Epw variant). If the variant does not exist, we may need to extend the WeatherSource enum in src/weather/mod.rs - but that would be a change outside this file. Check existing code: the context says "weather module already supports parsing", so WeatherSource likely already has Epw variant. If not, we need to add it. For this task, assume it exists; if not, note as gap and implement minimal addition.
    3. Also allow `with_weather_synthetic` for synthetic weather? Not required by EXT-02; EXT-02 says "EPW import". So just EPW.
    4. Update `CaseBuilder::build()` to propagate the weather field into the final CaseSpec (if not already).
    5. Write unit test `test_custom_epw`: builder with .with_weather_epw("custom.epw") builds CaseSpec whose weather field is WeatherSource::Epw with that path.
    6. Ensure CaseSpec validation checks that EPW file exists? Maybe defer to simulation; just store path.
  </action>
  <verify>
    cargo test -- --test-threads=1 --show-output ashrae_140_cases::epw
  </verify>
  <done>Custom EPW support added to CaseBuilder; CaseSpec can use non-default weather.</done>
</task>

<task type="auto">
  <name>Task 4: Write extensibility documentation and examples</name>
  <files>
    - docs/cases/quickstart.md
  </files>
  <action>
    Create docs/cases/quickstart.md with comprehensive guide:
    1. Introduction: "Why build custom cases?" and when to use the built-in ASHRAE cases vs custom.
    2. Prerequisites: Rust installed, fluxion library linked.
    3. Step-by-step tutorial building a simple custom case:
       - Using `CaseBuilder::new()`
       - Adding zones with `.rectangular_zone(length, width, height)`
       - Adding a second zone and connecting with `.add_common_wall()`
       - Selecting construction with `.with_construction_type(...)` or using assembly library: `let wood_frame = assembly_library.get("wood_frame_R20").unwrap(); builder.with_construction(wood_frame.clone())`
       - Setting windows with `.with_south_window(area)` and properties
       - Setting HVAC setpoints
       - Setting custom weather: `.with_weather_epw("path/to/your.epw")`
       - Finalizing with `.build()?`
       - Running simulation: `Model::new(case_spec)?.simulate(1, false)`
    4. Advanced customization: patch fields of CaseSpec directly, adding multiple constructions, multi-zone patterns.
    5. Using the assembly library: loading from default config/assemblies.yaml or custom path; listing available assemblies.
    6. Example code snippets with full main() function that compiles.
    7. Tips: geometry units (meters), thermal properties (U-value in W/m²K, R-value in m²K/W), EPW format expectations.
    8. Link to API reference (path: `src/validation/ashrae_140_cases.rs` documentation comments).
    9. Troubleshooting: common errors (missing assembly, invalid EPW path, mismatched zones).
    10. Conclusion and next steps (try different geometries, explore other CaseBuilder methods).
    4-5 pages of content, code blocks properly formatted.
    4. Link to full API reference (point to online docs or rustdoc).
    5. Add to docs/Make.html or docs/index.md? Not necessary; just providing the file is sufficient for this task.
  </action>
  <verify>
    # Check file exists and contains expected sections
    test -f docs/cases/quickstart.md && grep -q "rectangular_zone" docs/cases/quickstart.md && grep -q "add_common_wall" docs/cases/quickstart.md && grep -q "assembly_library" docs/cases/quickstart.md && grep -q ".epw" docs/cases/quickstart.md
  </verify>
  <done>Quickstart documentation created with step-by-step examples covering all EXT features; file contains essential terms.</done>
</task>

</tasks>

<verification>
- All tasks compile: `cargo check` passes.
- Assembly library YAML loads without errors; default assemblies file exists.
- CaseBuilder extended with new methods; unit tests cover geometry creation and EPW assignment.
- Documentation file exists and includes usage of rectangular_zone, add_common_wall, assembly library, and EPW.
- No clippy warnings in modified files.
</verification>

<success_criteria>
The extensible case framework is complete: users can create custom geometries easily, use custom EPW weather, leverage a reusable assembly library, and learn via comprehensive documentation. All EXT-01..04 requirements satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-analysis-visualization/07-08-SUMMARY.md` summarizing:
- CaseBuilder API extensions: rectangular_zone, add_common_wall, with_weather_epw
- AssemblyLibrary design and default assemblies.yaml contents
- Documentation structure (sections, examples)
- Usage patterns for custom case creation
</output>
