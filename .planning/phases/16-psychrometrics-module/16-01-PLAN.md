---
phase: 16-psychrometrics-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/weather/psychrometrics.rs, src/weather/mod.rs]
autonomous: true
requirements: [WEATHER-02]
user_setup: []

must_haves:
  truths:
    - "Saturation vapor pressure calculations produce accurate values matching ASHRAE Fundamentals reference tables"
    - "Dew point calculations always return values ≤ dry bulb temperature"
    - "Iterative calculations converge within tolerance and max iteration limits"
    - "All psychrometric functions use ASHRAE standard units (Pa, °C, kg/kg, kJ/kg)"
  artifacts:
    - path: "src/weather/psychrometrics.rs"
      provides: "Core psychrometric calculation functions"
      min_lines: 200
      exports: ["saturation_vapor_pressure", "calculate_dew_point", "calculate_wet_bulb", "calculate_humidity_ratio", "calculate_enthalpy", "STANDARD_ATMOSPHERIC_PRESSURE_Pa"]
    - path: "src/weather/mod.rs"
      provides: "Module exports for psychrometrics"
      exports: ["pub mod psychrometrics;", "pub use self::psychrometrics::*;"]
  key_links:
    - from: "src/weather/psychrometrics.rs"
      to: "ASHRAE Fundamentals Chapter 1"
      via: "Magnus-Tetens formula coefficients"
      pattern: "A = 610.78; B = 17.27; C = 237.3"
    - from: "calculate_dew_point()"
      to: "saturation_vapor_pressure()"
      via: "Newton-Raphson iteration"
      pattern: "p_sat(Td) = p_sat(T) × (RH/100)"
    - from: "calculate_dew_point()"
      to: "src/weather/psychrometrics.rs"
      via: "Physical constraint enforcement"
      pattern: "td.min(dry_bulb)"
---

<objective>
Implement core ASHRAE-compliant psychrometric calculation functions using exact formulations with iterative solving for dew point and wet-bulb temperatures.

Purpose: Provide accurate psychrometric calculations required for HVAC equipment validation and economizer control, following ASHRAE Fundamentals Chapter 1 methodology.
Output: Psychrometric calculation functions with comprehensive reference value validation and property tests.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-psychrometrics-module/16-CONTEXT.md
@.planning/phases/16-psychrometrics-module/16-RESEARCH.md
@src/weather/mod.rs
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->

From src/weather/mod.rs:
```rust
pub struct HourlyWeatherData {
    pub dry_bulb_temp: f64,  // °C
    pub humidity: f64,         // % RH (0-100)
    // ... other fields not used in psychrometric calculations
}
```

Standard atmospheric pressure constant:
```rust
pub const STANDARD_ATMOSPHERIC_PRESSURE_Pa: f64 = 101325.0; // Pa
```

ASHRAE Fundamentals formulas (from 16-RESEARCH.md):
- Magnus-Tetens: p_sat = 610.78 × exp((17.27 × T) / (T + 237.3))
- Dew point: Solve p_sat(Td) = p_sat(T) × (RH/100) using Newton-Raphson
- Enthalpy: h = 1.006·t + ω·(2501 + 1.86·t) kJ/kg where ω = (0.622 × p_sat × RH/100) / (P - p_sat × RH/100)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create psychrometrics module with saturation vapor pressure calculation</name>
  <files>src/weather/psychrometrics.rs</files>
  <action>Create new psychrometrics.rs module with:
- Module documentation referencing ASHRAE Fundamentals Chapter 1
- STANDARD_ATMOSPHERIC_PRESSURE_Pa constant (101325.0 Pa)
- saturation_vapor_pressure() function using Magnus-Tetens formula
  - Input: temperature in °C
  - Output: saturation vapor pressure in Pa
  - Coefficients: A=610.78 Pa, B=17.27, C=237.3°C
  - Formula: p_sat = A × exp((B × T) / (T + C))
- Add doc comments with formula explanation and ASHRAE reference
- Add inline test: test_saturation_vapor_pressure_reference_values()
  - Validate against ASHRAE reference values: p_sat(0°C)≈611.2 Pa, p_sat(20°C)≈2339 Pa, p_sat(30°C)≈4246 Pa
  - Tolerance: ±5 Pa

Per 16-RESEARCH.md Pattern 2 and Code Examples section for exact implementation details.</action>
  <verify>cargo test test_saturation_vapor_pressure_reference_values --lib</verify>
  <done>Function exists, compiles, and reference value tests pass with ±5 Pa tolerance</done>
</task>

<task type="auto">
  <name>Task 2: Implement dew point calculation with Newton-Raphson iteration</name>
  <files>src/weather/psychrometrics.rs</files>
  <action>Implement calculate_dew_point() function in psychrometrics.rs:
- Input: dry_bulb (°C), relative_humidity (0-100), pressure (Pa)
- Output: dew point temperature (°C)
- Algorithm:
  1. Calculate water vapor pressure: p_water = saturation_vapor_pressure(dry_bulb) × (RH/100)
  2. Newton-Raphson iteration: Td_{n+1} = Td_n - (p_sat(Td_n) - p_water) / (dp_sat/dT)
  3. Derivative: dp_sat/dT = p_sat × (B × C) / (T + C)²
  4. Initial guess: Td = dry_bulb
  5. Convergence tolerance: 1e-6
  6. Max iterations: 20 (prevent infinite loops)
  7. Physical constraint: dew_point ≤ dry_bulb (clamp after iteration)
- Add warning log if convergence fails after max iterations
- Add doc comments explaining Newton-Raphson method and ASHRAE reference
- Add inline tests:
  - test_dew_point_reference_values(): Validate 25°C/50% RH→13.9°C, 20°C/80% RH→16.4°C, 30°C/20% RH→5.0°C (tolerance ±0.5°C)
  - test_dew_point_le_dry_bulb(): Property test over temperature grid -10°C to 40°C and RH levels 10%, 30%, 50%, 70%, 90% to verify dew_point ≤ dry_bulb

Per 16-RESEARCH.md Pitfall 1 (dew point above dry bulb) and Code Examples section.</action>
  <verify>cargo test test_dew_point_reference_values test_dew_point_le_dry_bulb --lib</verify>
  <done>Function exists, converges, reference values pass with ±0.5°C tolerance, all property tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Implement wet-bulb temperature calculation</name>
  <files>src/weather/psychrometrics.rs</files>
  <action>Implement calculate_wet_bulb() function in psychrometrics.rs:
- Input: dry_bulb (°C), relative_humidity (0-100), pressure (Pa)
- Output: wet-bulb temperature (°C)
- Algorithm (per ASHRAE Fundamentals):
  1. Solve psychrometric equation iteratively for Tw
  2. Enthalpy balance: h(Tw, RH=100%) = h(T, RH)
  3. Use Newton-Raphson with initial guess: Tw = (dry_bulb + dew_point) / 2
  4. Convergence tolerance: 1e-6
  5. Max iterations: 20
  6. Fallback to bisection method if Newton-Raphson fails for RH > 90% (flat gradient)
- Add doc comments explaining psychrometric equation solving and ASHRAE reference
- Add inline tests:
  - test_wet_bulb_reference_values(): Validate against ASHRAE reference values with ±0.5°C tolerance
  - test_wet_bulb_convergence(): Verify convergence across full T/RH range

Per 16-RESEARCH.md Pitfall 6 (wet-bulb convergence at high humidity) and Wet-bulb calculation section.</action>
  <verify>cargo test test_wet_bulb_reference_values test_wet_bulb_convergence --lib</verify>
  <done>Function exists, converges, reference values pass with ±0.5°C tolerance</done>
</task>

<task type="auto">
  <name>Task 4: Implement humidity ratio calculation</name>
  <files>src/weather/psychrometrics.rs</files>
  <action>Implement calculate_humidity_ratio() function:
- Input: dry_bulb (°C), relative_humidity (0-100), pressure (Pa)
- Output: humidity ratio ω (kg_water_vapor / kg_dry_air)
- Formula: ω = (0.62198 × p_sat(T) × RH/100) / (P - p_sat(T) × RH/100)
  - Where RATIO_MW = 0.62198 (H2O/dry_air molecular weight ratio)
- Add doc comments with ASHRAE formula explanation
- Add inline test: test_humidity_ratio_reference_values() with ±1% tolerance

Per 16-RESEARCH.md Code Examples section.</action>
  <verify>cargo test test_humidity_ratio_reference_values --lib</verify>
  <done>Function exists, reference values pass within ±1% tolerance</done>
</task>

<task type="auto">
  <name>Task 5: Implement enthalpy calculation and export module</name>
  <files>src/weather/psychrometrics.rs, src/weather/mod.rs</files>
  <action>Implement calculate_enthalpy() function in psychrometrics.rs:
- Input: dry_bulb (°C), relative_humidity (0-100), pressure (Pa)
- Output: enthalpy h (kJ/kg)
- Formula: h = 1.006 × T + ω × (2501 + 1.86 × T)
  - Where CP_DRY_AIR = 1.006 kJ/(kg·K), LATENT_HEAT = 2501.0 kJ/kg, CP_WATER_VAPOR = 1.86 kJ/(kg·K)
  - ω from calculate_humidity_ratio()
- Add doc comments with ASHRAE exact formula explanation
- Add inline tests:
  - test_enthalpy_reference_values(): Validate 25°C/50% RH→50.4 kJ/kg, 20°C/80% RH→49.0 kJ/kg, 30°C/20% RH→36.3 kJ/kg (tolerance ±0.5 kJ/kg)
  - test_enthalpy_monotonic_with_temperature(): Verify enthalpy increases with temperature at fixed RH
  - test_enthalpy_monotonic_with_rh(): Verify enthalpy increases with RH at fixed temperature

Then update src/weather/mod.rs to export psychrometrics module:
- Add: `pub mod psychrometrics;` after existing sub-modules
- Add re-export: `pub use self::psychrometrics::*;` to expose all psychrometric functions at weather:: namespace
- Verify module compiles with: cargo check

Per 16-RESEARCH.md Pattern 3 (Enthalpy calculation), Code Examples section, and 16-CONTEXT.md "Module Placement" section.</action>
  <verify>cargo test test_enthalpy_reference_values test_enthalpy_monotonic_with_temperature test_enthalpy_monotonic_with_rh --lib && cargo check</verify>
  <done>Enthalpy function exists, reference values pass within ±0.5 kJ/kg tolerance, all monotonicity tests pass, psychrometrics module exported from weather crate</done>
</task>

</tasks>

<verification>
All psychrometric calculations produce accurate results:
- Saturation vapor pressure matches ASHRAE Fundamentals reference tables (±5 Pa)
- Dew point calculations always satisfy dew_point ≤ dry_bulb constraint
- Wet-bulb calculations converge across full T/RH range including high humidity (>90%)
- Humidity ratio calculations match reference values within ±1%
- Enthalpy calculations satisfy monotonicity properties (increase with T and RH)
- All reference value tests pass within specified tolerances (±0.5°C for temperatures, ±0.5 kJ/kg for enthalpy, ±1% for humidity ratio)
</verification>

<success_criteria>
1. psychrometrics.rs module exists with all 5 core calculation functions and comprehensive tests
2. All unit tests pass with reference value validation (130+ test points across T/RH grid)
3. All property tests pass verifying physical invariants (dew_point ≤ dry_bulb, enthalpy monotonicity)
4. psychrometrics module is properly exported from weather crate and accessible via weather:: namespace
5. No test failures or compilation warnings
</success_criteria>

<output>
After completion, create `.planning/phases/16-psychrometrics-module/16-01-SUMMARY.md`
</output>
