# Rust
target/
# Variant build dirs at repo root (e.g. target_rtk_test/, target_release_test/)
/target_*/
**/*.rs.bk
*.pdb

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.venv/
venv/
ENV/
env/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
# Accidental 'cd ~' typo dir at repo root (agent artifact)
/~
.DS_Store
*.iml
.classpath
.project
.settings/
*.sublime-project
*.sublime-workspace

# Editor config
.editorconfig

# Build artifacts
*.o
*.a
*.lib
*.cdtor.c
*.res

# Temporary files
tmp/
*.tmp
*.bak
*.orig
.cache/
.pytest_cache/
.coverage
htmlcov/
coverage/
*.profraw

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Project-specific
.azure/
.env
.env.local
*.copilotmd
cobertura.xml
worktrees/

# EnergyPlus simulation artifacts
energyplus/
runs/
tests/energyplus_data/

# Generated tool scripts (not source)
tools/create_case_900_osm.rb
tools/create_case_900_with_mcp.py
tools/generate_energyplus_idf.py
tools/list_mcp_tools.py
tools/mcp_client.py
tools/phase_25_parametric_sweep.py
tools/run_energyplus_simulations.py
tools/train_residual_model.py

# Generated example workflows
examples/analysis_workflow.py
examples/optimization_workflow.py
examples/validation_workflow.py

# Session debug/diagnostic binaries (auto-generated for debugging)
src/bin/session_*.rs
src/bin/case_*.rs
src/bin/debug_*.rs
src/bin/validate_*.rs
src/bin/tmp_*.rs

# Extra physics modules (experimental)
src/physics/combined_heat_balance.rs
src/physics/convection.rs
src/physics/ctf_flux_direction_test.rs
src/physics/multi_node_ctf.rs
src/physics/newton_solver.rs
src/physics/per_surface_ctf.rs
src/physics/per_surface_integration.rs
src/physics/per_surface_model.rs
src/physics/radiation.rs
src/physics/view_factor.rs
src/sim/solar_distribution.rs
src/validation/phase30_diagnostics.rs
src/validation/solar_diagnostics.rs

# Test helpers and diagnostics
tests/ashrae_140_*.rs
!tests/ashrae_140_validation.rs
!tests/ashrae_140_coverage.rs
!tests/ashrae_140_case_970_validation.rs
!tests/ashrae_140_case_600_660_plant.rs
!tests/ashrae_140_case_600.rs
!tests/ashrae_140_case_600_cz3.rs
!tests/ashrae_140_case_600_cz7.rs
!tests/ashrae_140_case_920.rs
tests/ashrae_140_*/      # Test subdirectories
tests/extract_hourly_*.rs
tests/test_multi_*.rs
tests/*_diagnostics/     # Test diagnostic directories
tests/ctf_*.rs           # CTF comparison tests
tests/phase2_*.rs       # Phase 2 diagnostic tests
tests/*_ORIG             # Backup/orig files

# Validation outputs and diagnostics
*.log
validation_*.txt
validation_results.json
diagnostics_*.csv
hourly_output*.csv
session_*_test_results.txt
*.profraw

# Benchmark results
benchmark_results.json
gate_status.json
validation_baseline.json

# Benchmark data
benchmarks/outputs/
benchmarks/energyplus/
benchmarks/weather/

# Analysis and investigation artifacts
**/*_ANALYSIS.md
**/*_INVESTIGATION.md
**/*_REPORT.md
**/*_PLAN.md
**/*_PROMPT.md
CONTINUATION_PROMPT*.md

# Session scripts
scripts/session_*.sh

# Tools/scripts (diagnostic and analysis)
tools/analyze_*.py
tools/compare_*.py
tools/debug_*.py
tools/diagnose_*.py
tools/extract_*.py
tools/phase30_*.py
tools/quick_*.py
tools/replicate_*.py
tools/run_*.py
tools/solar_*.py
tools/statistical_*.py
tools/test_*.py
tools/validate_*.py
tools/verify_*.py
tools/energyplus_*.csv

# Planning artifacts that shouldn't be committed
.planning/phases/
.planning/debug/
.planning/V07_RELEASE_DECISION.md
*_profile_hourly.csv

# Auto Claude data directory
.auto-claude/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/
benches/orchestration_decisions/baselines/current_tdqs.json

# Bernstein-agent runtime state (resolves the design decision raised in
# issue #2837).  `.sdd/` is the Bernstein agent-orchestration runtime —
# `attestations/`, `traces/`, `index/`, `memory/`, `agents/`, `routing/`,
# `caching/`, `backlog/`, `metrics/`, `upgrades/`, `auth/`, `archive/`,
# `default/`, `knowledge/file_index.json`, and `knowledge/decisions.jsonl`
# are all per-developer / per-session state (Ed25519 signatures, JSONL
# trace logs, SQLite codebase caches, agent catalog snapshots, etc.).
# The previous `.sdd/runtime/` ignore was the original intent — issue
# #2837 generalized it to the whole directory because:
#   1. The only "doc-like" file (`.sdd/knowledge/architecture.md`) is an
#      auto-generated file index from the Bernstein tool, not
#      human-authored documentation.  Its drift was the bug #2818/#2828
#      sweep had to fix, and a CI freshness gate would just re-create
#      the maintenance burden in a different place.
#   2. `.sdd/index/codebase.db` (26 MB) and the JSONL trace files are
#      genuinely per-machine session state and have no business in git.
#   3. Templates and scripts that reference `.sdd/` paths (e.g.
#      `templates/skills/bernstein-signal-check.md`,
#      `scripts/autonomous_parameter_sweep.py`) write runtime data and
#      do not depend on the files being tracked.
# Local agents and developers regenerate their `.sdd/` state on first run;
# no shared canonical view is lost because none existed — every Bernstein
# run rebuilt the index from scratch.
.sdd/

# Bernstein-agent auto-generated session file (per AGENTS.md line 218).
# The root `.md` policy gate (`scripts/check_root_md_policy.py`) only
# *warns* on this file; the `.gitignore` line is the real defense that
# prevents it from ever being staged.  See issue #2466.
CLAUDE.md

# ONNX model artifacts (issue #1335) — pinned via tests/surrogate_models/registry.json, never committed
*.onnx
models/

# FMI 2.0 XSD cache used by `.agents/results/issue-D1-multi-zone-fmi-verification.py`.
# Fetched on demand from modelica/fmi-standard; never committed.
.agents/results/fmi-xsd/
