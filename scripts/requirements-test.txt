# Test dependencies for OSimFlow Python orchestration scripts (Issue #1847).
# Keep in sync with the root `requirements-dev.txt`.

# Core test framework + plugins referenced by scripts/pytest.ini
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# State store imports `boto3` defensively (try-import) at module-load time.
# Pull it in for CI so the conditional import resolves to a real client
# that we can mock, instead of the `None` fallback.
boto3>=1.34.0
botocore>=1.34.0

# YAML parsing — required by `scripts/release_gate_checker.py` (top-level
# `import yaml`) and by `scripts/autonomous_parameter_sweep.py::load_brief_spec`
# when a `--brief path/to/brief.yaml` is supplied (Issue #2951). Keep aligned
# with the `pyyaml>=6.0` pin in `requirements-dev.txt`.
pyyaml>=6.0

# NumPy — required at pytest *collection* time by
# `scripts/ci/test_generate_training_data.py` (top-level `import numpy`,
# added by Issue #3454). Without it the Scripts Test Suite fails during
# collection ("ModuleNotFoundError: No module named 'numpy'") before any
# test runs. Keep aligned with the numpy pin in `requirements-dev.txt`.
numpy>=1.24.0

# tqdm — imported by `scripts/generate_training_data.py` (progress bars,
# Issue #3454's companion module). Its absence surfaces as an ERROR at
# pytest *setup* of the generate-training-data tests even after numpy is
# restored. Keep aligned with the tqdm>=4.65.0 pin in `requirements-dev.txt`.
tqdm>=4.65.0
