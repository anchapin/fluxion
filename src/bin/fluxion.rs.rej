--- src/bin/fluxion.rs
+++ src/bin/fluxion.rs
@@ -395,9 +395,9 @@
         // Diagnostics mode commands
         Some(("diagnostics", sub_matches)) => match sub_matches.subcommand() {
             Some(("cases-195-470", _)) => {
-                #[cfg(test)]
+                #[cfg(not(test))]
                 {
-                    use fluxion::validation::diagnostics;
+                    use fluxion::validation::diagnostics;
                     let result = diagnostics::run_cases_195_470();
                     match result {
                         Ok(_) => println!("Cases 195, 470, 920 diagnostic passed"),
@@ -406,7 +406,7 @@
                 }

-                #[cfg(not(test))]
+                #[cfg(test)]
                 {
                     println!("Diagnostic mode is only available in test builds");
                 }
             }
