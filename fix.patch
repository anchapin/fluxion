--- src/ai/shared_batch_service.rs
+++ src/ai/shared_batch_service.rs
@@ -213,20 +213,18 @@
         let mut handles = Vec::new();
         let n_requests = 10;

         for i in 0..n_requests {
             let service = service.clone();
             let handle = thread::spawn(move || {
                 let rx = service.submit(vec![i as f64, (i as f64 + 1.0)]);
-                rx.recv()
+                let _ = rx.recv();
             });
             handles.push(handle);
         }

         // Wait for all to complete.
         for h in handles {
             let _ = h.join();
         }

         // The service should have processed all requests. Since we don't have
         // an easy way to count batch calls without a mock, we just verify
         // correctness.
     }
