{
  "history": [
    {
      "actual": 2485
    }
  ],
  "max_lines": 2485,
  "notes": "Issue #3574: extend module-size gate to src/python/model_bindings.rs, which crossed the smallest ratcheted threshold after Issue #3543. Current size snapshotted as the historical maximum; the bound can only tighten (lowered via the YAML `max_lines` field). Companion cleanup work that decomposes this module is tracked separately and is out of scope for #3574."
}